================================================================================
  TOWER DEFENSE — GAME DESIGN, TECHNICAL REFERENCE & DEVELOPMENT ROADMAP
  Last updated: 2026-05-26  (session 11 — Phase 10 Offset Refactor + Codebase Logic Sync)
================================================================================


════════════════════════════════════════════════════════════════════════════════
 1. SCENE OVERVIEW — DTGamePlay.unity
════════════════════════════════════════════════════════════════════════════════

File: Assets/3.Scenes/DTGamePlay.unity

── Camera ──────────────────────────────────────────────────────────────────────
  GameObject         : Main Camera
  Projection         : Orthographic  ← switched from Perspective (session 4)
  orthographicSize   : 14
  Position           : (23.3, 15, 2.02)
  Rotation Euler     : X=30°, Y=0°, Z=0°   ← pitch 30°
  Aspect target      : 16:9 (1920×1080)

  Visible area on ground plane (Y=0):
    Width  X : 2 × 14 × (16/9) ≈ 49.8 world units  → grid 42u fits with buffer
    Depth  Z : camera sees Z ≈ 4..52 at ground
    Camera center on ground: Z = 2.02 + 15/tan(30°) ≈ 28

── Ground / Playable area ──────────────────────────────────────────────────────
  GameMapVisualize
    Type       : Unity Plane mesh (native 10×10)
    Scale      : (4.2, 5, 2.4)  → 42u × 24u world
    Position   : (23.3, 0, 28)
    Bounds X   : [2.3, 44.3]
    Bounds Z   : [16, 40]

── Environment objects (ArkEnvironment children) ───────────────────────────────
  CityGround   : large plaza floor (~100×72u) — NOT touched, base layer
  Sky          : skybox — NOT touched
  Buildings    : moved closer via EnvironmentContractor (factor 0.5 from grid center)
  Cliffs       : moved closer via EnvironmentContractor
  Mountains    : MtnL/MtnR pushed to Z=130, MtnC to Z=140 (far backdrop)
  Pillars      : top-edge items (Z 35-65) pushed to Z=50-60 range
  Vegetation   : tall trees (mesh > 1u) pushed to Z=50-60 (backdrop peek)
  Pipes/Beams  : contracted by EnvironmentContractor

── Spawn / PathView ────────────────────────────────────────────────────────────
  PathView/CreatePoint  : pos (2, 0, 29)
    → Just outside plane left edge (plane X starts at 2.3)
    → Z=29 aligned with START row world Z = 6×2 + 17 = 29

── Gates (Phase 4.5 + 4.6 — DONE) ─────────────────────────────────────────────
  GateStart  : spawned at world (3.3, 0, 29) = grid (0,6) on game start
  GateEnd    : spawned at world (43.3, 0, 29) = grid (20,6) on game start
  → Instantiated runtime bởi TDEnemyPathMainView.SpawnGates()
  → Visual: 3D wireframe box (Arknights-style), redesigned session 6 (Phase 4.6)


════════════════════════════════════════════════════════════════════════════════
 2. GRID SYSTEM
════════════════════════════════════════════════════════════════════════════════

  CONFIG_GRID_CELL_SIZE = 2f                  (TDConstant.cs)
  Plane bounds         = (42, 24) world units (scale 4.2×2.4 × Unity plane 10)
  Grid width  = Floor(42 / 2f) = 21           (TDGridMainModel.cs runtime)
  Grid height = Floor(24 / 2f) = 12           (TDGridMainModel.cs runtime)
  Total cells = 252

  Offset calculation (TDGridMainModel.cs):
    offsetX = planePos.x - (mapSize.x/2) + (cellSize/2) = 23.3 - 21 + 1 = 3.3
    offsetZ = planePos.z - (mapSize.z/2) + (cellSize/2) = 28   - 12 + 1 = 17

  Cell (x,y) → world = (x*2 + 3.3,  0,  y*2 + 17)
    Cell (0,  0)  → world (3.3,  0, 17)   ← bottom-left
    Cell (20, 11) → world (43.3, 0, 39)   ← top-right
    Cell (0,  6)  → world (3.3,  0, 29)   ← START / GateStart
    Cell (20, 6)  → world (43.3, 0, 29)   ← END   / GateEnd


════════════════════════════════════════════════════════════════════════════════
 3. CODE ARCHITECTURE
════════════════════════════════════════════════════════════════════════════════

  Pattern : MVC-like (Model / Control / View)
  DI      : ServiceLocator + Injector (Assets/2.Scripts/Services/)
  Events  : C# Action delegates

  ── Model ───────────────────────────────────────────────────────────────────
  TDConstant.cs                    ← all config values (grid, path zones, wave timing, prefabs, lives)
  TDGridMainModel.cs               ← world-space grid positions, snap, occupancy
                                      + CellToWorld / WorldToCell / IsInBounds [Phase 10]
  TDGridDTO.cs                     ← logical width×height IGridCellDTO[,] grid
  TDGridCellDTO.cs                 ← cell data: position(x,y), isWalkable, CellType
  TDaStarPathModel.cs              ← heuristic (Manhattan: dx+dy), GetNeighbors
  TDFlyweightBulletFactoryModel.cs ← lazy pool per TowerType (Flyweight), bullet Spawn/Return
  TDFlyweightTowerDataSettings.cs  ← ScriptableObject; TowerData: bulletPrefab, cost, damage,
                                      attackSpeed, rangeOffsets (Vector2Int[]) [Phase 6.2 + 10]
  TDTowerBehaviorModel.cs          ← reads damage/attackSpeed/range dari SO; GetTowerRange → TDOffsetRangeDTO
  TDRangeDTO.cs                    ← abstract base; virtual GetCellsInRange (bounding-box) [Phase 10 NEW]
  TDOffsetRangeDTO.cs              ← Arknights-style explicit cell offsets, O(n), 4-dir auto-rotate [Phase 10 NEW]

  Platform: Android Mobile (Legacy Input System — activeInputHandler=0)
             → không dùng com.unity.inputsystem package
             → dùng Input.GetMouseButtonDown + EventSystem.IsPointerOverGameObject(fingerId)

  ── Control ─────────────────────────────────────────────────────────────────
  TDGameplayMainControl.cs   ← thin orchestrator
  TDPauseControl.cs          ← Toggle/Pause/Resume + Time.timeScale; onPauseChanged [Phase 8.5 NEW]
  TDPlayerLifeControl.cs     ← LoseLife(), onLifeChanged, onGameOver [Phase 4.5]
  TDEnemyPathMainControl.cs  ← InitEnemyPath, GenerateAllPaths, ComputeValidTowerCells,
                                InitEnemyPool (ObjectPool), ReturnEnemy, PauseAwareDelay,
                                StartWaveLoop, SpawnWave, onValidTowerCellsReady,
                                onWaveStart events [Phase 4.5 + 8.5]
  TDaStarPathControl.cs      ← ComputePath (A*), BuildDefinedPath (straight segments)
  TDPathGeneratorControl.cs  ← random waypoint generator per Y-zone
  TDObstacleControl.cs       ← place obstacles on non-path cells with spacing
  TDEnemyPathControl.cs      ← CreatePath() → instantiate Plane tiles on grid
  TDEnemyControl.cs          ← convert IGridCellDTO path → Vector3 world positions
  TDEnemyRegistry.cs         ← centralized enemy list; Register/Unregister/GetAll [Phase 6.1 NEW]
  TDTowerMainControl.cs      ← select tower, rotate (E/Q), place
  TDPlaceTowerControl.cs     ← raycast placement, occupancy check
  TDTowerFactoryControl.cs   ← factory spawn + init
  TDTowerBehaviorMainControl.cs  ← attack cooldown timer, onGetLastAttackTime event
  TDTowerBehaviorSubControl.cs   ← spawn bullet từ đúng pool theo type [Phase 6.1 refactor]
  TDUserInputControl.cs      ← mobile/desktop input event dispatching

  ── View ────────────────────────────────────────────────────────────────────
  TDGameplayMainView.cs      ← scene entry, init grid + life system, screen flash
                               [Phase 4.5: Initialize life, subscribe onLifeChanged/onGameOver]
  TDEnemyPathMainView.cs     ← path events + obstacle wiring + SpawnGates()
                               [Phase 4.5: m_GateStartPrefab/EndPrefab, OnWaveStart]
  TDEnemyPathView.cs         ← Plane tile pool
  TDEnemyView.cs             ← enemy MonoBehaviour: move along path, reach-end → LoseLife
                               [Phase 4.5: m_HasReachedEnd guard]
                               [Phase 6.1: PathProgress property, Register/Unregister registry]
  TDTowerMainView.cs         ← 5 tower holders, input handler, green highlight tiles
  TDTowerWeaponView.cs       ← placed tower behavior: tick scan (0.2s) + PathProgress priority
                               IsInRange per frame (clears target if out of range) [Phase 6.1]
                               Range nguồn từ TDOffsetRangeDTO [Phase 10]
  TDTowerHolderView.cs       ← HUD slot (button + cost)
  TDGateView.cs              ← GateStart MonoBehaviour: PlaySpawnEffect() per wave [Phase 4.5]
  TDGateEndView.cs           ← GateEnd MonoBehaviour: trigger visual + life text [Phase 4.5]
  [DELETED] TDEnemyDetector.cs ← removed Phase 6.1 (replaced by Registry+tick scan)


════════════════════════════════════════════════════════════════════════════════
 4. GAME INIT FLOW (Current Implementation)
════════════════════════════════════════════════════════════════════════════════

  ┌──── GAME START (1 lần) ──────────────────────────────────────────────┐
  │                                                                       │
  │  TDControl.InitOtherControl()                                         │
  │    ├─ TDPlayerLifeControl.api = new TDPlayerLifeControl()  [4.5]     │
  │    └─ ... (tất cả control khác)                                       │
  │                                                                       │
  │  TDGameplayMainView.Start()                                           │
  │    ├─ CheckSceneLoaded()                                              │
  │    ├─ TDPlayerLifeControl.api.Initialize()  → lives=20, fire event   │
  │    ├─ Subscribe onLifeChanged → UpdateLifeText + FlashScreen          │
  │    ├─ Subscribe onGameOver   → Log (TODO Phase 8: GameOver panel)     │
  │    └─ InitGameplay()                                                  │
  │         ├─ TDGridMainModel.Initialize(mapSize, planePos)              │
  │         ├─ CreateGrid()  → grid 21×12                                 │
  │         └─ TDGameplayMainControl.InitEnemyPath(view, gridDTO)        │
  │                                                                       │
  │  TDEnemyPathMainView.Initialize()                                     │
  │    └─ TDEnemyPathMainControl.api.InitEnemyPool(m_Slime, transform)   │
  │         ObjectPool cap=20 maxSize=50; createFunc: Instantiate+scale   │
  │                                                                       │
  │  TDEnemyPathMainView.ImplementPath()  [async]                         │
  │    ├─ await createGridCompletion                                      │
  │    ├─ RegistryEvents()  (incl. onWaveStart → PlaySpawnEffect)         │
  │    ├─ InitEnemyPath(start=0,6  end=20,6)                             │
  │    ├─ SpawnGates()  [Phase 4.5]                                       │
  │    │    ├─ Instantiate GateStart at grid[0,6] = (3.3, 0, 29)         │
  │    │    └─ Instantiate GateEnd   at grid[20,6] = (43.3, 0, 29)       │
  │    ├─ GenerateAllPaths(start, end) → 3 corridors                      │
  │    └─ PlaceObstacles → ComputeValidTowerCells                         │
  │                                                                       │
  └───────────────────────────────────────────────────────────────────────┘

  ┌──── PER WAVE (10 waves total) ───────────────────────────────────────┐
  │                                                                       │
  │  StartWaveLoop:                                                       │
  │    for waveIdx in 0..CONFIG_MAX_WAVES (10):                           │
  │      pathIdx = Random.Range(0, allPaths.Count)                        │
  │      onWaveStart?.Invoke(waveIdx)  → GateView.PlaySpawnEffect() [4.5]│
  │      await SpawnWave(spawn, wavePath, waveIdx, ct)                    │
  │      await PauseAwareDelay(CONFIG_WAVE_INTERVAL_MS=10000, ct) [8.5]  │
  │                                                                       │
  │  SpawnWave:                                                           │
  │    for i in 0..CONFIG_ENEMIES_NUMBER (5):                             │
  │      enemy = m_EnemyPool.Get()   ← Object Pool [Phase 8.5]           │
  │      enemy.position = spawnPos; rotation = identity                   │
  │      key = "w{waveIdx}-e{i}-{instanceId}"                            │
  │      enemy.Initialize(key) → reset state + Subscribe + Register      │
  │      enemy.SetPath(wavePath)                                          │
  │      await PauseAwareDelay(CONFIG_ENEMY_SPAWN_DELAY_MS=2000, ct)     │
  │                                                                       │
  └───────────────────────────────────────────────────────────────────────┘

  ── Key design decisions (locked) ─────────────────────────────────────
    - Maze cố định: 3 corridors gen 1 lần lúc start
    - Enemy là WAYPOINT FOLLOWER (không A* runtime)
    - Khi enemy hết path → LoseLife() + ReturnToPool() (không Destroy) [Phase 8.5]
    - Khi enemy chết (TakeDamage ≤ 0) → ReturnToPool() (không Destroy) [Phase 8.5]
    - PauseAwareDelay: Task.Yield() loop cộng Time.deltaTime → tự pause khi timeScale=0 [Phase 8.5]
    - GateEnd trigger chỉ play visual effect (safety net, không LoseLife lại)


════════════════════════════════════════════════════════════════════════════════
 5. PATH GENERATION ALGORITHM (TDPathGeneratorControl)
════════════════════════════════════════════════════════════════════════════════

  Input  : start (Vector2Int), end (Vector2Int), yMin, yMax (zone bounds)
  Output : Vector2Int[] waypoints, mỗi cặp liên tiếp cùng X HOẶC cùng Y

  Algorithm:
    1. Enter zone: enterY = Random(yMin, yMax)
       if enterY != start.y → add (start.x, enterY)

    2. Zigzag loop:
       while (curX + minStep <= end.x - 1):
         stepX = Random(minStep, maxStep); nextX = min(curX+stepX, end.x-1)
         add (nextX, curY); curX = nextX
         nextY = PickDifferentY(curY, yMin, yMax, minDelta)
         add (curX, nextY); curY = nextY

    3. Exit: if curX != end.x → add (end.x, curY)

  Tunables:
    CONFIG_PATH_MIN_STEP_X         = 2
    CONFIG_PATH_MAX_STEP_X         = 5
    CONFIG_PATH_MIN_VERTICAL_DELTA = 2


════════════════════════════════════════════════════════════════════════════════
 6. BUILD DEFINED PATH ALGORITHM (TDaStarPathControl.BuildDefinedPath)
════════════════════════════════════════════════════════════════════════════════

  Input  : gridDTO, start, end, waypoints[]
  Output : List<IGridCellDTO>

  BuildDefinedPath:
    path=[]; cur=start
    foreach wp in waypoints: AppendStraightSegment(path, cur, wp); cur=wp
    AppendStraightSegment(path, cur, end)
    path.Add(cell(end))

  AppendStraightSegment: move X first, then Y (Manhattan walk, no A*)


════════════════════════════════════════════════════════════════════════════════
 7. TOWER SELECTION, HIGHLIGHT & PLACEMENT FLOW (Phase 4 — DONE)
════════════════════════════════════════════════════════════════════════════════

  ── 2-Phase Arknights Placement ────────────────────────────────────────────
  Phase 1: Drag ngón tay đến cell → ghost snap, m_IsDirectionSelected=false
  Phase 2: Swipe từ cell center vượt k_DirectionThresholdRatio×cellSize (0.35×2=0.7u) → direction chọn
           Ghost đóng băng tại ô hiện tại; direction tính từ frozen cell center
  Đổi sang cell mới → reset về Phase 1 (chỉ áp dụng trên mobile)

  MOBILE: TouchPhase.Ended + m_IsDirectionSelected=true → place
  DESKTOP: LMB place (không cần direction), E/Q rotate, RMB cancel

  Ghost scale: (1.0, 1.0, 1.0) — cellSize=2u

  ComputeRotationIndex(delta):
    |dx|≥|dz|: dx>0→90°(right), dx<0→270°(left)
    |dz|>|dx|: dz>0→0°(fwd), dz<0→180°(back)

  Bugs fixed A–I (xem Known Issues)


════════════════════════════════════════════════════════════════════════════════
 8. GATE START / GATE END (Phase 4.5 + 4.6 — DONE)
════════════════════════════════════════════════════════════════════════════════

  ── Visual Design (Phase 4.6 — Session 6) ────────────────────────────────────

  Arknights-style 3D wireframe box visible from 30° orthographic camera.
  Box dimensions: 2×2u footprint, 2.2u tall, raised +0.13u above ground
  (path tiles sit at y=0.1 → box bottom ring at y=0.13+0.035=0.165 → clears tiles)

  Box structure (12 edges + 5 face fills):
    4 corner posts  : Post_FL / Post_FR / Post_BL / Post_BR
                      size 0.07×2.20×0.07u, placed at (±hs, hh, ±hs)
    4 bottom ring   : Bot_Front / Bot_Back / Bot_Left / Bot_Right
                      horizontal bars at y = 0.035 + BOX_Y offset
    4 top ring      : Top_Front / Top_Back / Top_Left / Top_Right
                      horizontal bars at y = (2.20 - 0.035) + BOX_Y offset
    5 face fills    : Face_Front/Back/Left/Right (side quads) + Face_Top (cap)
                      transparent quads at 7% alpha sitting just outside edges

  Materials (Assets/1.Assets/Resources/Materials/Gates/):
    GS_Edge.mat  — URP/Unlit, Opaque,       red   (1.00, 0.10, 0.10, 0.90)
    GS_Face.mat  — URP/Unlit, Transparent,  red   (1.00, 0.10, 0.10, 0.07)
    GS_Glow.mat  — URP/Unlit, Transparent,  red   (1.00, 0.08, 0.08, 0.18)
    GS_Arrow.mat — URP/Unlit, Transparent,  red   (1.00, 0.45, 0.45, 0.50)
    GE_Edge.mat  — URP/Unlit, Opaque,       blue  (0.15, 0.65, 1.00, 0.90)
    GE_Face.mat  — URP/Unlit, Transparent,  blue  (0.15, 0.65, 1.00, 0.07)
    GE_Glow.mat  — URP/Unlit, Transparent,  blue  (0.15, 0.65, 1.00, 0.18)
    NOTE: All materials have _EMISSION disabled + _EmissionColor=black.
          Emission keyword overrides alpha in URP/Unlit → must keep off.

  ── Prefab Structure ─────────────────────────────────────────────────────────

  GateStart.prefab  (Assets/1.Assets/Resources/Prefabs/GateStart.prefab)
    ├─ [TDGateView]      m_SpawnEffect → SpawnParticles ✓
    ├─ GroundGlow        Quad flat y=0.01, 3.0×3.0, GS_Glow mat (red 18%)
    ├─ Post_FL/FR/BL/BR  4 corner posts, GS_Edge mat (red opaque), raised +0.13u
    ├─ Bot_Front/Back/Left/Right   bottom ring bars, GS_Edge mat, raised +0.13u
    ├─ Top_Front/Back/Left/Right   top ring bars, GS_Edge mat, raised +0.13u
    ├─ Face_Front/Back/Left/Right/Top   transparent face fills, GS_Face mat (7%)
    ├─ Arrow             Quad flat on floor, GS_Arrow mat (red 50%)
    └─ SpawnParticles    ParticleSystem — red burst 20p, plays per wave

  GateEnd.prefab  (Assets/1.Assets/Resources/Prefabs/GateEnd.prefab)
    ├─ [TDGateEndView]   m_HitEffect → HitParticles, m_LifeText → LifeText ✓
    ├─ [BoxCollider]     isTrigger=true, size (2,4,2), center (0,2,0)
    │                    → OnTriggerEnter: plays HitParticles (visual only, no LoseLife)
    ├─ HQProp            TowerCrystals prefab, scale 0.75
    ├─ GroundGlow        Quad flat y=0.01, GE_Glow mat (blue 18%)
    ├─ Post_FL/FR/BL/BR  4 corner posts, GE_Edge mat (blue opaque), raised +0.13u
    ├─ Bot_Front/Back/Left/Right   bottom ring bars, GE_Edge mat, raised +0.13u
    ├─ Top_Front/Back/Left/Right   top ring bars, GE_Edge mat, raised +0.13u
    ├─ Face_Front/Back/Left/Right/Top   transparent face fills, GE_Face mat (7%)
    ├─ CoreGlow          ParticleSystem — blue loop, continuous ambient glow
    ├─ HitParticles      ParticleSystem — blue burst 20p khi enemy chạm trigger
    └─ LifeText          TextMeshPro 3D, tilted 55° to face camera, "♥ 20" above top ring

  ── Life System Flow ─────────────────────────────────────────────────────────

  TDPlayerLifeControl (singleton):
    Initialize(): lives = CONFIG_PLAYER_STARTING_LIVES (20), fire onLifeChanged(20)
    LoseLife():   lives--, fire onLifeChanged(lives), if lives<=0 fire onGameOver()

  TDEnemyView.Update() — primary trigger:
    if m_CurrentPathIndex >= m_PathsPosition.Count && !m_HasReachedEnd:
      m_HasReachedEnd = true
      TDPlayerLifeControl.api.LoseLife()
      Destroy(gameObject)

  Guard: m_PathsPosition.Count == 0 → skip (path chưa assign, tránh false trigger)

  TDGameplayMainView subscribes:
    onLifeChanged(lives) → m_LifeText.text = "♥ {lives}" + FlashScreen()
    onGameOver()         → Debug.Log("GAME OVER")  [TODO Phase 8: GameOver panel]

  TDGateEndView.OnTriggerEnter(col):
    → TDEnemyView found → m_HitEffect.Play()  (visual only, no double LoseLife)

  ── FlashScreen Coroutine ─────────────────────────────────────────────────
    m_ScreenFlash.color = (1,0,0,0.35) → lerp alpha 0.35→0 over 0.35s → Color.clear
    m_ScreenFlash: full-screen Image trong Canvas (alpha=0 default)

  ── Inspector assignments (DONE) ─────────────────────────────────────────
    ✅ TDEnemyPathMainView → m_GateStartPrefab = GateStart.prefab
    ✅ TDEnemyPathMainView → m_GateEndPrefab   = GateEnd.prefab
    ✅ TDGameplayMainView  → m_LifeText        = TextMeshProUGUI (HUD life display)
    ✅ TDGameplayMainView  → m_ScreenFlash     = Image (full-screen overlay, alpha=0)


════════════════════════════════════════════════════════════════════════════════
 9. OBSTACLE PLACEMENT (TDObstacleControl)
════════════════════════════════════════════════════════════════════════════════

  Tunables:
    CONFIG_OBSTACLE_COUNT            = 12
    CONFIG_OBSTACLE_EXCLUSION_RADIUS = 2
    CONFIG_OBSTACLE_SPACING          = 3
  Inspector: TDEnemyPathMainView.m_ObstaclePrefabs[5]


════════════════════════════════════════════════════════════════════════════════
 10. WAVE SYSTEM
════════════════════════════════════════════════════════════════════════════════

  CONFIG_MAX_WAVES             = 10
  CONFIG_ENEMIES_NUMBER        = 5
  CONFIG_ENEMY_SPAWN_DELAY_MS  = 2000
  CONFIG_WAVE_INTERVAL_MS      = 10000
  Total: ~3.5 phút


════════════════════════════════════════════════════════════════════════════════
 11. ENEMY MOVEMENT (TDEnemyView)
════════════════════════════════════════════════════════════════════════════════

  State fields:
    m_PathsPosition    : List<Vector3>  (world positions của path)
    m_CurrentPathIndex : int
    m_HasReachedEnd    : bool           ← guard tránh LoseLife nhiều lần [4.5]
    m_HasBeenReturned  : bool           ← guard tránh double ReturnToPool [8.5]
    m_EnemyHealth      : CONFIG_ENEMY_HEALTH = 300f
    m_MoveSpeed        : CONFIG_ENEMY_MOVE_SPEED = 3f
    m_EnemyKey         : string         ← unique "w{wave}-e{i}-{instanceId}"

  PathProgress: m_CurrentPathIndex / m_PathsPosition.Count (0→1)
    → Tower dùng để ưu tiên enemy gần GateEnd nhất

  Initialize(key): reset tất cả state, Subscribe onGetEnemyPathPos, Registry.Register
  ReturnToPool():  guard m_HasBeenReturned → Unsubscribe → Unregister → Pool.Release [8.5]
  TakeDamage(dmg): health -= dmg; health ≤ 0 → ReturnToPool() [không Destroy]
  OnDestroy():     chỉ cleanup nếu !m_HasBeenReturned (scene unload safety)

  Update():
    guard: m_PathsPosition==null || Count==0 || m_HasReachedEnd → return
    if m_CurrentPathIndex < Count:
      MoveTowards(target, speed*dt)
      LookRotation(dir)
      if dist < 0.1f: m_CurrentPathIndex++
    else:
      m_HasReachedEnd = true
      TDPlayerLifeControl.api.LoseLife()
      ReturnToPool()   ← [Phase 8.5: không Destroy — pool release]


════════════════════════════════════════════════════════════════════════════════
 12. CONSTANTS REFERENCE (TDConstant.cs hiện tại)
════════════════════════════════════════════════════════════════════════════════

  ── Grid ─────────────────────────────────────────────────────────────────
  CONFIG_GRID_CELL_SIZE          = 2f
  CONFIG_PATH_MIN_GRID_Y         = 0
  CONFIG_ENEMY_START_POINT       = (0,  6)  → world (3.3,  0, 29)
  CONFIG_ENEMY_END_POINT         = (20, 6)  → world (43.3, 0, 29)

  ── Path generation ──────────────────────────────────────────────────────
  CONFIG_PATH_Y_ZONES            = [(7,11), (0,5), (2,9)]
  CONFIG_PATH_MIN_STEP_X         = 2
  CONFIG_PATH_MAX_STEP_X         = 5
  CONFIG_PATH_MIN_VERTICAL_DELTA = 2

  ── Obstacles ────────────────────────────────────────────────────────────
  CONFIG_OBSTACLE_COUNT             = 12
  CONFIG_OBSTACLE_EXCLUSION_RADIUS  = 2
  CONFIG_OBSTACLE_SPACING           = 3

  ── Wave system ──────────────────────────────────────────────────────────
  CONFIG_MAX_WAVES                  = 10
  CONFIG_ENEMIES_NUMBER             = 5
  CONFIG_ENEMY_SPAWN_DELAY_MS       = 2000
  CONFIG_WAVE_INTERVAL_MS           = 10000

  ── Enemy ────────────────────────────────────────────────────────────────
  CONFIG_ENEMY_MOVE_SPEED        = 3f
  CONFIG_ENEMY_HEALTH            = 300f
  CONFIG_ENEMY_VISUAL_SCALE      = 1.5f   (applied at spawn in SpawnWave)

  ── Player / Gates [Phase 4.5] ───────────────────────────────────────────
  CONFIG_PLAYER_STARTING_LIVES   = 20
  PREFAB_GATE_START              = "GateStart"
  PREFAB_GATE_END                = "GateEnd"

  ── Tower ─────────────────────────────────────────────────────────────────
  CONFIG_TOWER_ROTATIONS         = {0°, 90°, 180°, 270°}
  CONFIG_TOWER (ScriptableObject) = "Tower Bullet Config"

  ── Misc ─────────────────────────────────────────────────────────────────
  CONFIG_PATH_OFFSET_Y           = 0.1f
  CONFIG_NUM_PATHS               = 3   (legacy, use CONFIG_PATH_Y_ZONES.Length)

  ── Win/Lose (Phase 8 — TODO) ────────────────────────────────────────────
  CONFIG_PLAYER_STARTING_GOLD    = 100  ← TODO
  CONFIG_ENEMY_REWARD_GOLD       = 10   ← TODO


════════════════════════════════════════════════════════════════════════════════
 13. SCENE TUNING HISTORY
════════════════════════════════════════════════════════════════════════════════

  ── Camera evolution ───────────────────────────────────────────────────────
  Session 2-3 (Perspective):
    Start: pos (23.3,28,-0.79) pitch 60° FOV 55°
    Step1: pos (23.3,22,3) FOV 42° → Step2: pitch 50° → Step3: 48° → Step4: FOV 48°

  Session 4 (Orthographic):
    Step5: Perspective→Ortho, orthoSize=20, pitch 48→35°
    Step6: orthoSize=11 (too small) → grid resize 20×12→14×8, cellSize 2→3
    Step7: orthoSize=12, pitch 35→30°, Y 22→15, Z→2
    Step8: 14×8 cellSize=3 → 21×12 cellSize=2, orthoSize 12→14
    FINAL: pos (23.3,15,2.02), pitch=30°, orthoSize=14

  ── Plane evolution ────────────────────────────────────────────────────────
  Session 2-3: (5,5,5)→(3,5,3)→(4,5,3)→(4,5,2.4) pos (23.3,0,28) → 40×24
  Session 4: (6,5,3.6)→(4.2,5,2.4) → world 42×24, grid 21×12  (FINAL)

  ── Tower ghost scale history ──────────────────────────────────────────────
  0.5f → 1.5f (session 4) → 1.0f (session 4 final)

  ── Enemy scale ────────────────────────────────────────────────────────────
  CONFIG_ENEMY_VISUAL_SCALE: 2.5f (cellSize=3) → 1.5f (cellSize=2 final)


════════════════════════════════════════════════════════════════════════════════
 14. DEVELOPMENT ROADMAP — UPDATED STATUS
════════════════════════════════════════════════════════════════════════════════

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 0 — Cleanup & Grid Sync                          [✅ DONE]        ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    ✅ CONFIG_GRID_CELL_SIZE = 2f synced
    ✅ Grid runtime dynamic từ Plane bounds
    ✅ Camera + plane + grid offset match

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 1 — Bug Fixes + A* Refactor                      [✅ DONE]        ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    ✅ 5 bugs fixed (A* heuristic, ComputePath, spawn delay, LookRotation,
       InitEnemyPath round-trip)

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 2 — Multi-path Foundation                        [✅ DONE]        ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    ✅ Random waypoints per Y-zone, 3 corridors

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 3 — Wave System + Random Path per Wave           [✅ DONE]        ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    ✅ 10 waves, random corridor per wave, 5 enemies stagger 2s
    ✅ CancellationToken added (session 8) — không còn exception khi scene unload

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 3.5 — Obstacles Decoration                       [✅ DONE]        ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    ✅ 12 obstacles, tránh path/exclusion zone, 5 prefab variants

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 3.6 — Camera + Environment Tuning (Perspective)  [✅ DONE]        ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    ✅ EnvironmentContractor, TopEdgePusher, PullBack
    ✅ Mountains Z=130-140

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 3.7 — Camera Orthographic + Grid Resize          [✅ DONE]        ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    ✅ Perspective → Orthographic, orthoSize=14
    ✅ Pitch 48°→30°, Y=22→15
    ✅ Grid 20×12 cellSize=2 → 21×12 cellSize=2, plane 42×24
    ✅ Enemy scale 1.5, tower ghost scale 1.0, path tile scale dynamic 0.2

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 4 — Tower Highlight + Arknights Placement        [✅ DONE]        ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    ✅ 2-phase placement: drag→cell (P1), swipe→direction (P2), lift=place
    ✅ m_IsDirectionSelected + m_LastSnappedPos
    ✅ Mobile: lift chỉ place khi direction đã chọn
    ✅ Desktop: LMB/RMB; direction auto-follow chuột
    ✅ ValidTowerCells highlight, obstacles SetOccupiedCell

    ── Direction Selection (Session 8 rewrite) ──────────────────────────────
    Platform-split logic trong UpdateGhostTransform():

    MOBILE (#if UNITY_ANDROID && !UNITY_EDITOR):
      Phase 1 — ghost follows ngón tay (snap to grid)
      Phase 2 — ngón tay > 35% cellSize (0.7f) từ cell center:
        → ghost ĐÓNG BĂNG tại ô hiện tại
        → direction lock, tính từ frozen cell center (không phải snapped pos)
        → direction update theo quadrant khi ngón tay tiếp tục di chuyển
        → lift ngón tay → place (chỉ khi m_IsDirectionSelected=true)
      Reset: place hoặc cancel

    DESKTOP / EDITOR (#else):
      Ghost luôn follow chuột tự do (không đóng băng)
      Direction luôn follow chuột — cập nhật ngay khi sqrMagnitude > 0.01f
      Không cần threshold — tower luôn quay về phía chuột đang ở
      LMB đặt bất kỳ lúc nào, RMB cancel

    Key fix: threshold cũ (1.0f = cellSize/2) = snap boundary → direction
             không bao giờ lock được trên mobile (reset ngay khi threshold hit).
             Mới: threshold=0.35f×cellSize=0.7f < snap boundary 1.0f → OK

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 4.5 — Gate Start / Gate End                      [✅ DONE]        ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    ✅ TDPlayerLifeControl: LoseLife(), onLifeChanged, onGameOver
    ✅ TDControl: TDPlayerLifeControl.api registered
    ✅ TDEnemyView: m_HasReachedEnd guard + LoseLife + Destroy khi hết path
    ✅ TDEnemyPathMainControl: onWaveStart event (fires trước mỗi wave)
    ✅ TDEnemyPathMainView: SpawnGates() + OnWaveStart → PlaySpawnEffect
    ✅ TDGameplayMainView: Initialize life, onLifeChanged→text+flash, onGameOver
    ✅ TDGateView.cs: PlaySpawnEffect() trên GateStart
    ✅ TDGateEndView.cs: trigger visual + life text update

    ✅ GateStart.prefab (Phase 4.5 original):
       SM_Gen_Env_Cliff_Arch_01 (scale 0.35) + cyan glow + green arrow
       + SpawnParticles (cyan burst) + TDGateView (m_SpawnEffect assigned)
       → Redesigned in Phase 4.6 (see below)

    ✅ GateEnd.prefab (Phase 4.5 original):
       TowerRoofedKeep (scale 1.0) + red/orange glow
       + BoxCollider trigger (2,4,2) + HitParticles + CoreGlow (loop)
       + LifeText TextMeshPro "♥ 20" + TDGateEndView (fields assigned)
       → Redesigned in Phase 4.6 (see below)

    ✅  Inspector assignments hoàn tất:
       → TDEnemyPathMainView: m_GateStartPrefab, m_GateEndPrefab
       → TDGameplayMainView:  m_LifeText (TextMeshProUGUI), m_ScreenFlash (Image)

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 4.6 — Gate Visual Redesign (Session 6)           [✅ DONE]        ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    Goal: Arknights-style 3D wireframe box with visible depth from 30° ortho cam.
    Previous design (Arch prop + flat glow quad) lacked 3D depth cues.

    ✅ GateStart.prefab rebuilt — 3D wireframe box, red color scheme:
       GroundGlow (flat quad) + 4 corner posts + 4 bottom ring bars
       + 4 top ring bars + 5 transparent face fills + direction Arrow
       + SpawnParticles (red burst) + TDGateView (m_SpawnEffect preserved)

    ✅ GateEnd.prefab rebuilt — 3D wireframe box, blue color scheme:
       HQProp (TowerCrystals) + GroundGlow + 12 edge bars + 5 face fills
       + CoreGlow (loop) + HitParticles (burst) + LifeText (face-camera)
       + BoxCollider trigger (2,4,2) + TDGateEndView (fields preserved)

    ✅ 7 new URP/Unlit materials — Assets/1.Assets/Resources/Materials/Gates/
       GS_Edge, GS_Face, GS_Glow, GS_Arrow (red)
       GE_Edge, GE_Face, GE_Glow (blue)
       All: _EMISSION disabled, _EmissionColor=black (emission breaks alpha in URP)

    ✅ Box raised +0.13u above ground (path tiles at y=0.1 → no overlap)
       GateRaiseBox.cs Editor script applied then self-deleted

    ✅ Path tiles skip gate cells:
       TDEnemyPathView.VisualizeAllPaths → path.GetRange(1, path.Count-2)
       → cell index 0 (GateStart) and last (GateEnd) excluded from tile rendering

    ✅ URP transparency rules documented:
       • _EMISSION keyword completely overrides alpha — must disable on all mats
       • Opaque materials (_Surface=0) ignore alpha — must use _Surface=1 (Transparent)

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 5 — Multi-start / Màn 2                          [TODO]            ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    CONFIG_START_POINTS = Vector2Int[] nhiều điểm spawn
    Mỗi start → 1-2 paths riêng đến END

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 6 — Tower UI Integration                         [⚠️ PARTIAL]      ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    ✅ 5 TowerHolder HUD slots, Arknights 2-phase placement
    ✅ TDPlaceTowerControl, TDTowerWeaponView
    ✅ TDTowerFactoryView.OnCreateTowerSuccess → tower.Init(key) [đã fix]
    ✅ CanvasScaler Reference Resolution: 1125×750 → 1920×1080  [Session 9]
       + HUD tất cả elements scale ×1.5 để bù lại (xem Phase 6.3 bên dưới)

    ⚠️  TODO: PlacementPanel missing trong scene hierarchy (mobile Cancel button)

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 6.1 — Tower Attack System Redesign (Session 7)   [✅ DONE]        ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    Goal: Fix bug pool sai type, thay trigger detection bằng Registry+tick scan.

    ✅ Bug A fix — Bullet pool global state:
       TDFlyweightBulletFactoryModel: xóa m_TowerType global, 1 pool lazy per TowerType
       TDFlyweightTowerDataSettings:  xóa m_Prefab/SetPrefab, thêm GetPrefab(type) pure lookup
       TDBulletsView:                 thêm OwnerType property để ReturnToPool đúng pool
       TDTowerBehaviorSubControl:     xóa SetupSubControl(), Attack() dùng Spawn(type)
       TDTowerFactoryView:            xóa SetupSubControl() call
       TDTowerBehaviorModel:          xóa dead settings property
       TowerData.prefab → bulletPrefab [FormerlySerializedAs]

    ✅ Enemy Detection redesign — Registry + Tick-based (Hướng 3):
       TDEnemyRegistry (NEW):  singleton List<TDEnemyView>, Register/Unregister/GetAll
       TDEnemyView:            + PathProgress = currentIndex/totalCount (0→1)
                                 Register(this) trong Initialize, Unregister trong OnDestroy
       TDTowerWeaponView:      tick scan mỗi 0.2s từ Registry
                                 pick enemy PathProgress cao nhất (gần GateEnd nhất)
                                 IsInRange check mỗi frame → tự clear target khi out of range
       ITowerRangeDTO:         + DetectionRadius property (implemented bởi cả 3 DTO)
       TDEnemyDetector.cs:     DELETED — không còn cần trigger collider trên prefab

    ✅ Lợi ích khi scale:
       Wave nhiều enemy: O(n) registry iteration × 5 scan/s (không phụ thuộc framerate)
       Tower mới: chỉ cần TowerType enum + SO entry + model data, không cần setup prefab trigger
       Multi-tower correct: mỗi tower độc lập, pool không bao giờ cross-contaminate

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 6.2 — Tower Config SO Refactor (Session 8)       [✅ DONE]        ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    Goal: Xóa hardcode damage/attackSpeed/range trong C# switch → đưa vào SO.
          Thêm tower mới chỉ cần 1 enum line + 1 SO entry, không đụng code.

    ✅ RangeType enum thêm vào Enums.cs: Area | HorizontalCone | VerticalCone
       ⚠️ REMOVED session 11 (Phase 10) — replaced bởi rangeOffsets approach

    ✅ TowerData struct (TDFlyweightTowerDataSettings.cs) — session 8:
       + damage       : float
       + attackSpeed  : float
       + rangeType    : RangeType  ← REMOVED session 11
       + rangeValue   : int        ← REMOVED session 11
       → session 11: REPLACED bởi rangeOffsets : Vector2Int[]
       GetData(type) pure lookup; GetPrefab/GetCost delegate sang GetData

    ✅ TDTowerBehaviorModel đọc từ SO (bỏ 3 switch hardcode):
       GetDamage()      → Setting.GetData(type).damage
       GetAttackSpeed() → Setting.GetData(type).attackSpeed
       GetTowerRange()  [session 8]  → rangeType switch → new [Area/Horizontal/Vertical]RangeDTO(rangeValue)
       GetTowerRange()  [session 11] → new TDOffsetRangeDTO(data?.rangeOffsets)  ← 1 dòng

    ✅ SO Resources/Configs/Tower Bullet Config.asset filled (session 8 values, session 11 updated):
       Cannon:    damage=5,  speed=3 | rangeOffsets: (1,0)(2,0)(3,0)
       Catapult:  damage=20, speed=1 | rangeOffsets: (1,-1)(1,0)(1,1)(2,-1)(2,0)(2,1)
       MissileG02:damage=10, speed=1 | rangeOffsets: (1,0)(2,0)(3,0)(4,0)
       MissileG03:damage=20, speed=1 | rangeOffsets: (1,-1..1)(2,-2..2)(3,-1..1) — 11 cells
       Mortar:    damage=15, speed=2 | rangeOffsets: (2,-1..1)(3,-1..1)(4,0) — 7 cells

    ✅ SO duplicate xóa: Assets/2.Scripts/Model/Config/SO/Tower Bullet Config.asset
       DTLoadFirst.unity GUID updated → trỏ đúng Resources/Configs/ file

    ✅ TDVerticalRangeDTO fix (copy-paste bug từ session 7):
       Cũ: forward cone dot > 0.9 (giống hệt HorizontalCone — sai)
       Mới: Mathf.Abs(Dot(right, toEnemy)) > 0.7f
       → Catapult tấn công theo trục NGANG (vuông góc forward) — bao phủ trái+phải

  ─────────────────────────────────────────────────────────────────────────────

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 6.3 — HUD Scale Fix for 1920×1080 (Session 9)   [✅ DONE]        ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    Goal: Sau khi đổi CanvasScaler từ 1125×750 → 1920×1080, HUD quá nhỏ.
          Scale tất cả elements ×1.5 (= 1080/750) để khôi phục apparent size.

    ── Vấn đề gốc ────────────────────────────────────────────────────────────
    TowerMainView có HorizontalLayoutGroup (childControlWidth/Height=True).
    → LayoutElement trên mỗi TowerHolder mới là nguồn kích thước thực tế,
      không phải RectTransform.sizeDelta trực tiếp.
    → Manual anchoredPosition set bị ignore hoàn toàn bởi HLG.

    ── Thay đổi (×1.5 so với giá trị cũ) ────────────────────────────────────
    TxtLifePoint (♥ 20):
      sizeDelta       : (200, 50)  → (300, 75)
      anchoredPosition: (0, -36)   → (0, -54)
      fontSize        : 36         → 54

    TowerMainView container:
      sizeDelta       : (686, 162) → (1029, 243)
      anchoredPosition: (20, 20)   → (30, 30)

    TowerMainView / HorizontalLayoutGroup:
      padding (L/R/T/B): 6        → 9
      spacing           : 6       → 9

    TowerHolder ×5 (via LayoutElement):
      minWidth / preferredWidth  : 130 → 195
      minHeight / preferredHeight: 150 → 225

    TowerHolder inner children (×5, mỗi holder):
      InnerBox  : sizeDelta (82, 90)  → (123, 135)  | pos (0, -8)   → (0, -12)
      Tower     : sizeDelta (96, 96)  → (144, 144)  | pos (0,  4)   → (0,   6)
      CostBg    : sizeDelta (72, 36)  → (108,  54)  | pos (-10, 10) → (-15, 15)
      Cost (TMP): fontSize ×1.5

    ── Ghi chú kỹ thuật ──────────────────────────────────────────────────────
    - Với HLG childControlWidth/Height=True: phải set LayoutElement.preferredWidth/
      minWidth (không set RectTransform.sizeDelta trực tiếp — bị override).
    - Scale ratio 1.5 ≈ 1080/750 (height-based) vì CanvasScaler Match=Height (slider=1).

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 10 — Grid-cell Range + Offset Refactor (Session 11) [✅ DONE]    ║
  ╚══════════════════════════════════════════════════════════════════════════╝

  ── Part A: Grid Cell Helpers ────────────────────────────────────────────────
    ✅ IGridMainModel + TDGridMainModel: 3 method mới
       CellToWorld(Vector2Int)  → m_Grid[cell.x, cell.y]
       WorldToCell(Vector3)     → RoundToInt((world - offset) / cellSize), clamped
       IsInBounds(Vector2Int)   → bounds check không clamp

    ✅ ITowerRangeDTO: thêm GetCellsInRange(Vector2Int, Quaternion) → List<Vector2Int>

    ✅ TDRangeDTO.cs (abstract base class):
       GetCellsInRange virtual — default impl dùng bounding box scan + IsInRange
       searchRadius = CeilToInt(DetectionRadius / cellSize)

  ── Part B: Offset-based Range (Arknights style) ─────────────────────────────
    Lý do: geometric DTOs (dot product / radius) không mô tả được range pattern
    phức tạp như Arknights. Thay bằng explicit cell offsets.

    ✅ TDOffsetRangeDTO.cs (NEW):
       Constructor: TDOffsetRangeDTO(Vector2Int[] offsets)
       Offsets định nghĩa khi tower facing +X (Y=90°) — x=forward, y=right-of-facing
       DetectionRadius: max(offset.magnitude) × cellSize (cached)
       IsInRange: WorldToCell(enemy) → check cell trong rotated offsets — O(n), no alloc
       GetCellsInRange: rotate offsets → filter IsInBounds → return list
       RotateOffset: 4 hướng cardinal:
         Y=  0° (facing +Z): (dx,dy) → ( dy,  dx)
         Y= 90° (facing +X): (dx,dy) → ( dx,  dy)  ← definition space
         Y=180° (facing -Z): (dx,dy) → (-dy, -dx)
         Y=270° (facing -X): (dx,dy) → (-dx, -dy)

    ✅ TDRangeDTO.GetCellsInRange → virtual (để TDOffsetRangeDTO override hiệu quả hơn)

    ✅ TDFlyweightTowerDataSettings — TowerData:
       XÓA: RangeType rangeType, int rangeValue
       THÊM: Vector2Int[] rangeOffsets

    ✅ TDTowerBehaviorModel.GetTowerRange():
       return new TDOffsetRangeDTO(data?.rangeOffsets)  ← 1 dòng, không switch

    ✅ Enums.cs: XÓA RangeType enum (Area/HorizontalCone/VerticalCone)

    ✅ DELETED: TDAreaRangeDTO.cs, TDHorizontalRangeDTO.cs, TDVerticalRangeDTO.cs

  ── Part C: SO Range Patterns ────────────────────────────────────────────────
    rangeOffsets set trong Tower Bullet Config.asset (offset = cells, facing +X):

    Cannon    (3 ô thẳng):      (1,0)(2,0)(3,0)
    Catapult  (2×3 wide area):  (1,-1)(1,0)(1,1)(2,-1)(2,0)(2,1)
    MissileG02 (4 ô thẳng):    (1,0)(2,0)(3,0)(4,0)
    MissileG03 (diamond area):  (1,-1)(1,0)(1,1)
                                (2,-2)(2,-1)(2,0)(2,1)(2,2)
                                (3,-1)(3,0)(3,1)
    Mortar    (skip 1, area):   (2,-1)(2,0)(2,1)(3,-1)(3,0)(3,1)(4,0)

    → Thêm tower mới: chỉ cần 1 enum line + 1 SO entry + vẽ offsets trong Inspector

  ── Part D: Custom Inspector Grid Editor ─────────────────────────────────────
    ✅ TowerDataDrawer.cs (Assets/2.Scripts/Editor/) — CustomPropertyDrawer cho TowerData
       Hiển thị lưới 8×9 click-to-toggle trong Inspector (x: -1→6, y: -4→4)
       Tower (0,0) = màu xanh dương, không toggle
       Ô active = màu xanh lá, ô inactive = tối với tọa độ nhỏ làm reference
       Click ô → toggle rangeOffsets array tự động — không cần edit số tay

  ── Pattern usage (Phase 7 area damage) ──────────────────────────────────────
    var towerCell = TDGridMainModel.api.WorldToCell(bulletHitPos);
    var cells = towerRange.GetCellsInRange(towerCell, towerRotation);
    // → TakeDamage tất cả enemy có enemyCell nằm trong cells list

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 7 — Ammo + Combat                                [TODO]            ║
  ╚══════════════════════════════════════════════════════════════════════════╝

  Core combat pipeline đã có sẵn từ Phase 6.1 (pool + spawn + TakeDamage).
  Việc còn lại của Phase 7:
    1. Area damage cho MissileG03 — nổ damage tất cả enemy trong range pattern
       (cần Phase 10 xong trước để dùng grid-cell range, tránh viết lại 2 lần)
    2. Wiring đúng ammo prefab per tower type trong SO Inspector
       KenneyAmmo: AmmoArrow / AmmoBoulder / AmmoBullet / AmmoBeam
    3. Bullet timeout / max distance — bullet bay ra ngoài map không return pool

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 8 — Win/Lose Conditions + HUD                    [TODO]            ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    Life=0 → GameOver screen (onGameOver stub đã có)
    Waves complete + all enemies dead → Victory screen

    HUD additions:
      → "Wave 3/10" indicator
      → Gold counter
      → Next wave countdown timer
      → Game speed toggle 1x/2x
      → CONFIG_PLAYER_STARTING_GOLD=100, CONFIG_ENEMY_REWARD_GOLD=10

  ─────────────────────────────────────────────────────────────────────────────

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 8.5 — Pause System + Enemy Object Pool (Session 10) [✅ DONE]     ║
  ╚══════════════════════════════════════════════════════════════════════════╝

  ── Pause System ─────────────────────────────────────────────────────────────
    TDPauseControl.cs (NEW):
      IsPaused: bool; Toggle(); Pause(); Resume(); onPauseChanged: Action<bool>
      Pause()  → Time.timeScale = 0f → onPauseChanged(true)
      Resume() → Time.timeScale = 1f → onPauseChanged(false)

    PauseAwareDelay (trong TDEnemyPathMainControl):
      Thay Task.Delay(ms, ct) bằng vòng lặp dùng Time.deltaTime accumulation
      Time.deltaTime = 0 khi timeScale=0 → wave loop tự pause mà không cần
      cancel/restart — wave timer và spawn delay đều dừng khi game pause

    TDControl: TDPauseControl.api = new TDPauseControl() (đăng ký khi init)

    TDGameplayMainView:
      [SerializeField] m_PausePanel: GameObject
      Subscribe onPauseChanged → SetActive(isPaused) trên m_PausePanel
      OnPauseButtonClicked() / OnResumeButtonClicked() public methods

    UI (Canvas/SafeArea — DTGamePlay.unity):
      PauseButton: top-right 90×90, dark bg 75% alpha, text "II", anchor=(1,1)
        onClick → TDPauseControl.api.Toggle()
      PausePanel: full-screen dark overlay 65% alpha, SetActive(false) mặc định
        TitleText: "PAUSED" fontSize=60 centered at (50%, 60%)
        ResumeButton: 300×80 green, text "RESUME" centered at (50%, 45%)
          onClick → TDPauseControl.api.Resume()

  ── Enemy Object Pool ────────────────────────────────────────────────────────
    Pattern: ObjectPool<TDEnemyView> — giống TDFlyweightBulletFactoryModel

    TDEnemyPathMainControl:
      InitEnemyPool(prefab, parent, capacity=20, maxSize=50)
        createFunc: Instantiate + set localScale CONFIG_ENEMY_VISUAL_SCALE
        actionOnGet: SetActive(true); actionOnRelease: SetActive(false)
      ReturnEnemy(enemy): m_EnemyPool.Release(enemy)
      SpawnWave: dùng m_EnemyPool.Get() thay Instantiate
        → reset position/rotation trước khi Initialize(key)

    TDEnemyPathMainView:
      Initialize(): gọi TDEnemyPathMainControl.api.InitEnemyPool(m_Slime, transform)
      OnGetAllPaths: StartWaveLoop(m_SpawnPos, ...) — bỏ param prefab

    TDEnemyView:
      m_HasBeenReturned: bool — guard tránh double-return
      Initialize(key): reset tất cả state (health, pathIndex, hasReachedEnd, path)
                        + subscribe event + register registry
      ReturnToPool(): guard → unsubscribe → unregister → ReturnEnemy(this)
      TakeDamage: gọi ReturnToPool() thay Destroy
      Update (reach end): gọi ReturnToPool() thay Destroy
      OnDestroy: chỉ cleanup nếu !m_HasBeenReturned (scene unload safety)

  ── Ghi chú kỹ thuật ─────────────────────────────────────────────────────────
    - Tower đặt lên path cells: giữ nguyên (sẽ dùng cho Operator mechanic như Arknights)
    - PauseAwareDelay không dùng Task.Delay(real-time) mà dùng Task.Yield() loop
      → tương thích với Unity main thread context, không cần lock hay semaphore
    - Enemy pool collectionCheck=true → phát hiện double-release lúc dev
    - StartWaveLoop signature đổi: bỏ TDEnemyView prefab param (pool tự quản lý)

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 9 — Polish & Performance                         [TODO]            ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    Polish: particles, VFX, sounds, camera shake on life lost
    Performance: object pooling (tiles, ammo) — enemies DONE (Phase 8.5) ✓


════════════════════════════════════════════════════════════════════════════════
 15. KNOWN ISSUES & TECH DEBT
════════════════════════════════════════════════════════════════════════════════

  ✅  Tower không attack                        FIXED session 7 (Phase 6.1)
      TDTowerFactoryView.OnCreateTowerSuccess đã subscribe + gọi tower.Init(key)
      Pool bug (global m_TowerType) đã fix — mỗi TowerType có pool riêng

  ✅  async void StartWaveLoop không có CancellationToken  FIXED session 8
      TDEnemyPathMainControl.StartWaveLoop + SpawnWave nhận CancellationToken
      TDEnemyPathMainView: m_WaveCts tạo khi start, Cancel+Dispose trong OnDestroy
      Task.Delay dùng token → dừng sạch khi scene unload, không còn exception

  ✅  TDGridMainModel singleton không reset khi reload scene  FIXED session 8
      Initialize(): ??= → = (luôn tạo instance mới, không giữ lại state cũ)

  ✅  Phase 4.5 Inspector đã assign (session 5)
      TDEnemyPathMainView: m_GateStartPrefab, m_GateEndPrefab
      TDGameplayMainView:  m_LifeText, m_ScreenFlash

  ✅  Gate cells hiển thị path tile bên dưới   FIXED session 6 (Phase 4.6)
      TDEnemyPathView.VisualizeAllPaths: path.GetRange(1, path.Count-2)

  ✅  Gate box edges bị path tile che mất       FIXED session 6 (Phase 4.6)
      Raise toàn bộ box parts +0.13u qua GateRaiseBox.cs Editor script

  ✅  Gate alpha không hoạt động (URP/Unlit)    FIXED session 6 (Phase 4.6)
      DisableKeyword("_EMISSION") + _EmissionColor=black + _Surface=1

  ✅  Obstacle cells gọi SetOccupiedCell  FIXED session 3
  ✅  Mobile Rotate + Cancel button       FIXED session 3 (Arknights-style)
  ✅  Build APK lỗi CS0246 MenuItem       FIXED session 3 (#if UNITY_EDITOR)
  ✅  Ghost không place khi chưa chọn hướng FIXED session 4 (m_IsDirectionSelected)
  ✅  Enemy false-trigger reach-end       FIXED session 5 (Count==0 guard)

  ✅  Gate cells không gọi SetOccupiedCell  FIXED session 8
      TDEnemyPathMainView.SpawnGates(): SetOccupiedCell(startWorld) + SetOccupiedCell(endWorld)
      → Player không còn đặt được tower lên ô GateStart/GateEnd

  ✅  TDVerticalRangeDTO copy-paste từ TDHorizontalRangeDTO  FIXED session 8 (Phase 6.2)
      Mới: Mathf.Abs(Dot(right, toEnemy)) > 0.7f → perpendicular band attack

  ✅  TDFlyweightBulletFactoryModel global m_TowerType state  FIXED session 7 (Phase 6.1)
  ✅  TDEnemyDetector SetTarget(null) mất target (Bug B)      FIXED session 7 (Phase 6.1)
  ✅  DetectionArea child GO trên tower prefabs               FIXED (đã clean trước session 8)

  ✅  Mobile direction selection không hoạt động             FIXED session 8
      Bug: m_DirectionThreshold (1.0f) = cellSize/2 → snap cell cùng lúc threshold → direction reset ngay
      Fix mobile: ghost đóng băng; delta từ frozen cell; threshold = cellSize×0.35f (< snap boundary)
      Fix desktop: direction luôn follow chuột (no threshold, sqrMag>0.01f), ghost không đóng băng
      Platform-split: #if UNITY_ANDROID && !UNITY_EDITOR trong UpdateGhostTransform()

  ✅  CanvasScaler 1125×750 → 1920×1080 + HUD scale fix    FIXED session 9 (Phase 6.3)
      TxtLifePoint, TowerMainView, TowerHolder ×5 và inner children scale ×1.5
      HorizontalLayoutGroup padding/spacing 6→9; LayoutElement preferredW/H updated

  ⚠️  PlacementPanel missing trong scene hierarchy
      TDTowerMainView.InitPlacementPanel(): transform.Find(GAMEPLAY_PLACEMENT_PANEL) = null
      → Mobile Cancel button không hiển thị (null-safe → không crash)
      Fix: Thêm PlacementPanel GameObject vào scene DTGamePlay.unity

  ✅  SO duplicate Tower Bullet Config                        FIXED session 8
      Xóa Assets/2.Scripts/Model/Config/SO/Tower Bullet Config.asset
      DTLoadFirst.unity GUID → trỏ đúng Resources/Configs/Tower Bullet Config.asset


════════════════════════════════════════════════════════════════════════════════
 16. DEPENDENCY GRAPH (session 7)
════════════════════════════════════════════════════════════════════════════════

  ✅ Phase 0   (Grid sync)              ── DONE
  ✅ Phase 1   (Bug fixes A*)           ── DONE
  ✅ Phase 2   (Multi-path)             ── DONE
  ✅ Phase 3   (Wave system)            ── DONE
  ✅ Phase 3.5 (Obstacles)              ── DONE
  ✅ Phase 3.6 (Camera/Env Perspective) ── DONE
  ✅ Phase 3.7 (Camera Ortho + resize)  ── DONE
  ✅ Phase 4   (Tower Arknights place)  ── DONE
     │   Session 8: direction selection bug fixed (ghost freeze + threshold 35% cellSize)
  ✅ Phase 4.5 (Gate Start/End + Life)  ── DONE
     │   Session 8: gate cells SetOccupiedCell fixed
  ✅ Phase 4.6 (Gate Visual Redesign)   ── DONE (session 6)
  ✅ Phase 6.1 (Tower Attack Redesign)  ── DONE (session 7)
     │   Pool per type, Registry+tick detection, PathProgress priority
  ✅ Phase 6.2 (Tower Config SO Refactor) ── DONE (session 8, updated session 11)
     │   damage/attackSpeed → SO; bỏ hardcode switch
     │   TDVerticalRangeDTO fixed; SO duplicate cleaned; wave CTS added
     │   ⚠️  Session 11: rangeType/rangeValue REMOVED → replaced by rangeOffsets (Vector2Int[])
     │
  ⬜ Phase 5   (Multi-start)            ── cần Phase 3+4 DONE ✓
     │
  ⚠️  Phase 6  (Tower UI)               ── PARTIAL
     │   ✅ CanvasScaler 1920×1080 + HUD scale fix: DONE (Phase 6.3) ✓
     │   Còn: PlacementPanel GO missing trong scene
     │   Attack system: DONE (Phase 6.1) ✓
     │   Config SO: DONE (Phase 6.2) ✓
     │
  ✅ Phase 10  (Grid-cell Range Refactor) ── DONE (session 11)
     │   Part 1: CellToWorld / WorldToCell / IsInBounds added to TDGridMainModel
     │   Part 2: TDOffsetRangeDTO (Arknights-style explicit cell offsets, auto-rotate 4 dirs)
     │           TDAreaRangeDTO / TDHorizontalRangeDTO / TDVerticalRangeDTO → DELETED
     │           RangeType enum → DELETED; TDRangeDTO abstract base (virtual GetCellsInRange)
     │   Part 3: SO rangeOffsets configured for all 5 tower types
     │   Part 4: TowerDataDrawer custom Inspector — click-to-toggle 8×9 grid (Editor only)
     │
  ⬜ Phase 7   (Ammo + Combat)          ── cần Phase 10 DONE ✓ — sẵn sàng
     │   Core pipeline đã có (6.1). Còn: area damage, ammo wiring, bullet timeout
     │
  ⬜ Phase 11  (Multi-Enemy Type)       ── có thể song song với Phase 7
     │   Normal / Fast / Tank / Boss + WaveConfig 10 waves
     │   Scope: ~2.5 giờ (xem Section 18 — Phase 11 detail)
     │
  ⬜ Phase 8   (Win/Lose + HUD)         ── cần Phase 4.5 ✓ + Phase 7
     │   (onGameOver stub đã có, chỉ cần GameOver panel UI)
     │
  ✅ Phase 8.5 (Pause + Enemy Pool)     ── DONE (session 10)
     │
  ⬜ Phase 9   (Polish + Perf)          ── final pass

  ── Thứ tự implement đề xuất (cập nhật session 11) ──────────────────────
  ✅ Phase 10 DONE → Phase 7 → Phase 11 → Phase 8 → Phase 9
                               (song song)

  Lý do thứ tự:
    Phase 10 trước Phase 7  : area damage cần grid-cell range → tránh viết lại (DONE)
    Phase 11 song song P7   : multi-enemy độc lập với combat ammo logic
    Phase 8 sau Phase 7     : Win/Lose cần combat hoàn chỉnh để test đúng
    Phase 9 cuối            : polish sau khi core gameplay stable


════════════════════════════════════════════════════════════════════════════════
 17. KEY FILES QUICK REFERENCE
════════════════════════════════════════════════════════════════════════════════

  ── Model / Config ──────────────────────────────────────────────────────────
  Assets/2.Scripts/Model/Config/Constant/TDConstant.cs
  Assets/2.Scripts/Model/Info/Grid/TDGridMainModel.cs
  Assets/2.Scripts/Model/Info/Grid/TDGridDTO.cs
  Assets/2.Scripts/Model/Info/AStarPath/TDaStarPathModel.cs

  ── Tower Range (Session 11) ─────────────────────────────────────────────────
  Assets/2.Scripts/Model/Tower/Interfaces/ITowerRangeDTO.cs     ← interface (DetectionRadius, IsInRange, GetCellsInRange)
  Assets/2.Scripts/Model/Tower/TowerRange/TDRangeDTO.cs         ← [NEW s11] abstract base; virtual GetCellsInRange (bounding-box scan)
  Assets/2.Scripts/Model/Tower/TowerRange/TDOffsetRangeDTO.cs   ← [NEW s11] Arknights-style; O(n) GetCellsInRange; 4-dir auto-rotate
  Assets/2.Scripts/Editor/TowerDataDrawer.cs                    ← [NEW s11] CustomPropertyDrawer click-to-toggle 8×9 grid (Editor only)
  Assets/1.Assets/Resources/Configs/Tower Bullet Config.asset   ← rangeOffsets: Cannon/Catapult/MissileG02/MissileG03/Mortar

  ── Control / Path ──────────────────────────────────────────────────────────
  Assets/2.Scripts/Control/PathControl/TDaStarPathControl.cs
  Assets/2.Scripts/Control/PathControl/TDPathGeneratorControl.cs
  Assets/2.Scripts/Control/PathControl/TDObstacleControl.cs
  Assets/2.Scripts/Control/PathControl/TDEnemyPathMainControl.cs
  Assets/2.Scripts/Control/PathControl/TDEnemyPathControl.cs

  ── Control / Gameplay ──────────────────────────────────────────────────────
  Assets/2.Scripts/Control/Gameplay/TDGameplayMainControl.cs
  Assets/2.Scripts/Control/Gameplay/TDPlayerLifeControl.cs      ← [Phase 4.5 NEW]
  Assets/2.Scripts/Control/Enemy/TDEnemyControl.cs
  Assets/2.Scripts/Control/Enemy/TDEnemyRegistry.cs             ← [Phase 6.1 NEW]
  Assets/2.Scripts/Control/PathControl/TDEnemyPathMainControl.cs ← [Session 8: CancellationToken]
  Assets/2.Scripts/Control/Tower/TDTowerMainControl.cs
  Assets/2.Scripts/Control/Tower/PlaceTower/TDPlaceTowerControl.cs
  Assets/2.Scripts/Control/Tower/TowerFactory/TDTowerBehaviorMainControl.cs
  Assets/2.Scripts/Control/Tower/TowerBehavior/TDTowerBehaviorSubControl.cs
  Assets/2.Scripts/Control/TDControl.cs
  Assets/2.Scripts/Control/InputHandler/TDUserInputControl.cs

  ── View ────────────────────────────────────────────────────────────────────
  Assets/2.Scripts/View/GamePlay/TDGameplayMainView.cs           ← [4.5: life+flash]
  Assets/2.Scripts/View/GamePlay/EnemyPath/TDEnemyPathMainView.cs ← [4.5: SpawnGates]
  Assets/2.Scripts/View/GamePlay/EnemyPath/TDEnemyPathView.cs
  Assets/2.Scripts/View/GamePlay/Enemy/TDEnemyView.cs            ← [4.5: reach-end, 6.1: PathProgress+Registry]
  Assets/2.Scripts/View/GamePlay/Tower/TDTowerMainView.cs
  Assets/2.Scripts/View/GamePlay/Tower/TDTowerWeaponView.cs      ← [6.1: tick scan + priority]
  Assets/2.Scripts/View/GamePlay/Tower/TDTowerHolderView.cs
  Assets/2.Scripts/View/GamePlay/Tower/TDTowerFactoryView.cs     ← [6.1: removed SetupSubControl]
  Assets/2.Scripts/View/GamePlay/Projectile/TDBulletsView.cs     ← [6.1: OwnerType property]
  Assets/2.Scripts/View/GamePlay/Gate/TDGateView.cs              ← [Phase 4.5 NEW]
  Assets/2.Scripts/View/GamePlay/Gate/TDGateEndView.cs           ← [Phase 4.5 NEW]

  ── Scene ───────────────────────────────────────────────────────────────────
  Assets/3.Scenes/DTGamePlay.unity
    Camera: Ortho size=14, pos (23.3,15,2.02), pitch 30°
    GameMapVisualize: scale (4.2,5,2.4), pos (23.3,0,28) → grid 21×12
    PathView/CreatePoint: (2,0,29)

  ── Prefabs ─────────────────────────────────────────────────────────────────
  Assets/1.Assets/Resources/Prefabs/GateStart.prefab    ← [Phase 4.5 NEW, 4.6 REDESIGN]
  Assets/1.Assets/Resources/Prefabs/GateEnd.prefab      ← [Phase 4.5 NEW, 4.6 REDESIGN]
  Assets/1.Assets/Resources/Prefabs/FattyCannonG02.prefab
  Assets/1.Assets/Resources/Prefabs/FattyCatapultG02.prefab
  Assets/1.Assets/Resources/Prefabs/FattyMissileG02.prefab
  Assets/1.Assets/Resources/Prefabs/FattyMissileG03.prefab
  Assets/1.Assets/Resources/Prefabs/FattyMortarG02.prefab
  Assets/1.Assets/Resources/Prefabs/Slime.prefab
  Assets/1.Assets/Resources/Prefabs/Plane.prefab

  ── Materials (Gates) ────────────────────────────────────────────────────────
  Assets/1.Assets/Resources/Materials/Gates/GS_Edge.mat   ← [Phase 4.6 NEW] GateStart edge
  Assets/1.Assets/Resources/Materials/Gates/GS_Face.mat   ← [Phase 4.6 NEW] GateStart face
  Assets/1.Assets/Resources/Materials/Gates/GS_Glow.mat   ← [Phase 4.6 NEW] GateStart glow
  Assets/1.Assets/Resources/Materials/Gates/GS_Arrow.mat  ← [Phase 4.6 NEW] GateStart arrow
  Assets/1.Assets/Resources/Materials/Gates/GE_Edge.mat   ← [Phase 4.6 NEW] GateEnd edge
  Assets/1.Assets/Resources/Materials/Gates/GE_Face.mat   ← [Phase 4.6 NEW] GateEnd face
  Assets/1.Assets/Resources/Materials/Gates/GE_Glow.mat   ← [Phase 4.6 NEW] GateEnd glow

  ── Scripts modified (Session 6) ─────────────────────────────────────────────
  Assets/2.Scripts/View/GamePlay/EnemyPath/TDEnemyPathView.cs
    ← VisualizeAllPaths: skip gate cells (GetRange 1..count-2)

  ── Scene modified (Session 9 — Phase 6.3 HUD Scale Fix) ────────────────────
  Assets/3.Scenes/DTGamePlay.unity
    Canvas / CanvasScaler: Reference Resolution 1125×750 → 1920×1080
    Canvas/SafeArea/TxtLifePoint:
      RectTransform sizeDelta (200,50)→(300,75); anchoredPos (0,-36)→(0,-54)
      TextMeshProUGUI fontSize 36 → 54
    Canvas/SafeArea/TowerMainView:
      RectTransform sizeDelta (686,162)→(1029,243); anchoredPos (20,20)→(30,30)
      HorizontalLayoutGroup padding 6→9, spacing 6→9
    Canvas/SafeArea/TowerMainView/TowerHolder ×5:
      LayoutElement minWidth/preferredWidth 130→195; minHeight/preferredHeight 150→225
      InnerBox  sizeDelta (82,90)→(123,135); anchoredPos (0,-8)→(0,-12)
      Tower     sizeDelta (96,96)→(144,144); anchoredPos (0,4)→(0,6)
      CostBg    sizeDelta (72,36)→(108,54);  anchoredPos (-10,10)→(-15,15)
      Cost TMP  fontSize ×1.5

  ── Scripts modified (Session 8 — Phase 6.2 + Bug Fixes) ────────────────────
  Assets/2.Scripts/Model/Config/Enum/Enums.cs
    ← added RangeType enum: Area | HorizontalCone | VerticalCone
      ⚠️ REMOVED in session 11 (replaced by offset-based approach)
  Assets/2.Scripts/Model/Config/TDFlyweightTowerDataSettings.cs
    ← TowerData + damage/attackSpeed/rangeType/rangeValue fields
    ← GetData(type) pure lookup; GetPrefab/GetCost delegate to GetData
      ⚠️ Session 11: rangeType/rangeValue REMOVED; rangeOffsets (Vector2Int[]) ADDED
  Assets/2.Scripts/Model/Info/Tower Type/TDTowerBehaviorModel.cs
    ← removed 3 hardcode switch; reads damage/speed/range from SO via Setting
  Assets/2.Scripts/Model/Tower/TowerRange/TDVerticalRangeDTO.cs
    ← fixed: perpendicular band Mathf.Abs(Dot(right,...)) > 0.7f (was copy of Horizontal)
  Assets/2.Scripts/Model/Info/Grid/TDGridMainModel.cs
    ← Initialize(): ??= → = (reset singleton on scene reload)
  Assets/2.Scripts/View/GamePlay/Tower/TDTowerMainView.cs
    ← removed [SerializeField] m_DirectionThreshold; added k_DirectionThresholdRatio=0.35f
    ← UpdateGhostTransform(): platform-split via #if UNITY_ANDROID && !UNITY_EDITOR
       Mobile: ghost freezes at 35% cellSize; delta from frozen cell; direction locks until place/cancel
       Desktop: ghost always follows mouse; direction always updates (no threshold); no freeze
  Assets/2.Scripts/View/GamePlay/EnemyPath/TDEnemyPathMainView.cs
    ← SpawnGates(): SetOccupiedCell for both gate cells
    ← added m_WaveCts (CancellationTokenSource); Cancel+Dispose in OnDestroy
    ← StartWaveLoop call passes m_WaveCts.Token
  Assets/2.Scripts/Control/PathControl/TDEnemyPathMainControl.cs
    ← StartWaveLoop + SpawnWave accept CancellationToken
    ← Task.Delay uses token; try/catch OperationCanceledException
  Assets/3.Scenes/DTLoadFirst.unity
    ← RepResourceObject.objects GUID updated → Resources/Configs/Tower Bullet Config.asset
  [DELETED] Assets/2.Scripts/Model/Config/SO/Tower Bullet Config.asset
    ← SO duplicate removed (was stale, data=0 for new fields)
  Assets/1.Assets/Resources/Configs/Tower Bullet Config.asset
    ← filled: damage/attackSpeed/rangeType/rangeValue cho 5 tower types
      ⚠️ Session 11: rangeType/rangeValue replaced by rangeOffsets (Vector2Int[])

  ── Scripts modified (Session 7 — Phase 6.1) ─────────────────────────────────
  Assets/2.Scripts/Model/Config/TDFlyweightTowerDataSettings.cs
    ← removed m_Prefab/SetPrefab/pool callbacks; added GetPrefab(type) pure lookup
    ← TowerData.prefab renamed to bulletPrefab [FormerlySerializedAs]
  Assets/2.Scripts/Model/Info/Flyweight/TDFlyweightBulletFactoryModel.cs
    ← stateless pool: removed m_TowerType; Spawn(TowerType), GetPoolFor(TowerType) lazy
  Assets/2.Scripts/View/GamePlay/Projectile/TDBulletsView.cs
    ← added OwnerType property (set by Spawn, used by ReturnToPool)
  Assets/2.Scripts/Control/Tower/TowerBehavior/TDTowerBehaviorSubControl.cs
    ← removed SetupSubControl(); Attack() dùng Spawn(type) trực tiếp
  Assets/2.Scripts/View/GamePlay/Tower/TDTowerFactoryView.cs
    ← removed SetupSubControl() call
  Assets/2.Scripts/Model/Info/Tower Type/TDTowerBehaviorModel.cs
    ← removed dead settings property
  Assets/2.Scripts/Model/Tower/Interfaces/ITowerRangeDTO.cs
    ← added DetectionRadius property
  Assets/2.Scripts/Model/Tower/TowerRange/TDHorizontalRangeDTO.cs
  Assets/2.Scripts/Model/Tower/TowerRange/TDAreaRangeDTO.cs
  Assets/2.Scripts/Model/Tower/TowerRange/TDVerticalRangeDTO.cs
    ← implement DetectionRadius => m_Range
  Assets/2.Scripts/View/GamePlay/Enemy/TDEnemyView.cs
    ← added PathProgress property; Register/Unregister TDEnemyRegistry
  Assets/2.Scripts/View/GamePlay/Tower/TDTowerWeaponView.cs
    ← replaced trigger/SetTarget with tick scan (0.2s) + PathProgress priority
    ← removed SetTarget() public method
  Assets/2.Scripts/Control/TDControl.cs
    ← added TDEnemyRegistry.api = new TDEnemyRegistry()
  [DELETED] Assets/2.Scripts/View/GamePlay/Tower/TDEnemyDetector.cs

  ── Key Asset Paths ──────────────────────────────────────────────────────────
  HQ prop (GateEnd) : Assets/1.Assets/Resources/Prefabs/KenneyTower/ (TowerCrystals)

  ── Scripts modified (Session 11 — Phase 10 Grid-cell Range Refactor) ────────
  Assets/2.Scripts/Model/Info/Grid/Interfaces/IGridMainModel.cs
    ← added 3 methods: CellToWorld(Vector2Int), WorldToCell(Vector3), IsInBounds(Vector2Int)
  Assets/2.Scripts/Model/Info/Grid/TDGridMainModel.cs
    ← implemented CellToWorld (m_Grid[x,y]), WorldToCell (RoundToInt+Clamp), IsInBounds
  Assets/2.Scripts/Model/Tower/Interfaces/ITowerRangeDTO.cs
    ← added: List<Vector2Int> GetCellsInRange(Vector2Int towerCell, Quaternion towerRotation)
  Assets/2.Scripts/Model/Tower/TowerRange/TDRangeDTO.cs           [NEW]
    ← abstract base class implementing ITowerRangeDTO
    ← virtual GetCellsInRange: bounding-box scan via IsInRange (O(r²))
  Assets/2.Scripts/Model/Tower/TowerRange/TDOffsetRangeDTO.cs     [NEW]
    ← Arknights-style explicit cell offsets; overrides GetCellsInRange O(n)
    ← RotateOffset: Y=0→(dy,dx), Y=90→(dx,dy), Y=180→(-dy,-dx), Y=270→(-dx,-dy)
    ← DetectionRadius = max(offset.magnitude) * cellSize
  Assets/2.Scripts/Model/Config/TDFlyweightTowerDataSettings.cs
    ← TowerData: REMOVED rangeType (RangeType) + rangeValue (int)
    ← TowerData: ADDED rangeOffsets (Vector2Int[])
  Assets/2.Scripts/Model/Info/Tower Type/TDTowerBehaviorModel.cs
    ← GetTowerRange: simplified to new TDOffsetRangeDTO(data?.rangeOffsets)
  Assets/2.Scripts/Model/Config/Enum/Enums.cs
    ← REMOVED RangeType enum (Area | HorizontalCone | VerticalCone)
      (was added session 8; superseded by offset approach)
  Assets/2.Scripts/Editor/TowerDataDrawer.cs                      [NEW]
    ← CustomPropertyDrawer for TowerData; grid x[-1..6] y[-4..4], CELL=20f
    ← click-to-toggle cells; tower at (0,0)=blue/non-toggle; green=active; dark=inactive
    ← 7px coordinate labels on inactive cells for reference
  Assets/1.Assets/Resources/Configs/Tower Bullet Config.asset
    ← rangeOffsets populated for all 5 towers:
       Cannon:    (1,0)(2,0)(3,0)
       Catapult:  (1,-1)(1,0)(1,1)(2,-1)(2,0)(2,1)
       MissileG02:(1,0)(2,0)(3,0)(4,0)
       MissileG03:(1,-1)(1,0)(1,1)(2,-2)(2,-1)(2,0)(2,1)(2,2)(3,-1)(3,0)(3,1)
       Mortar:    (2,-1)(2,0)(2,1)(3,-1)(3,0)(3,1)(4,0)
  [DELETED] Assets/2.Scripts/Model/Tower/TowerRange/TDAreaRangeDTO.cs
  [DELETED] Assets/2.Scripts/Model/Tower/TowerRange/TDHorizontalRangeDTO.cs
  [DELETED] Assets/2.Scripts/Model/Tower/TowerRange/TDVerticalRangeDTO.cs


================================================================================
  END OF DOCUMENT
================================================================================
