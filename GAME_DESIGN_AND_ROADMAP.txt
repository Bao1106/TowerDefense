================================================================================
  TOWER DEFENSE — GAME DESIGN, TECHNICAL REFERENCE & DEVELOPMENT ROADMAP
  Last updated: 2026-05-30  (session 27 — Toon Shader + Outline + HP Bar ZTest Always Fix)
================================================================================


════════════════════════════════════════════════════════════════════════════════
 1. SCENE OVERVIEW — DTGamePlay.unity
════════════════════════════════════════════════════════════════════════════════

File: Assets/3.Scenes/DTGamePlay.unity

── Camera ──────────────────────────────────────────────────────────────────────
  GameObject         : Main Camera
  Projection         : Orthographic  ← switched from Perspective (session 4)
  orthographicSize   : 14
  Position           : (24.5, 15, 3.0)    ← updated session 20 (was 23.3,15,2.02)
  Rotation Euler     : X=30°, Y=0°, Z=0°   ← pitch 30°
  Aspect target      : 16:9 (1920×1080)

  Visible area on ground plane (Y=0):
    Width  X : 2 × 14 × (16/9) ≈ 49.8 world units  → grid 44u fits with ~3u buffer each side
    Depth  Z : camera sees Z ≈ 5..53 at ground
    Camera center on ground: Z = 3.0 + 15/tan(30°) ≈ 29

── Ground / Playable area ──────────────────────────────────────────────────────
  GameMapVisualize
    Type       : Unity Plane mesh (native 10×10)
    Scale      : (4.4, 5, 2.6)  → 44u × 26u world   ← updated session 20 (was 4.2×2.4)
    Position   : (23.3, 0, 27.7)                     ← updated session 20 (was 23.3,0,28)
    Bounds X   : [1.3, 45.3]    ← aligned with CityGround tile boundaries [s20]
    Bounds Z   : [14.7, 40.7]   ← aligned with CityGround tile boundaries [s20]
    MeshRenderer : enabled (material: ground2)
    TDGridVisualizerView : disabled (gizmos hidden) [s20]

── Environment objects (ArkEnvironment children) ───────────────────────────────
  CityGround   : 322+ mesh tiles in gameplay zone, 2u×2u each, boundaries at
                 X = 1.3 + n×2  (→ 1.3, 3.3, 5.3, ..., 45.3)
                 Z = 14.7 + n×2 (→ 14.7, 16.7, ..., 40.7)
                 → GameMapVisualize edges now align EXACTLY with tile boundaries [s20]
  Sky          : skybox — NOT touched
  Buildings    : moved closer via EnvironmentContractor (factor 0.5 from grid center)
  Cliffs       : moved closer via EnvironmentContractor
  Mountains    : MtnL/MtnR pushed to Z=130, MtnC to Z=140 (far backdrop)
  Pillars      : top-edge items (Z 35-65) pushed to Z=50-60 range
  Vegetation   : tall trees (mesh > 1u) pushed to Z=50-60 (backdrop peek)
  Pipes/Beams  : contracted by EnvironmentContractor

── Spawn / PathView ────────────────────────────────────────────────────────────
  PathView/CreatePoint  : pos (1, 0, 27.7)    ← updated session 20 (was 2,0,29)
    → Just outside plane left edge (plane X starts at 1.3)
    → Z=27.7 aligned with START row world Z = cell(0,6) center

── Gates (Phase 4.5 + 4.6 — DONE) ─────────────────────────────────────────────
  GateStart  : spawned at world (2.3, 0, 27.7)  = grid (0,6)   ← updated s20 (was 3.3,0,29)
  GateEnd    : spawned at world (44.3, 0, 27.7) = grid (21,6)  ← updated s20 (was 43.3,0,29)
  → Instantiated runtime bởi TDEnemyPathMainView.SpawnGates()
  → Visual: 3D wireframe box (Arknights-style), redesigned session 6 (Phase 4.6)


════════════════════════════════════════════════════════════════════════════════
 2. GRID SYSTEM
════════════════════════════════════════════════════════════════════════════════

  CONFIG_GRID_CELL_SIZE = 2f                  (TDConstant.cs)
  Plane bounds         = (44, 26) world units (scale 4.4×2.6 × Unity plane 10)  [updated s20]
  Grid width  = Floor(44 / 2f) = 22           (TDGridMainModel.cs runtime)       [updated s20]
  Grid height = Floor(26 / 2f) = 13           (TDGridMainModel.cs runtime)       [updated s20]
  Total cells = 286                                                               [updated s20]

  Offset calculation (TDGridMainModel.cs):
    offsetX = planePos.x - (mapSize.x/2) + (cellSize/2) = 23.3 - 22 + 1 = 2.3  [updated s20]
    offsetZ = planePos.z - (mapSize.z/2) + (cellSize/2) = 27.7 - 13 + 1 = 15.7 [updated s20]

  Cell (x,y) → world = (x*2 + 2.3,  0,  y*2 + 15.7)
    Cell (0,  0)  → world (2.3,  0, 15.7)   ← bottom-left
    Cell (21, 12) → world (44.3, 0, 39.7)   ← top-right
    Cell (0,  6)  → world (2.3,  0, 27.7)   ← START / GateStart  [updated s20]
    Cell (21, 6)  → world (44.3, 0, 27.7)   ← END   / GateEnd    [updated s20]

  CityGround tile alignment (verified s20):
    GameMapVisualize edges land EXACTLY on CityGround 2u-tile boundaries:
    X: left=1.3, right=45.3  ←→  CityGround X boundaries 1.3+n×2
    Z: bot=14.7, top=40.7    ←→  CityGround Z boundaries 14.7+n×2
    → Each game cell (2u×2u) corresponds to exactly 1 CityGround floor tile


════════════════════════════════════════════════════════════════════════════════
 3. CODE ARCHITECTURE
════════════════════════════════════════════════════════════════════════════════

  Pattern : MVC-like (Model / Control / View)
  DI      : Static singleton api (gameplay); Event Bus cho cross-cutting concerns
  Events  : C# Action delegates + TDGameEventBus (s23)

  Code style (enforced s23):
    - Allman braces: { luôn trên dòng riêng
    - 1 space quanh operator (+=, -=, =) — không alignment padding

  ── Interfaces (Tower) ───────────────────────────────────────────────────────
  IDeployableDTO.cs   ← [NEW s23] Stats chung cho mọi unit đặt lên map (Tower + Operator)
                         TowerType, Damage, AttackSpeed, AttackType, MaxTargets, RangeOffsets
  IPlacedUnit.cs      ← [NEW s23] Interface cho unit đang đứng trên map
                         UnitType, Init(key, slotInfo), OnRemove()
  ITowerRangeDTO.cs   ← DetectionRadius, IsInRange, GetCellsInRange
  ITowerFactoryControl.cs ← CreateUnit(prefab, pos, rot, slotInfo) [s23: renamed từ CreateTower]

  ── Model ───────────────────────────────────────────────────────────────────
  TDConstant.cs                    ← all config values (grid, path zones, wave timing, prefabs, lives)
  TDGridMainModel.cs               ← world-space grid positions, snap, occupancy
                                      + CellToWorld / WorldToCell / IsInBounds [Phase 10]
  TDGridDTO.cs                     ← logical width×height IGridCellDTO[,] grid
  TDGridCellDTO.cs                 ← cell data: position(x,y), isWalkable, CellType
  TDaStarPathModel.cs              ← heuristic (Manhattan: dx+dy), GetNeighbors
  TDFlyweightBulletFactoryModel.cs ← lazy pool per TowerType; pool TDAttackVFX [Phase 7]
  TDFlyweightTowerDataSettings.cs  ← ScriptableObject; TowerData : IDeployableDTO
                                      towerPrefab, ammoPrefab, cost, damage, attackSpeed,
                                      attackType, maxTargets, rangeOffsets, icon [s23: IDeployableDTO]
  TDTowerBehaviorModel.cs          ← Dictionary<TowerType, IDeployableDTO>; không còn if/else [s23]
  TDRangeDTO.cs                    ← abstract base; virtual GetCellsInRange (bounding-box) [Phase 10]
  TDOffsetRangeDTO.cs              ← Arknights-style explicit cell offsets, O(n), 4-dir auto-rotate [Phase 10]
  TDFlyweightOperatorDataSettings.cs ← ScriptableObject; OperatorData : IDeployableDTO
                                       operatorType, cost, hp, damage, attackSpeed, blockCount,
                                       rangeOffsets, icon [s23: IDeployableDTO + OperatorType]
  TDTowerSlotInfo.cs (struct)      ← {prefab, towerType, operatorType, cost, icon} [s23: +operatorType]

  Platform: Android Mobile (Legacy Input System — activeInputHandler=0)
             → không dùng com.unity.inputsystem package
             → dùng Input.GetMouseButtonDown + EventSystem.IsPointerOverGameObject(fingerId)

  ── Control ─────────────────────────────────────────────────────────────────
  TDGameplayMainControl.cs   ← thin orchestrator
  TDPauseControl.cs          ← Toggle/Pause/Resume + Time.timeScale; onPauseChanged [Phase 8.5 NEW]
  TDPlayerLifeControl.cs     ← LoseLife(), onLifeChanged, onGameOver [Phase 4.5]
  TDGoldControl.cs           ← Gold, Tick() passive income, AddGold, SpendGold; onGoldChanged [Phase 8]
  TDGameStateControl.cs      ← TotalEnemies/KilledEnemies/AllWavesSpawned/IsGameEnded;
                                OnEnemyRemoved (killed+escaped), OnAllWavesSpawned, OnGameOver;
                                onEnemyCountChanged, onVictory [Phase 8]
  TDSpeedControl.cs          ← 1×/2× toggle, pause-aware timeScale; onSpeedChanged [Phase 8]
  TDEnemyPathMainControl.cs  ← InitEnemyPath, GenerateAllPaths, ComputeValidTowerCells,
                                InitEnemyPool (ObjectPool), ReturnEnemy, PauseAwareDelay,
                                StartWaveLoop, SpawnWave, onValidTowerCellsReady,
                                onWaveStart events [Phase 4.5 + 8.5]
  TDaStarPathControl.cs      ← ComputePath (A*), BuildDefinedPath (straight segments)
  TDPathGeneratorControl.cs  ← random waypoint generator per Y-zone
  TDObstacleControl.cs       ← place obstacles on non-path cells with spacing;
                                multi-cell footprint (radius auto-computed from prefab bounds);
                                onObstaclesPlaced(centers, prefabIndices, allBlocked) [s20]
  TDEnemyPathControl.cs      ← CreatePath() → instantiate Plane tiles on grid
  TDEnemyControl.cs          ← convert IGridCellDTO path → Vector3 world positions
  TDEnemyRegistry.cs         ← centralized enemy list; Register/Unregister/GetAll [Phase 6.1 NEW]
  TDTowerMainControl.cs      ← select tower, rotate (E/Q), place
                                [s23: m_CurrentSlot stored on select; onGetTowerPrefab: Action<TDTowerSlotInfo>]
  TDPlaceTowerControl.cs     ← raycast placement, occupancy check
                                [s23: nhận TDTowerSlotInfo; xóa RegisterOperator (moved vào OperatorView)]
  TDTowerFactoryControl.cs   ← [s23] CreateUnit(prefab, pos, rot, slotInfo); GetComponent<IPlacedUnit>;
                                onCreateUnitSuccess: Action<IPlacedUnit>; backward-compat onCreateTowerSuccess
  TDTowerBehaviorMainControl.cs  ← attack cooldown; AttackTargets(List<TDEnemyView>) [Phase 7]
  TDTowerBehaviorSubControl.cs   ← spawn N TDAttackVFX per target list [Phase 7]
  TDUserInputControl.cs      ← mobile/desktop input event dispatching
  TDOperatorRegistry.cs      ← [Phase 14 s22] Track valid path cells, operator positions,
                                blocked enemy refs, ForceUnblock khi operator chết

  ── View ────────────────────────────────────────────────────────────────────
  TDGameplayMainView.cs      ← scene entry, init grid + life system
                               [Phase 4.5: Initialize life]
                               [Phase 8: STRIPPED — life text + flash moved to TDGameplayHUDView]
  TDGameplayHUDView.cs       ← HUD display (trừ TowerMainView); gắn Canvas/SafeArea/Container
                               [Phase 8 NEW]: cache tất cả via transform.Find() + PATH_xxx constants
                               subscribe: onLifeChanged, onGoldChanged, onEnemyCountChanged,
                               onVictory, onGameOver, onPauseChanged, onSpeedChanged
                               Update: TDGoldControl.api.Tick(deltaTime)
  TDEnemyPathMainView.cs     ← path events + obstacle wiring + SpawnGates()
                               [Phase 4.5: m_GateStartPrefab/EndPrefab, OnWaveStart]
  TDEnemyPathView.cs         ← Plane tile pool
  TDEnemyView.cs             ← enemy MonoBehaviour: move along path, reach-end → LoseLife
                               [Phase 4.5: m_HasReachedEnd guard]
                               [Phase 6.1: PathProgress property, Register/Unregister registry]
                               [Phase 13: Animator via GetComponent Awake; const triggers;
                                Die coroutine; m_IsDying guard; Rebind on pool reuse]
                               [Phase 14 s22: check TDOperatorRegistry.CanBlock on arrive;
                                m_IsBlocked; attack operator timer; ForceUnblock on Die]
  TDTowerMainView.cs         ← 5 tower holders, input handler, green highlight tiles,
                               range highlight tile pool (Phase 12)
  TDTowerWeaponView.cs       ← m_Targets List, ScanForTargets sort PathProgress, validate per frame
                               AttackTargets với rangeDTO + towerRotation [Phase 7]
                               [s23: implements IPlacedUnit; OnRemove() unsubscribe]
  TDAttackVFX.cs             ← [NEW Phase 7] thay TDBulletsView; homing → OnImpact → Single/AOE damage
  [DELETED] TDBulletsView.cs ← removed Phase 7 (replaced by TDAttackVFX)
  TDTowerHolderView.cs       ← HUD slot (button + cost); Cost property; SetInteractable() [Phase 8]
  TDSlotHolderMainView.cs    ← [Phase 15 s22] Dynamic tower slot bar — instantiate SlotHolder prefabs
                                từ Resources, wire buttons, ghost placement, Arknights 2-phase drag,
                                highlights (green=tower zone, blue=operator path)
                                [s23: OnCreateTower(TDTowerSlotInfo); 1-space style fix]
  TDSlotHolderItemView.cs    ← [Phase 15 s22] Một slot button: SlotIcon (Image), cost text,
                                select button, disabled overlay
  TDOperatorView.cs          ← [Phase 14 s22] Operator: attack blocked enemies, TakeDamage() khi
                                bị enemy phản công, Die() → ForceUnblock all enemies + Destroy
                               [s23: implements IPlacedUnit; Init(key,slotInfo) tự RegisterOperator;
                                dùng OperatorType thay m_OperatorDataIndex; raise TDGameEventBus.OperatorDied]
  TDTowerFactoryView.cs      ← [s23: empty stub — Init logic moved vào TDTowerFactoryControl.CreateUnit]
  TDGateView.cs              ← GateStart MonoBehaviour: PlaySpawnEffect() per wave [Phase 4.5]
  TDGateEndView.cs           ← GateEnd MonoBehaviour: trigger visual + life text [Phase 4.5]
  [DELETED] TDEnemyDetector.cs ← removed Phase 6.1 (replaced by Registry+tick scan)

  ── Event Bus ────────────────────────────────────────────────────────────────
  TDGameEventBus.cs          ← [NEW s23] Static event hub cho cross-cutting concerns (VFX, Audio)
                                Events: OnEnemyDied(pos,type), OnEnemySpawned, OnTowerAttacked,
                                OnOperatorDied(cell), OnLifeLost, OnVictory, OnGameOver, OnWaveStarted
                                Gameplay raise event; audio/VFX subscribe độc lập — zero coupling


════════════════════════════════════════════════════════════════════════════════
 4. GAME INIT FLOW (Current Implementation)
════════════════════════════════════════════════════════════════════════════════

  ┌──── GAME START (1 lần) ──────────────────────────────────────────────┐
  │                                                                       │
  │  TDControl.InitOtherControl()                                         │
  │    ├─ TDPlayerLifeControl.api = new TDPlayerLifeControl()  [4.5]     │
  │    └─ ... (tất cả control khác)                                       │
  │                                                                       │
  │  TDGameplayMainView.Start()                                           │
  │    ├─ CheckSceneLoaded()                                              │
  │    ├─ TDPlayerLifeControl.api.Initialize()  → lives=20, fire event   │
  │    ├─ Subscribe onLifeChanged → UpdateLifeText + FlashScreen          │
  │    ├─ Subscribe onGameOver   → Log (TODO Phase 8: GameOver panel)     │
  │    └─ InitGameplay()                                                  │
  │         ├─ TDGridMainModel.Initialize(mapSize, planePos)              │
  │         ├─ CreateGrid()  → grid 21×12                                 │
  │         └─ TDGameplayMainControl.InitEnemyPath(view, gridDTO)        │
  │                                                                       │
  │  TDEnemyPathMainView.Initialize()                                     │
  │    └─ TDEnemyPathMainControl.api.InitEnemyPool(m_Slime, transform)   │
  │         ObjectPool cap=20 maxSize=50; createFunc: Instantiate+scale   │
  │                                                                       │
  │  TDEnemyPathMainView.ImplementPath()  [async]                         │
  │    ├─ await createGridCompletion                                      │
  │    ├─ RegistryEvents()  (incl. onWaveStart → PlaySpawnEffect)         │
  │    ├─ InitEnemyPath(start=0,6  end=20,6)                             │
  │    ├─ SpawnGates()  [Phase 4.5]                                       │
  │    │    ├─ Instantiate GateStart at grid[0,6] = (3.3, 0, 29)         │
  │    │    └─ Instantiate GateEnd   at grid[20,6] = (43.3, 0, 29)       │
  │    ├─ GenerateAllPaths(start, end) → 3 corridors                      │
  │    └─ PlaceObstacles → ComputeValidTowerCells                         │
  │                                                                       │
  └───────────────────────────────────────────────────────────────────────┘

  ┌──── PER WAVE (LevelConfig-driven — Phase 11) ────────────────────────┐
  │                                                                       │
  │  StartWaveLoop(spawnPos, allPaths, LevelConfig, ct):                  │
  │    waveBatches = BuildWavePlans(difficulty, waveCount, totalEnemies)  │
  │    ratio = DifficultyRatioTable.Get(difficulty)  ← HP×/Speed×        │
  │    for waveIdx in 0..waveBatches.Count:                               │
  │      corridor = allPaths[Random.Range(0, allPaths.Count)]             │
  │      onWaveStart?.Invoke(waveIdx) → GateView.PlaySpawnEffect() [4.5] │
  │      await SpawnBatch(spawnPos, corridor, waveBatches[waveIdx], ...)  │
  │      await PauseAwareDelay(config.waveInterval, ct)                   │
  │                                                                       │
  │  BuildWavePlans(difficulty, waveCount, totalEnemies):                 │
  │    Clamp Extreme/Nightmare: waveCount≥15, totalEnemies≥75             │
  │    GetBossParams(difficulty) → (bossWaves, bossPerWave, mult)         │
  │      Easy/Normal: 1 bossWave, 1 boss, 2.0×                           │
  │      Hard:        2 bossWaves, 1 boss, 2.5×                           │
  │      Extreme:     3 bossWaves, 1 boss, 2.5×                           │
  │      Nightmare:   5 bossWaves, 2 boss, 2.5×                           │
  │    Solve r: regularWaves×r + bossWaves×(r×mult) = totalEnemies       │
  │    regularSize  = round(r);  bossWaveSize = round(r×mult)             │
  │    Boss wave positions: boss k → wave ceil(W×k/B)-1 (0-indexed)       │
  │    Regular wave: Normal/Fast/Tank mix (ratio-proportional), shuffled   │
  │    Boss wave:    mix (bossWaveSize - bossPerWave) + boss spawn LAST   │
  │                                                                       │
  │  SpawnBatch:                                                          │
  │    for each EnemyType in batch:                                       │
  │      pool = m_EnemyPools[type].Get()  ← 4 pools, 1 per EnemyType    │
  │      hp    = baseHP    × ratio.hpMult                                 │
  │      speed = baseSpeed × ratio.speedMult                              │
  │      key = "w{waveIdx}-{type}-e{i}-{instanceId}"                     │
  │      enemy.Initialize(key, hp, speed, data.dieDuration, enemyType)   │
  │      enemy.SetPath(corridor)                                          │
  │      await PauseAwareDelay(config.spawnInterval, ct)                  │
  │                                                                       │
  └───────────────────────────────────────────────────────────────────────┘

  ── Key design decisions (locked) ─────────────────────────────────────
    - Maze cố định: 3 corridors gen 1 lần lúc start
    - Enemy là WAYPOINT FOLLOWER (không A* runtime)
    - Khi enemy hết path → LoseLife() + ReturnToPool() (không Destroy) [Phase 8.5]
    - Khi enemy chết (TakeDamage ≤ 0) → ReturnToPool() (không Destroy) [Phase 8.5]
    - PauseAwareDelay: Task.Yield() loop cộng Time.deltaTime → tự pause khi timeScale=0 [Phase 8.5]
    - GateEnd trigger chỉ play visual effect (safety net, không LoseLife lại)


════════════════════════════════════════════════════════════════════════════════
 5. PATH GENERATION ALGORITHM (TDPathGeneratorControl)
════════════════════════════════════════════════════════════════════════════════

  Input  : start (Vector2Int), end (Vector2Int), yMin, yMax (zone bounds)
  Output : Vector2Int[] waypoints, mỗi cặp liên tiếp cùng X HOẶC cùng Y

  Algorithm:
    1. Enter zone: enterY = Random(yMin, yMax)
       if enterY != start.y → add (start.x, enterY)

    2. Zigzag loop:
       while (curX + minStep <= end.x - 1):
         stepX = Random(minStep, maxStep); nextX = min(curX+stepX, end.x-1)
         add (nextX, curY); curX = nextX
         nextY = PickDifferentY(curY, yMin, yMax, minDelta)
         add (curX, nextY); curY = nextY

    3. Exit: if curX != end.x → add (end.x, curY)

  Tunables:
    CONFIG_PATH_MIN_STEP_X         = 2
    CONFIG_PATH_MAX_STEP_X         = 5
    CONFIG_PATH_MIN_VERTICAL_DELTA = 2


════════════════════════════════════════════════════════════════════════════════
 6. BUILD DEFINED PATH ALGORITHM (TDaStarPathControl.BuildDefinedPath)
════════════════════════════════════════════════════════════════════════════════

  Input  : gridDTO, start, end, waypoints[]
  Output : List<IGridCellDTO>

  BuildDefinedPath:
    path=[]; cur=start
    foreach wp in waypoints: AppendStraightSegment(path, cur, wp); cur=wp
    AppendStraightSegment(path, cur, end)
    path.Add(cell(end))

  AppendStraightSegment: move X first, then Y (Manhattan walk, no A*)


════════════════════════════════════════════════════════════════════════════════
 7. TOWER SELECTION, HIGHLIGHT & PLACEMENT FLOW (Phase 4 — DONE)
════════════════════════════════════════════════════════════════════════════════

  ── 2-Phase Arknights Placement ────────────────────────────────────────────
  Phase 1: Drag ngón tay đến cell → ghost snap, m_IsDirectionSelected=false
  Phase 2: Swipe từ cell center vượt k_DirectionThresholdRatio×cellSize (0.35×2=0.7u) → direction chọn
           Ghost đóng băng tại ô hiện tại; direction tính từ frozen cell center
  Đổi sang cell mới → reset về Phase 1 (chỉ áp dụng trên mobile)

  MOBILE: TouchPhase.Ended + m_IsDirectionSelected=true → place
  DESKTOP: LMB place (không cần direction), E/Q rotate, RMB cancel

  Ghost scale: (1.0, 1.0, 1.0) — cellSize=2u

  ComputeRotationIndex(delta):
    |dx|≥|dz|: dx>0→90°(right), dx<0→270°(left)
    |dz|>|dx|: dz>0→0°(fwd), dz<0→180°(back)

  Bugs fixed A–I (xem Known Issues)


════════════════════════════════════════════════════════════════════════════════
 8. GATE START / GATE END (Phase 4.5 + 4.6 — DONE)
════════════════════════════════════════════════════════════════════════════════

  ── Visual Design (Phase 4.6 — Session 6) ────────────────────────────────────

  Arknights-style 3D wireframe box visible from 30° orthographic camera.
  Box dimensions: 2×2u footprint, 2.2u tall, raised +0.13u above ground
  (path tiles sit at y=0.1 → box bottom ring at y=0.13+0.035=0.165 → clears tiles)

  Box structure (12 edges + 5 face fills):
    4 corner posts  : Post_FL / Post_FR / Post_BL / Post_BR
                      size 0.07×2.20×0.07u, placed at (±hs, hh, ±hs)
    4 bottom ring   : Bot_Front / Bot_Back / Bot_Left / Bot_Right
                      horizontal bars at y = 0.035 + BOX_Y offset
    4 top ring      : Top_Front / Top_Back / Top_Left / Top_Right
                      horizontal bars at y = (2.20 - 0.035) + BOX_Y offset
    5 face fills    : Face_Front/Back/Left/Right (side quads) + Face_Top (cap)
                      transparent quads at 7% alpha sitting just outside edges

  Materials (Assets/1.Assets/Resources/Materials/Gates/):
    GS_Edge.mat  — URP/Unlit, Opaque,       red   (1.00, 0.10, 0.10, 0.90)
    GS_Face.mat  — URP/Unlit, Transparent,  red   (1.00, 0.10, 0.10, 0.07)
    GS_Glow.mat  — URP/Unlit, Transparent,  red   (1.00, 0.08, 0.08, 0.18)
    GS_Arrow.mat — URP/Unlit, Transparent,  red   (1.00, 0.45, 0.45, 0.50)
    GE_Edge.mat  — URP/Unlit, Opaque,       blue  (0.15, 0.65, 1.00, 0.90)
    GE_Face.mat  — URP/Unlit, Transparent,  blue  (0.15, 0.65, 1.00, 0.07)
    GE_Glow.mat  — URP/Unlit, Transparent,  blue  (0.15, 0.65, 1.00, 0.18)
    NOTE: All materials have _EMISSION disabled + _EmissionColor=black.
          Emission keyword overrides alpha in URP/Unlit → must keep off.

  ── Prefab Structure ─────────────────────────────────────────────────────────

  GateStart.prefab  (Assets/1.Assets/Resources/Prefabs/GateStart.prefab)
    ├─ [TDGateView]      m_SpawnEffect → SpawnParticles ✓
    ├─ GroundGlow        Quad flat y=0.01, 3.0×3.0, GS_Glow mat (red 18%)
    ├─ Post_FL/FR/BL/BR  4 corner posts, GS_Edge mat (red opaque), raised +0.13u
    ├─ Bot_Front/Back/Left/Right   bottom ring bars, GS_Edge mat, raised +0.13u
    ├─ Top_Front/Back/Left/Right   top ring bars, GS_Edge mat, raised +0.13u
    ├─ Face_Front/Back/Left/Right/Top   transparent face fills, GS_Face mat (7%)
    ├─ Arrow             Quad flat on floor, GS_Arrow mat (red 50%)
    └─ SpawnParticles    ParticleSystem — red burst 20p, plays per wave

  GateEnd.prefab  (Assets/1.Assets/Resources/Prefabs/GateEnd.prefab)
    ├─ [TDGateEndView]   m_HitEffect → HitParticles, m_LifeText → LifeText ✓
    ├─ [BoxCollider]     isTrigger=true, size (2,4,2), center (0,2,0)
    │                    → OnTriggerEnter: plays HitParticles (visual only, no LoseLife)
    ├─ HQProp            TowerCrystals prefab, scale 0.75
    ├─ GroundGlow        Quad flat y=0.01, GE_Glow mat (blue 18%)
    ├─ Post_FL/FR/BL/BR  4 corner posts, GE_Edge mat (blue opaque), raised +0.13u
    ├─ Bot_Front/Back/Left/Right   bottom ring bars, GE_Edge mat, raised +0.13u
    ├─ Top_Front/Back/Left/Right   top ring bars, GE_Edge mat, raised +0.13u
    ├─ Face_Front/Back/Left/Right/Top   transparent face fills, GE_Face mat (7%)
    ├─ CoreGlow          ParticleSystem — blue loop, continuous ambient glow
    ├─ HitParticles      ParticleSystem — blue burst 20p khi enemy chạm trigger
    └─ LifeText          TextMeshPro 3D, tilted 55° to face camera, "♥ 20" above top ring

  ── Life System Flow ─────────────────────────────────────────────────────────

  TDPlayerLifeControl (singleton):
    Initialize(): lives = CONFIG_PLAYER_STARTING_LIVES (20), fire onLifeChanged(20)
    LoseLife():   lives--, fire onLifeChanged(lives), if lives<=0 fire onGameOver()

  TDEnemyView.Update() — primary trigger:
    if m_CurrentPathIndex >= m_PathsPosition.Count && !m_HasReachedEnd:
      m_HasReachedEnd = true
      TDPlayerLifeControl.api.LoseLife()
      Destroy(gameObject)

  Guard: m_PathsPosition.Count == 0 → skip (path chưa assign, tránh false trigger)

  TDGameplayMainView subscribes:
    onLifeChanged(lives) → m_LifeText.text = "♥ {lives}" + FlashScreen()
    onGameOver()         → Debug.Log("GAME OVER")  [TODO Phase 8: GameOver panel]

  TDGateEndView.OnTriggerEnter(col):
    → TDEnemyView found → m_HitEffect.Play()  (visual only, no double LoseLife)

  ── FlashScreen Coroutine ─────────────────────────────────────────────────
    m_ScreenFlash.color = (1,0,0,0.35) → lerp alpha 0.35→0 over 0.35s → Color.clear
    m_ScreenFlash: full-screen Image trong Canvas (alpha=0 default)

  ── Inspector assignments (DONE) ─────────────────────────────────────────
    ✅ TDEnemyPathMainView → m_GateStartPrefab = GateStart.prefab
    ✅ TDEnemyPathMainView → m_GateEndPrefab   = GateEnd.prefab
    ✅ TDGameplayMainView  → m_LifeText        = TextMeshProUGUI (HUD life display)
    ✅ TDGameplayMainView  → m_ScreenFlash     = Image (full-screen overlay, alpha=0)


════════════════════════════════════════════════════════════════════════════════
 9. OBSTACLE PLACEMENT (TDObstacleControl)
════════════════════════════════════════════════════════════════════════════════

  Tunables:
    CONFIG_OBSTACLE_COUNT            = 12
    CONFIG_OBSTACLE_EXCLUSION_RADIUS = 2
    CONFIG_OBSTACLE_SPACING          = 3
  Inspector: TDEnemyPathMainView.m_ObstaclePrefabs[5]

  ── Multi-cell Footprint System (Session 20) ─────────────────────────────────

  Each obstacle has a footprint radius (0 = 1×1 cell, 1 = 3×3 cells, etc.)
  Auto-computed at runtime by ComputeFootprintRadii() in TDEnemyPathMainView:
    1. Instantiate each prefab briefly (inactive)
    2. Encapsulate all Renderer bounds in a single Bounds
    3. radius = max(0, CeilToInt(max(size.x, size.z) / (2 × cellSize) - 0.5f))
    4. Destroy temp instance

  Formula results for current 5 prefabs (cellSize=2):
    Rock_01  → radius=1 (3×3 blocked)
    Rock_05  → radius=1 (3×3 blocked)
    Bush_02  → radius=1 (3×3 blocked)
    Shrub_03 → radius=0 (1×1 blocked)
    Tree_01  → radius=1 (3×3 blocked)

  PlaceObstacles() pre-assigns a random prefab index at selection time so
  the visual spawned and the blocked footprint always match.

  onObstaclesPlaced signature (Session 20):
    Action<List<Vector2Int> centers, List<int> prefabIndices, List<Vector2Int> allBlocked>
    centers      → where to Instantiate the visual
    prefabIndices → which m_ObstaclePrefabs[] index per center
    allBlocked    → ALL grid cells blocked (center + footprint cells)
                   used to SetOccupiedCell AND exclude from valid tower cells

  Flow after PlaceObstacles:
    OnObstaclesPlaced → spawn visuals + SetOccupiedCell(allBlocked)
                      → ComputeValidTowerCells(allBlocked excluded)
    OnValidTowerCellsReady → spawn TowerZone tiles on valid cells


════════════════════════════════════════════════════════════════════════════════
 10. WAVE SYSTEM
════════════════════════════════════════════════════════════════════════════════

  ✅  Phase 11 DONE — LevelConfig-driven wave system (session 14)

  LevelConfig (SO — Assets/1.Assets/Resources/Configs/Level Config.asset):
    levelIndex    : màn hiện tại (dùng k_CurrentLevelIndex = 0)
    difficulty    : Easy/Normal/Hard/Extreme/Nightmare
    totalEnemies  : target tổng (actual có thể ±5% do rounding)
    waveCount     : số wave (Extreme/Nightmare clamp lên ≥15)
    waveInterval  : giây nghỉ giữa wave
    spawnInterval : giây giữa 2 enemy trong 1 wave

  Wave structure tự động từ BuildWavePlans():
    Regular wave: ~4-5 enemy, Normal/Fast/Tank mix
    Boss wave:    2.5× enemy count, mix + boss(es) spawn LAST
    Boss positions: ceil(W×k/B)-1 formula → spacing đều, boss cuối = wave cuối

  Constants cũ (DEPRECATED — không dùng cho enemy spawn nữa):
    CONFIG_MAX_WAVES, CONFIG_ENEMIES_NUMBER, CONFIG_ENEMY_SPAWN_DELAY_MS,
    CONFIG_WAVE_INTERVAL_MS  → thay bằng LevelConfig SO fields


════════════════════════════════════════════════════════════════════════════════
 11. ENEMY MOVEMENT (TDEnemyView)
════════════════════════════════════════════════════════════════════════════════

  State fields:
    m_PathsPosition    : List<Vector3>  (world positions của path)
    m_CurrentPathIndex : int
    m_HasReachedEnd    : bool           ← guard tránh LoseLife nhiều lần [4.5]
    m_HasBeenReturned  : bool           ← guard tránh double ReturnToPool [8.5]
    m_EnemyHealth      : float          ← set từ SpawnBatch (baseHP × hpMult) [Phase 11]
    m_MoveSpeed        : float          ← set từ SpawnBatch (baseSpeed × speedMult) [Phase 11]
    m_EnemyKey         : string         ← unique "w{wave}-{type}-e{i}-{instanceId}"
    EnemyType          : EnemyType      ← property { get; private set; } [Phase 11]

  PathProgress: m_CurrentPathIndex / m_PathsPosition.Count (0→1)
    → Tower dùng để ưu tiên enemy gần GateEnd nhất

  State fields (session 16):
    m_IsDying       : bool  ← guard TakeDamage + Update khi đang play Die anim [Phase 13]
    m_DieDuration   : float ← nhận từ EnemyData.dieDuration qua Initialize() [Phase 13]

  Animation fields [Phase 13]:
    m_Animator       : Animator — GetComponent<Animator>() trong Awake()
    TRIGGER_WALK     : const "Walk"
    TRIGGER_GET_HIT  : const "GetHit"
    TRIGGER_DIE      : const "Die"
    TriggerSafe(str) : null-safe SetTrigger (bỏ qua nếu m_Animator == null)

  Initialize(key, hp, speed, dieDuration, enemyType): reset state, set stats,
    Rebind()+Update(0) để reset animator từ pool → TriggerSafe(WALK)
    Subscribe + Register [Phase 11, 13]
  TakeDamage(dmg): guard m_IsDying; health -= dmg; ≤ 0 → Die(); else TriggerSafe(GET_HIT)
  Die(): m_IsDying=true; Unsubscribe+Unregister; TriggerSafe(DIE);
         dieDuration>0 → Coroutine → PoolReturn(); else PoolReturn()
  PoolReturn(): guard → m_HasBeenReturned=true → Pool.Release
  ReturnToPool(): reach-end path → guard → Unsubscribe+Unregister → Pool.Release [8.5]
  OnDestroy():    chỉ cleanup nếu !m_HasBeenReturned (scene unload safety)

  Update():
    guard: m_PathsPosition==null || Count==0 || m_HasReachedEnd || m_IsDying → return
    if m_CurrentPathIndex < Count:
      MoveTowards(target, speed*dt)
      LookRotation(dir)
      if dist < 0.1f: m_CurrentPathIndex++
    else:
      m_HasReachedEnd = true
      TDPlayerLifeControl.api.LoseLife()
      TDGameStateControl.api?.OnEnemyRemoved()  ← [Phase 8: escape cũng là removed → Victory check]
      ReturnToPool()   ← [Phase 8.5: không Destroy — pool release]


════════════════════════════════════════════════════════════════════════════════
 12. CONSTANTS REFERENCE (TDConstant.cs hiện tại)
════════════════════════════════════════════════════════════════════════════════

  ── Grid ─────────────────────────────────────────────────────────────────
  CONFIG_GRID_CELL_SIZE          = 2f
  CONFIG_PATH_MIN_GRID_Y         = 0
  CONFIG_ENEMY_START_POINT       = (0,  6)  → world (2.3,  0, 27.7)  [updated s20]
  CONFIG_ENEMY_END_POINT         = (21, 6)  → world (44.3, 0, 27.7)  [updated s20 — was (20,6)]

  ── Path generation ──────────────────────────────────────────────────────
  CONFIG_PATH_Y_ZONES            = [(7,12), (0,5), (2,10)]            [updated s20 — was (7,11),(0,5),(2,9)]
  CONFIG_PATH_MIN_STEP_X         = 2
  CONFIG_PATH_MAX_STEP_X         = 5
  CONFIG_PATH_MIN_VERTICAL_DELTA = 2

  ── Obstacles ────────────────────────────────────────────────────────────
  CONFIG_OBSTACLE_COUNT             = 12
  CONFIG_OBSTACLE_EXCLUSION_RADIUS  = 2
  CONFIG_OBSTACLE_SPACING           = 3

  ── Wave system ──────────────────────────────────────────────────────────
  CONFIG_MAX_WAVES                  = 10
  CONFIG_ENEMIES_NUMBER             = 5
  CONFIG_ENEMY_SPAWN_DELAY_MS       = 2000
  CONFIG_WAVE_INTERVAL_MS           = 10000

  ── Enemy ────────────────────────────────────────────────────────────────
  CONFIG_ENEMY_MOVE_SPEED        = 3f     ← DEPRECATED (Phase 11: từ EnemyData SO)
  CONFIG_ENEMY_HEALTH            = 300f   ← DEPRECATED (Phase 11: từ EnemyData SO)
  CONFIG_ENEMY_VISUAL_SCALE      = 1.5f   ← DEPRECATED (Phase 11: baked vào từng prefab)

  Enemy stats nguồn thực tế (Phase 11):
    HP    = EnemyData.baseHP    × DifficultyRatioTable.hpMult
    Speed = EnemyData.baseSpeed × DifficultyRatioTable.speedMult
    Scale = baked vào prefab (Slime=1.5, Fast=0.56, Tank=0.74, Boss=0.32)

  ── Player / Gates [Phase 4.5] ───────────────────────────────────────────
  CONFIG_PLAYER_STARTING_LIVES   = 20
  PREFAB_GATE_START              = "GateStart"
  PREFAB_GATE_END                = "GateEnd"

  ── Tower ─────────────────────────────────────────────────────────────────
  CONFIG_TOWER_ROTATIONS         = {0°, 90°, 180°, 270°}
  CONFIG_TOWER (ScriptableObject) = "Tower Bullet Config"
  CONFIG_TOWER_ZONE_TILE_HEIGHT  = 0.12f  ← TowerZone cube scale.y; top face at y=0.06 [s20]
  CONFIG_TOWER_PLACE_Y           = 0.6f   ← tower base placed 0.6u above ground [s20 updated]
                                            (above TowerZone top face at 0.06; allows tower base to clear zone tile)

  ── Misc ─────────────────────────────────────────────────────────────────
  CONFIG_PATH_OFFSET_Y           = 0.1f
  CONFIG_NUM_PATHS               = 3   (legacy, use CONFIG_PATH_Y_ZONES.Length)

  ── HUD / Win/Lose (Phase 8 — DONE session 18+19) ────────────────────────
  CONFIG_PLAYER_STARTING_GOLD       = 10     ← khởi điểm gold
  CONFIG_GOLD_PASSIVE_RATE          = 5f     ← giây per +1 gold passive
  CONFIG_LIFE_LOW_THRESHOLD         = 5      ← <= threshold → life text đỏ
  PATH_GAMEPLAY_HUD_BACK_BUTTON     = "Header/HUDButtonRight/BackButton"
  PATH_GAMEPLAY_HUD_SETTING_BUTTON  = "Header/HUDButtonRight/SettingButton"
  PATH_GAMEPLAY_HUD_ENEMY_COUNT     = "Header/MapInfo/EnemyCount/TxtValue"
  PATH_GAMEPLAY_HUD_LIFE_POINT      = "Header/MapInfo/LifePoint/TxtValue"
  PATH_GAMEPLAY_HUD_SPEED_BUTTON    = "Header/HUDButtonLeft/SpeedButton"
  PATH_GAMEPLAY_HUD_SPEED_VALUE     = "Header/HUDButtonLeft/SpeedButton/TxtValue"
  PATH_GAMEPLAY_HUD_SPEED_ICON_NORMAL = "Header/HUDButtonLeft/SpeedButton/SpeedIcon/Normal"
  PATH_GAMEPLAY_HUD_SPEED_ICON_X2   = "Header/HUDButtonLeft/SpeedButton/SpeedIcon/SpeedX2"
  PATH_GAMEPLAY_HUD_PAUSE_BUTTON    = "Header/HUDButtonLeft/PauseButton"
  PATH_GAMEPLAY_HUD_CURRENCY        = "Bottom/Currency/TxtValue"
  PATH_GAMEPLAY_HUD_PAUSE_PANEL     = "PausePanel"
  PATH_GAMEPLAY_HUD_RESUME_BUTTON   = "PausePanel/ResumeButton"
  PATH_GAMEPLAY_SCREEN_FLASH        = "FlashScreen"  ← child của Container (session 19)


════════════════════════════════════════════════════════════════════════════════
 13. SCENE TUNING HISTORY
════════════════════════════════════════════════════════════════════════════════

  ── Camera evolution ───────────────────────────────────────────────────────
  Session 2-3 (Perspective):
    Start: pos (23.3,28,-0.79) pitch 60° FOV 55°
    Step1: pos (23.3,22,3) FOV 42° → Step2: pitch 50° → Step3: 48° → Step4: FOV 48°

  Session 4 (Orthographic):
    Step5: Perspective→Ortho, orthoSize=20, pitch 48→35°
    Step6: orthoSize=11 (too small) → grid resize 20×12→14×8, cellSize 2→3
    Step7: orthoSize=12, pitch 35→30°, Y 22→15, Z→2
    Step8: 14×8 cellSize=3 → 21×12 cellSize=2, orthoSize 12→14
    Step9 (session 20): pos adjusted (23.3,15,2.02) → (24.5,15,3.0) for better view framing
    FINAL: pos (24.5,15,3.0), pitch=30°, orthoSize=14

  ── Plane evolution ────────────────────────────────────────────────────────
  Session 2-3: (5,5,5)→(3,5,3)→(4,5,3)→(4,5,2.4) pos (23.3,0,28) → 40×24
  Session 4: (6,5,3.6)→(4.2,5,2.4) → world 42×24, grid 21×12
  Session 20: (4.2,5,2.4) pos(23.3,0,28) → (4.4,5,2.6) pos(23.3,0,27.7) → 44×26, grid 22×13
              CityGround alignment achieved; edges at X[1.3,45.3] Z[14.7,40.7] (FINAL)

  ── Tower ghost scale history ──────────────────────────────────────────────
  0.5f → 1.5f (session 4) → 1.0f (session 4 final)

  ── Enemy scale ────────────────────────────────────────────────────────────
  CONFIG_ENEMY_VISUAL_SCALE: 2.5f (cellSize=3) → 1.5f (cellSize=2 final)


════════════════════════════════════════════════════════════════════════════════
 14. DEVELOPMENT ROADMAP — UPDATED STATUS
════════════════════════════════════════════════════════════════════════════════

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 0 — Cleanup & Grid Sync                          [✅ DONE s1]     ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    CONFIG_GRID_CELL_SIZE=2f synced; grid runtime từ Plane bounds; camera/plane/offset khớp.

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 1 — Bug Fixes + A* Refactor                      [✅ DONE s1]    ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    5 bugs fixed: A* heuristic, ComputePath, spawn delay, LookRotation, InitEnemyPath.

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 2 — Multi-path Foundation                        [✅ DONE s2]    ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    Random waypoints per Y-zone; 3 corridors gen 1 lần lúc start.

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 3 — Wave System + Random Path per Wave           [✅ DONE s3]    ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    10 waves, random corridor per wave, 5 enemies stagger 2s; CancellationToken (s8).

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 3.5 — Obstacles Decoration                       [✅ DONE s3/s20]║
  ╚══════════════════════════════════════════════════════════════════════════╝
    12 obstacles, 5 prefab variants; multi-cell footprint auto-computed từ Renderer bounds.
    SetOccupiedCell trên toàn bộ footprint; TowerZone không spawn trên footprint cells.

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 3.6 — Camera + Environment Tuning                [✅ DONE s3]    ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    EnvironmentContractor; mountains Z=130-140; buildings/cliffs pulled in.

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 3.7 — Camera Orthographic + Grid Resize          [✅ DONE s4]    ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    Perspective→Ortho (size=14), pitch 30°, Y=15; grid 21×12 cellSize=2, plane 42×24.

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 4 — Tower Highlight + Arknights Placement        [✅ DONE s4/s8] ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    2-phase placement: drag→cell (P1), swipe>35%cellSize→direction (P2), lift=place.
    Mobile: ghost đóng băng tại ô; direction từ frozen cell; threshold=0.35×cellSize.
    Desktop: ghost follow chuột tự do; direction luôn update; LMB place, RMB cancel.
    ValidTowerCells highlight; obstacles SetOccupiedCell.

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 4.5 — Gate Start / Gate End                      [✅ DONE s5]    ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    TDPlayerLifeControl: LoseLife/onGameOver; SpawnGates() runtime; onWaveStart→PlaySpawnEffect.
    TDEnemyView: m_HasReachedEnd guard; onLifeChanged→HUD text+flash.
    Inspector: m_GateStartPrefab, m_GateEndPrefab, m_LifeText, m_ScreenFlash wired ✅.

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 4.6 — Gate Visual Redesign                       [✅ DONE s6]    ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    Arknights-style 3D wireframe box: 4 posts + 4 bottom/top rings + 5 transparent faces.
    GateStart = red scheme; GateEnd = blue scheme + BoxCollider trigger + LifeText.
    7 URP/Unlit materials (Gates/); _EMISSION disabled (emission overrides alpha in URP).
    Box raised +0.13u; path tiles skip gate cells (GetRange 1..count-2).

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 3.9 — PathTile Visual Restore (Session 25)       [✅ DONE]        ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    Goal: Khôi phục visual path tile đã bị bỏ — dùng PathTile.prefab thay Plane.prefab cũ.
    Mỗi tile tự scale theo size thực từ TDGridMainModel (derive từ GameMapVisualize).

    ✅ PREFAB_PATH: "Plane" → "PathTile" (TDConstant.cs)
    ✅ TDEnemyPathView.VisualizeAllPaths(): thêm spawn PathTile.prefab tại mỗi path cell
       tileScale = TDGridMainModel.api.cellSize / 10f  (Plane native 10u → 2u per cell)
       y = CONFIG_PATH_OFFSET_Y (0.1f); parent = view transform
       HashSet<Vector2Int> deduplicate 3 paths overlapping; skip gate cells (i=0, i=last)
       Tile tracked trong m_InstantiatedTiles → ClearPreviousPath() dọn sạch
    ✅ PathTile.prefab material: guid 03eccdb9 (missing) → Stone_mobile.mat (guid 094c8356)
       Stone_mobile: URP Lit, Opaque, _BaseColor=(1,1,1) full brightness, có normal map,
       cùng texture với Stone_normal nhưng sáng hơn → nổi trên nền CityGround tối

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 3.8 — TowerZone Visual + Grid-CityGround Alignment [✅ DONE]     ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    Goal: Arknights-style "gờ" — bright elevated tile marks where towers can go.
    Dark ground = enemy path; bright cube = valid placement zone.
    Grid cells align perfectly with CityGround floor tiles.

    ✅ TowerZone.mat: URP/Unlit, Opaque, light gray (0.85,0.85,0.85)
    ✅ TowerZone.prefab: 2×2×0.12 cube, center y=0 → top face at y=0.06
    ✅ TDEnemyPathMainView.m_TowerZonePrefab: Inspector assigned ✓
    ✅ ComputeValidTowerCells excludes: path cells + obstacle footprint cells + gate cells
    ✅ OnValidTowerCellsReady: Instantiate TowerZone at each valid cell (y=0, x/z from grid)
    ✅ CONFIG_TOWER_PLACE_Y = 0.6f: ghost + placed towers elevated 0.6u above ground
    ✅ TDTowerMainView.UpdateGhostTransform(): ghost y = CONFIG_TOWER_PLACE_Y (both mobile+desktop)
    ✅ TDPlaceTowerControl.CheckPlaceTower(): placedPosition.y = CONFIG_TOWER_PLACE_Y
    ✅ GameMapVisualize scale (4.4, 5, 2.6) → 44u×26u → grid 22×13 [s20]
    ✅ GameMapVisualize pos (23.3, 0, 27.7): edges X[1.3,45.3] Z[14.7,40.7] [s20]
       → perfectly aligned with CityGround 2u tile boundaries
    ✅ TDGridVisualizerView disabled (no more misaligned gizmo overlay) [s20]
    ✅ GameMapVisualize MeshRenderer re-enabled (material: ground2) [s20]
    ✅ PathView/CreatePoint: (1, 0, 27.7) — just left of new grid left edge [s20]
    ✅ CONFIG_ENEMY_END_POINT: (21, 6) — matches new 22-wide grid [s20]
    ✅ CONFIG_PATH_Y_ZONES: [(7,12),(0,5),(2,10)] — uses full 13-row height [s20]
    ✅ Camera pos: (24.5, 15, 3.0) — adjusted for new grid framing [s20]

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 5 — Multi-start / Màn 2                          [TODO]            ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    CONFIG_START_POINTS = Vector2Int[] nhiều điểm spawn
    Mỗi start → 1-2 paths riêng đến END

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 6 — Tower UI Integration                         [⚠️ PARTIAL]      ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    ✅ 5 TowerHolder HUD slots, Arknights 2-phase placement
    ✅ TDPlaceTowerControl, TDTowerWeaponView
    ✅ TDTowerFactoryView.OnCreateTowerSuccess → tower.Init(key) [đã fix]
    ✅ CanvasScaler Reference Resolution: 1125×750 → 1920×1080  [Session 9]
       + HUD tất cả elements scale ×1.5 để bù lại (xem Phase 6.3 bên dưới)

    ⚠️  TODO: PlacementPanel missing trong scene hierarchy (mobile Cancel button)

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 6.1 — Tower Attack System Redesign               [✅ DONE s7]    ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    Bullet pool: xóa global m_TowerType, 1 pool lazy per TowerType; TDBulletsView.OwnerType.
    Enemy detection: TDEnemyRegistry (singleton list) + tick scan 0.2s; PathProgress priority.
    TDEnemyDetector.cs DELETED; DetectionRadius property thêm vào ITowerRangeDTO.

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 6.2 — Tower Config SO Refactor                   [✅ DONE s8/s11]║
  ╚══════════════════════════════════════════════════════════════════════════╝
    TowerData SO: damage, attackSpeed, rangeOffsets (Vector2Int[]) per tower.
    GetTowerRange() = new TDOffsetRangeDTO(rangeOffsets) — không còn switch.
    SO duplicate xóa; DTLoadFirst.unity GUID fixed. TDVerticalRangeDTO copyPaste bug fixed.

  ─────────────────────────────────────────────────────────────────────────────

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 6.3 — HUD Scale Fix for 1920×1080                [✅ DONE s9]   ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    CanvasScaler 1125×750→1920×1080; toàn bộ HUD elements scale ×1.5 (=1080/750).
    Key: HLG childControlWidth=True → phải set LayoutElement.preferredWidth, không RectTransform.

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 10 — Grid-cell Range + Offset Refactor (Session 11) [✅ DONE]    ║
  ╚══════════════════════════════════════════════════════════════════════════╝

  ── Part A: Grid Cell Helpers ────────────────────────────────────────────────
    ✅ IGridMainModel + TDGridMainModel: 3 method mới
       CellToWorld(Vector2Int)  → m_Grid[cell.x, cell.y]
       WorldToCell(Vector3)     → RoundToInt((world - offset) / cellSize), clamped
       IsInBounds(Vector2Int)   → bounds check không clamp

    ✅ ITowerRangeDTO: thêm GetCellsInRange(Vector2Int, Quaternion) → List<Vector2Int>

    ✅ TDRangeDTO.cs (abstract base class):
       GetCellsInRange virtual — default impl dùng bounding box scan + IsInRange
       searchRadius = CeilToInt(DetectionRadius / cellSize)

  ── Part B: Offset-based Range (Arknights style) ─────────────────────────────
    Lý do: geometric DTOs (dot product / radius) không mô tả được range pattern
    phức tạp như Arknights. Thay bằng explicit cell offsets.

    ✅ TDOffsetRangeDTO.cs (NEW):
       Constructor: TDOffsetRangeDTO(Vector2Int[] offsets)
       Offsets định nghĩa khi tower facing +X (Y=90°) — x=forward, y=right-of-facing
       DetectionRadius: max(offset.magnitude) × cellSize (cached)
       IsInRange: WorldToCell(enemy) → check cell trong rotated offsets — O(n), no alloc
       GetCellsInRange: rotate offsets → filter IsInBounds → return list
       RotateOffset: 4 hướng cardinal:
         Y=  0° (facing +Z): (dx,dy) → ( dy,  dx)
         Y= 90° (facing +X): (dx,dy) → ( dx,  dy)  ← definition space
         Y=180° (facing -Z): (dx,dy) → (-dy, -dx)
         Y=270° (facing -X): (dx,dy) → (-dx, -dy)

    ✅ TDRangeDTO.GetCellsInRange → virtual (để TDOffsetRangeDTO override hiệu quả hơn)

    ✅ TDFlyweightTowerDataSettings — TowerData:
       XÓA: RangeType rangeType, int rangeValue
       THÊM: Vector2Int[] rangeOffsets

    ✅ TDTowerBehaviorModel.GetTowerRange():
       return new TDOffsetRangeDTO(data?.rangeOffsets)  ← 1 dòng, không switch

    ✅ Enums.cs: XÓA RangeType enum (Area/HorizontalCone/VerticalCone)

    ✅ DELETED: TDAreaRangeDTO.cs, TDHorizontalRangeDTO.cs, TDVerticalRangeDTO.cs

  ── Part C: SO Range Patterns ────────────────────────────────────────────────
    rangeOffsets set trong Tower Bullet Config.asset (offset = cells, facing +X):

    Cannon    (3 ô thẳng):      (1,0)(2,0)(3,0)
    Catapult  (2×3 wide area):  (1,-1)(1,0)(1,1)(2,-1)(2,0)(2,1)
    MissileG02 (4 ô thẳng):    (1,0)(2,0)(3,0)(4,0)
    MissileG03 (diamond area):  (1,-1)(1,0)(1,1)
                                (2,-2)(2,-1)(2,0)(2,1)(2,2)
                                (3,-1)(3,0)(3,1)
    Mortar    (skip 1, area):   (2,-1)(2,0)(2,1)(3,-1)(3,0)(3,1)(4,0)

    → Thêm tower mới: chỉ cần 1 enum line + 1 SO entry + vẽ offsets trong Inspector

  ── Part D: Custom Inspector Grid Editor ─────────────────────────────────────
    ✅ TowerDataDrawer.cs (Assets/2.Scripts/Editor/) — CustomPropertyDrawer cho TowerData
       Hiển thị lưới 8×9 click-to-toggle trong Inspector (x: -1→6, y: -4→4)
       Tower (0,0) = màu xanh dương, không toggle
       Ô active = màu xanh lá, ô inactive = tối với tọa độ nhỏ làm reference
       Click ô → toggle rangeOffsets array tự động — không cần edit số tay

  ── Pattern usage (AOE — implemented Phase 7) ────────────────────────────────
    // TDAttackVFX.ApplyAOEDamage():
    var hitCell = TDGridMainModel.api.WorldToCell(transform.position);
    var cells   = m_RangeDTO.GetCellsInRange(hitCell, m_TowerRotation);
    // → damage tất cả enemy có WorldToCell trong cells (copy list để tránh modify-while-iterate)

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 7 — Ammo + Combat                                [✅ DONE s12]   ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    AttackType: Single (1 target homing) / Multiple (top N) / AOE (splash rangeOffsets).
    TDAttackVFX: homing → fly-to-last-known nếu target chết → OnImpact → ReturnToPool.
    ScanForTargets: 0.2s tick, sort PathProgress desc, top N targets.
    Cannon=Single; Catapult=Multiple(3); MissileG02=Single; MissileG03/Mortar=AOE.
    ⬜ VFX prefab swap per tower (SO only, no code change needed)

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 11 — Multi-Enemy Type + LevelConfig              [✅ DONE s14]   ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    4 EnemyType: Normal(Slime s=1.5), Fast(SwarmFast s=0.56), Tank(TurtleTank s=0.74), Boss(DragonBoar s=0.32).
    LevelConfig SO: difficulty, totalEnemies, waveCount, waveInterval, spawnInterval.
    BuildWavePlans(): regular wave + boss event wave 2.5×; boss spawn last; ceil(W×k/B)-1 distribution.
    4 ObjectPool per EnemyType; DifficultyRatioTable (HP×/Speed× multiplier).
    ⬜ DEF/RES stats; Air Drone tier; BeholderPBR Elite

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 12 — Tower Range Visualization                   [✅ DONE s15]   ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    RefreshRangeHighlights(): lazy tile pool, GetCellsInRange per frame, SetActive.
    y=0.12f (>path tile 0.10f) → transparent queue sort đúng → on top ✓.
    HideRangeHighlights() khi cancel/place. Inspector: m_RangeHighlightPrefab assigned ✅.

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 13 — Enemy Animation System                      [✅ DONE s16]   ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    TDEnemyView: const triggers Walk/GetHit/Die; m_Animator (Awake); Rebind+Update(0) on pool reuse.
    SwarmFast-Game.controller + TurtleTank-Game.controller [NEW] — Trigger-based AnyState.
    DragonBoarCTRL: "Get Hit"→"GetHit" renamed.
    dieDuration: Slime=0, Fast=0.63s, Tank=2.0s, Boss=1.33s (set trong Enemy Data Config.asset).

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 8 — Win/Lose Conditions + HUD           [⚠️ PARTIAL s17-19]     ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    ✅ HUD Top Bar: BackButton(quit), EnemyCount, LifePoint(đỏ ≤5), SpeedToggle, PauseButton.
    ✅ Gold: passive +1/5s; kill reward; SpendGold khi đặt; holder disable khi thiếu gold.
    ✅ TDGoldControl, TDGameStateControl, TDSpeedControl, TDGameplayHUDView wired ✅.
    ✅ Win/Lose: Victory=AllWavesSpawned+AllRemoved; Defeat=lives≤0; IsGameEnded guard.
    ⬜ CÒN LẠI: VictoryPanel UI + GameOverPanel UI (stubs OnVictory/OnGameOver đã có).

  ─────────────────────────────────────────────────────────────────────────────

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 8.5 — Pause System + Enemy Object Pool           [✅ DONE s10]   ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    TDPauseControl: Toggle/Pause/Resume → Time.timeScale 0/1; onPauseChanged event.
    PauseAwareDelay: Task.Yield() loop + Time.deltaTime → tự pause khi timeScale=0.
    Enemy Pool: ObjectPool<TDEnemyView> cap=20 maxSize=50; ReturnToPool thay Destroy.
    m_HasBeenReturned guard tránh double-return khi Die + reach-end đồng thời.

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 14 — Operator System                             [✅ DONE s21/22]║
  ╚══════════════════════════════════════════════════════════════════════════╝
    Arknights path guard: đặt lên PATH cells, chặn enemy (blockCount=2), bidirectional combat.
    TDOperatorRegistry: ValidPathCells, Operators dict, BlockedEnemies dict, CanBlock, ForceUnblock.
    TDOperatorView: HP system, TakeDamage, Die()→UnregisterOperator→ForceUnblock→Destroy.
    Enemy: arrive→CanBlock()→m_IsBlocked=true; attackTimer→operator.TakeDamage(); Die→UnblockFromOperator.
    OperatorKnight.prefab: Synty Humanoid, sword animations (Idle/Attack01_SingleSword).
    MeleeOperator.prefab DELETED; TDMeleeRegistry DELETED.
    SO: Melee Operator Config.asset — hp=600, damage=50, blockCount=2, rangeOffsets={(0,0)}.
    ⚠️ Operator Die: Destroy ngay sau TriggerSafe("Die") → cần delay như dieDuration.
    ⬜ Retreat mechanic → Phase 17.

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 15 — Dynamic Slot System                         [✅ DONE s22]   ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    BuildSlots(): merge TowerBulletConfig + OperatorConfig → max 8 slots (random nếu >8).
    TDTowerSlotInfo struct: prefab, towerType, operatorType, cost, icon.
    TDSlotHolderItemView: icon, cost TMP, disabled overlay; auto-disable khi gold < cost.
    SlotHolder.prefab: ButtonSelector/SlotIcon/CostBg/DisabledOverlay + TDSlotHolderItemView.
    Icon assigned trong cả 2 Config.asset ✅ [s24].
    ⚠️ Debug.LogError còn trong InitTowerHolder() → đổi thành Log khi hoàn thiện.

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 16 — Interface Refactor + OperatorType           [✅ DONE s23]   ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    IDeployableDTO [NEW]: stats chung Tower+Operator; implement bởi TowerData+OperatorData (explicit).
    IPlacedUnit [NEW]: Init(key,slot)+OnRemove(); implement bởi TDTowerWeaponView+TDOperatorView.
    TDTowerBehaviorModel: Dictionary<TowerType, IDeployableDTO> (xóa if/else); InvalidateCache().
    TDTowerFactoryControl.CreateUnit(): GetComponent<IPlacedUnit> (unified, xóa branch Operator).
    TDOperatorView.Init() tự RegisterOperator; Die()→OnRemove()+EventBus+Destroy.
    onGetTowerPrefab: Action<TDTowerSlotInfo>; OperatorType enum: { Knight }.
    TDGameEventBus [NEW]: static event hub OnEnemyDied/OnOperatorDied/OnVictory/...
    Code style enforced: Allman braces, 1 space quanh operator.

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 18 — Toon Shader + Outline + HP Bar Depth Fix    [✅ DONE s27]   ║
  ╚══════════════════════════════════════════════════════════════════════════╝

  ── Toon Materials ───────────────────────────────────────────────────────────
    Shader: Toon Shaders Pro/URP/Toon (LightMode=UniversalForwardOnly, 3-band cel-shading)
    Folder: Assets/1.Assets/Materials/Toon/

    Material            | Enemy        | BaseMap source
    --------------------|--------------|--------------------------------
    Toon_Slime.mat       | Slime        | RPG Monster DUO (a291ce08)
    Toon_TurtleTank.mat  | TurtleTank   | RPG Monster DUO (a291ce08)
    Toon_SwarmFast.mat   | SwarmFast    | Monster Minion (2fc5fd69)
    Toon_DragonBoarBoss.mat | DragonBoar| FreeDragons (aa6db0ae)
    Toon_OperatorKnight.mat | Operator  | Synty Sidekick (a8c2d05f)

    Settings (sau điều chỉnh s27):
      _ShadowTint   : (0.25, 0.20, 0.28)  ← tím nhạt, không near-black
      _MiddleTint   : (0.72, 0.72, 0.75)
      _LightTint    : (1, 1, 1)
      _DiffuseThresholds: (-0.1, 0.2)     ← mở rộng vùng sáng
      _GIStrength   : 1.0                 ← full ambient
      _UseSecondThreshold: 1 (3-band)
      _RimColor     : (0.6, 0.8, 1.0)     ← xanh nhạt
      _SpecularThresholds: (0.01, 0.02)

  ── Outline System ───────────────────────────────────────────────────────────
    OutlineFeature (ScriptableRendererFeature) added → URP-Balanced-Renderer.asset
    ToonOutlineProfile.asset: DepthNormalOutlines, outlineColor=black, depth/normal strength=1.0
    ⚠️ ToonOutlineVolume (Global Volume) cần đặt thủ công vào DTGamePlay.unity
       Profile: Assets/Settings/ToonOutlineProfile.asset, isGlobal=true

  ── HP Bar Depth Fix ─────────────────────────────────────────────────────────
    Root cause: World Space Canvas dùng UI/Default (ZTest=LEqual) → model 3D đè.
    Fix: Assets/2.Scripts/Shaders/UI_AlwaysOnTop.shader [NEW]
         Queue=Overlay; ZTest Always; ZWrite Off; Blend SrcAlpha OneMinusSrcAlpha.
    Assets/1.Assets/Materials/UI/HPBar_AlwaysOnTop.mat → Custom/UI_AlwaysOnTop shader.
    Assigned to: HPBar_Enemy.prefab + HPBar_Operator.prefab (cả Background + Fill Images).

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 17 — Retreat Mechanic                            [TODO]            ║
  ╚══════════════════════════════════════════════════════════════════════════╝

  Requires Phase 16 (IPlacedUnit.OnRemove()) complete trước.

  ── Design ───────────────────────────────────────────────────────────────────
    Trigger: Click operator đang đứng trên map → hiện Retreat UI button
    Flow:
      User click Retreat
      → TDOperatorView.Retreat()
          → play retreat animation
          → TDOperatorRegistry.WithdrawOperator(cell):
              ForceUnblock() tất cả enemy (m_CurrentPathIndex++, resume)
              UnregisterOperator(cell)
          → IPlacedUnit.OnRemove()
          → Destroy(gameObject) sau animation

    Tách biệt Die() vs Retreat():
      Die()     = bị tiêu diệt bởi enemy → unregister → Destroy
      Retreat() = chủ động của player    → unregister → Destroy

    Gold refund: TBD (0% hoặc 50% tùy balance)
    Cooldown per operator type: optional

  ── Files cần thêm/sửa ───────────────────────────────────────────────────────
    TDOperatorView: + Retreat() method, tách logic unregister thành OnRemove()
    TDOperatorRegistry: + WithdrawOperator(cell) (alias UnregisterOperator với flag)
    TDSlotHolderMainView: detect click vào placed operator → show Retreat button
    TDGameplayHUDView: Retreat button UI (hoặc world-space button trên operator)

  ─────────────────────────────────────────────────────────────────────────────

  ╔══════════════════════════════════════════════════════════════════════════╗
  ║  PHASE 9 — Polish & Performance                         [TODO]            ║
  ╚══════════════════════════════════════════════════════════════════════════╝
    Polish: particles, VFX, sounds, camera shake on life lost
    Performance: object pooling (tiles, ammo) — enemies DONE (Phase 8.5) ✓


════════════════════════════════════════════════════════════════════════════════
 15. KNOWN ISSUES & TECH DEBT
════════════════════════════════════════════════════════════════════════════════

  ✅  Tower không attack                        FIXED session 7 (Phase 6.1)
      TDTowerFactoryView.OnCreateTowerSuccess đã subscribe + gọi tower.Init(key)
      Pool bug (global m_TowerType) đã fix — mỗi TowerType có pool riêng

  ✅  async void StartWaveLoop không có CancellationToken  FIXED session 8
      TDEnemyPathMainControl.StartWaveLoop + SpawnWave nhận CancellationToken
      TDEnemyPathMainView: m_WaveCts tạo khi start, Cancel+Dispose trong OnDestroy
      Task.Delay dùng token → dừng sạch khi scene unload, không còn exception

  ✅  TDGridMainModel singleton không reset khi reload scene  FIXED session 8
      Initialize(): ??= → = (luôn tạo instance mới, không giữ lại state cũ)

  ✅  Phase 4.5 Inspector đã assign (session 5)
      TDEnemyPathMainView: m_GateStartPrefab, m_GateEndPrefab
      TDGameplayMainView:  m_LifeText, m_ScreenFlash

  ✅  Gate cells hiển thị path tile bên dưới   FIXED session 6 (Phase 4.6)
      TDEnemyPathView.VisualizeAllPaths: path.GetRange(1, path.Count-2)

  ✅  Gate box edges bị path tile che mất       FIXED session 6 (Phase 4.6)
      Raise toàn bộ box parts +0.13u qua GateRaiseBox.cs Editor script

  ✅  Gate alpha không hoạt động (URP/Unlit)    FIXED session 6 (Phase 4.6)
      DisableKeyword("_EMISSION") + _EmissionColor=black + _Surface=1

  ✅  Obstacle cells gọi SetOccupiedCell  FIXED session 3
  ✅  Mobile Rotate + Cancel button       FIXED session 3 (Arknights-style)
  ✅  Build APK lỗi CS0246 MenuItem       FIXED session 3 (#if UNITY_EDITOR)
  ✅  Ghost không place khi chưa chọn hướng FIXED session 4 (m_IsDirectionSelected)
  ✅  Enemy false-trigger reach-end       FIXED session 5 (Count==0 guard)

  ✅  Gate cells không gọi SetOccupiedCell  FIXED session 8
      TDEnemyPathMainView.SpawnGates(): SetOccupiedCell(startWorld) + SetOccupiedCell(endWorld)
      → Player không còn đặt được tower lên ô GateStart/GateEnd

  ✅  TDVerticalRangeDTO copy-paste từ TDHorizontalRangeDTO  FIXED session 8 (Phase 6.2)
      Mới: Mathf.Abs(Dot(right, toEnemy)) > 0.7f → perpendicular band attack

  ✅  TDFlyweightBulletFactoryModel global m_TowerType state  FIXED session 7 (Phase 6.1)
  ✅  TDEnemyDetector SetTarget(null) mất target (Bug B)      FIXED session 7 (Phase 6.1)
  ✅  DetectionArea child GO trên tower prefabs               FIXED (đã clean trước session 8)

  ✅  Mobile direction selection không hoạt động             FIXED session 8
      Bug: m_DirectionThreshold (1.0f) = cellSize/2 → snap cell cùng lúc threshold → direction reset ngay
      Fix mobile: ghost đóng băng; delta từ frozen cell; threshold = cellSize×0.35f (< snap boundary)
      Fix desktop: direction luôn follow chuột (no threshold, sqrMag>0.01f), ghost không đóng băng
      Platform-split: #if UNITY_ANDROID && !UNITY_EDITOR trong UpdateGhostTransform()

  ✅  CanvasScaler 1125×750 → 1920×1080 + HUD scale fix    FIXED session 9 (Phase 6.3)
      TxtLifePoint, TowerMainView, TowerHolder ×5 và inner children scale ×1.5
      HorizontalLayoutGroup padding/spacing 6→9; LayoutElement preferredW/H updated

  ✅  White screen / overexposed game view                  FIXED session 15
      Root cause: ArkEnvironment floor tiles (Generic_Road, Generic_01_A) BaseColor=(1,1,1,1)
      → pure white surface + ACES Tonemapping blow out toàn bộ center area
      Fix: Global Volume profile — Tonemapping ACES → Neutral; postExposure +0.1 → -0.3
      File: Assets/3.Scenes/DTGamePlay.unity (Global Volume component)

  ✅  Range highlight bị path tile đè lên                   FIXED session 15
      Root cause: range highlight y=0.07f < path tile y=0.10f (CONFIG_PATH_OFFSET_Y)
      Transparent queue sort by Y (higher=closer=rendered last=on top) → path tile che highlight
      Fix: range highlight y=0.12f > path tile y=0.10f
      File: TDTowerMainView.cs line 362

  ✅  TDMeleeRegistry → TDOperatorRegistry refactor              DONE session 22
      Bidirectional combat: operator nhận damage, có HP, ForceUnblock khi chết
      MeleeOperator.prefab DELETED; OperatorKnight.prefab thay thế

  ✅  Dynamic Slot System                                        DONE session 22
      TDSlotHolderMainView + TDSlotHolderItemView + BuildSlots() từ cả 2 configs
      onGetTowerPrefab(prefab, type) thay onGetTowerName(name, type)

  ✅  Phase 16 — Interface Refactor                              DONE session 23
      IDeployableDTO + IPlacedUnit interfaces
      OperatorType enum; TDTowerSlotInfo.operatorType field
      TDTowerBehaviorModel: Dictionary<TowerType, IDeployableDTO>, xóa if/else
      TDTowerFactoryControl: GetComponent<IPlacedUnit>, CreateUnit unified
      TDPlaceTowerControl: RegisterOperator moved vào TDOperatorView.Init()
      onGetTowerPrefab: Action<TDTowerSlotInfo>; TDTowerFactoryView: empty stub

  ✅  Phase 3.9 — PathTile Visual Restore                        DONE session 25
      PREFAB_PATH: "Plane" → "PathTile"
      VisualizeAllPaths(): spawn PathTile.prefab tại path cells, tileScale=cellSize/10f
      PathTile.prefab material fixed (missing → Stone_mobile.mat)

  ✅  Phase 18 — Toon Shader + Outline + HP Bar Depth Fix        DONE session 27
      5 Toon materials (Toon Shaders Pro/URP/Toon) assigned vào enemy+operator prefabs
      OutlineFeature added to URP-Balanced-Renderer.asset (DepthNormalOutlines)
      Custom/UI_AlwaysOnTop shader (ZTest Always) → HPBar depth bug FIXED

  ⬜  Phase 17 — Retreat Mechanic (PLANNED, sau Phase 16)
      TDOperatorView.Retreat() tách riêng với Die()
      TDOperatorRegistry.WithdrawOperator(cell) → ForceUnblock + unregister
      Gold refund: TBD; cooldown per OperatorType: optional

  ⚠️  PlacementPanel missing trong scene hierarchy
      TDSlotHolderMainView.InitPlacementPanel(): transform.Find(GAMEPLAY_PLACEMENT_PANEL) = null
      → Mobile Cancel button không hiển thị (null-safe → không crash)
      Fix: Thêm PlacementPanel GameObject vào scene DTGamePlay.unity

  ⚠️  TDSlotHolderMainView.InitTowerHolder() có Debug.LogError dùng để test
      → đổi thành Debug.Log hoặc xóa khi hoàn thiện
  ✅  Icon field đã assign trong Tower Bullet Config.asset + Melee Operator Config.asset  [s24]
  ⚠️  TDTowerMainView.cs (file cũ) có thể còn tồn tại sau refactor Phase 15
      → xóa file nếu không còn sử dụng để tránh compile conflict
  ⚠️  Operator death animation: Destroy ngay sau TriggerSafe("Die")
      → cần thêm delay (tương tự EnemyData.dieDuration) để animation play xong
  ⚠️  HUD dùng text symbol thay sprite icon (session 17 review)
      ☠ ♥ ♦ là text character trong TextMeshPro, không phải Image sprites
      → Kém professional, không nhất quán với Arknights style
      Fix: Thay bằng Layer Lab PictoIconPack01 sprites (xem Section 19)

  ⚠️  Tower bar không full-width (session 17 review)
      Cards right-aligned → khoảng trống trống bên trái
      Fix: HorizontalLayoutGroup với childForceExpandWidth=true, hoặc anchor stretch

  ⚠️  Gold chip ở bottom-right thay vì top-right (session 17 review)
      Nên move lên HUDTopRight để nhất quán với Arknights layout
      Fix: Phase 8.HUD Polish step 4

  ✅  SO duplicate Tower Bullet Config                        FIXED session 8
      Xóa Assets/2.Scripts/Model/Config/SO/Tower Bullet Config.asset
      DTLoadFirst.unity GUID → trỏ đúng Resources/Configs/Tower Bullet Config.asset


════════════════════════════════════════════════════════════════════════════════
 16. DEPENDENCY GRAPH (session 7)
════════════════════════════════════════════════════════════════════════════════

  ✅ Phase 0   (Grid sync)              ── DONE
  ✅ Phase 1   (Bug fixes A*)           ── DONE
  ✅ Phase 2   (Multi-path)             ── DONE
  ✅ Phase 3   (Wave system)            ── DONE
  ✅ Phase 3.5 (Obstacles)              ── DONE
  ✅ Phase 3.6 (Camera/Env Perspective) ── DONE
  ✅ Phase 3.7 (Camera Ortho + resize)  ── DONE
  ✅ Phase 4   (Tower Arknights place)  ── DONE
     │   Session 8: direction selection bug fixed (ghost freeze + threshold 35% cellSize)
  ✅ Phase 4.5 (Gate Start/End + Life)  ── DONE
     │   Session 8: gate cells SetOccupiedCell fixed
  ✅ Phase 4.6 (Gate Visual Redesign)   ── DONE (session 6)
  ✅ Phase 6.1 (Tower Attack Redesign)  ── DONE (session 7)
     │   Pool per type, Registry+tick detection, PathProgress priority
  ✅ Phase 6.2 (Tower Config SO Refactor) ── DONE (session 8, updated session 11)
     │   damage/attackSpeed → SO; bỏ hardcode switch
     │   TDVerticalRangeDTO fixed; SO duplicate cleaned; wave CTS added
     │   ⚠️  Session 11: rangeType/rangeValue REMOVED → replaced by rangeOffsets (Vector2Int[])
     │
  ⬜ Phase 5   (Multi-start)            ── cần Phase 3+4 DONE ✓
     │
  ⚠️  Phase 6  (Tower UI)               ── PARTIAL
     │   ✅ CanvasScaler 1920×1080 + HUD scale fix: DONE (Phase 6.3) ✓
     │   Còn: PlacementPanel GO missing trong scene
     │   Attack system: DONE (Phase 6.1) ✓
     │   Config SO: DONE (Phase 6.2) ✓
     │
  ✅ Phase 10  (Grid-cell Range Refactor) ── DONE (session 11)
     │   Part 1: CellToWorld / WorldToCell / IsInBounds added to TDGridMainModel
     │   Part 2: TDOffsetRangeDTO (Arknights-style explicit cell offsets, auto-rotate 4 dirs)
     │           TDAreaRangeDTO / TDHorizontalRangeDTO / TDVerticalRangeDTO → DELETED
     │           RangeType enum → DELETED; TDRangeDTO abstract base (virtual GetCellsInRange)
     │   Part 3: SO rangeOffsets configured for all 5 tower types
     │   Part 4: TowerDataDrawer custom Inspector — click-to-toggle 8×9 grid (Editor only)
     │
  ✅ Phase 7   (Ammo + Combat)          ── DONE (session 12)
     │   AttackType Single/Multiple/AOE; TDAttackVFX homing; AOE splash GetCellsInRange
     │   TowerData SO: attackType + maxTargets; TowerDataDrawer cập nhật
     │
  ✅ Phase 11  (Multi-Enemy Type + LevelConfig)  ── DONE (session 14)
     │   EnemyType enum: Normal/Fast/Tank/Boss
     │   TDEnemyDataSettings SO: baseHP, baseSpeed, prefab per type
     │   TDLevelConfigSettings SO: difficulty, totalEnemies, waveCount, intervals
     │   DifficultyRatioTable: % type distribution + HP×/Speed× per difficulty
     │   4 ObjectPool<TDEnemyView> — 1 per EnemyType (Dictionary<EnemyType, pool>)
     │   BuildWavePlans(): regular wave + boss wave (2.5× size, boss spawn last)
     │   Boss distribution: ceil(W×k/B)-1 formula — spacing đều, cuối=wave cuối
     │   Extreme/Nightmare constraint: waveCount≥15, totalEnemies≥75
     │   3 enemy prefabs mới: SwarmFast(0.56), TurtleTank(0.74), DragonBoarBoss(0.32) scale baked
     │   path1Ratio REMOVED — wave corridor random per wave (old behavior preserved)
     │
  ✅ Phase 12  (Range Visualization)    ── DONE (session 15)
     │   TDTowerMainControl: onGetTowerName Action<string>→Action<string,TowerType>
     │   k_TowerDefs[] lookup table thay switch statement
     │   TDTowerMainView: RefreshRangeHighlights() lazy pool; HideRangeHighlights()
     │   RangeHighlight y=0.12f > path tile y=0.10f → transparent sort đúng
     │   Inspector: m_RangeHighlightPrefab = RangeHighlight.prefab
     │
  ✅ Phase 13  (Enemy Animation)        ── DONE (session 16)
     │   TDEnemyView: const triggers; Animator GetComponent Awake; Die coroutine
     │   TDEnemyDataSettings: EnemyData.dieDuration added
     │   SwarmFast-Game.controller + TurtleTank-Game.controller (NEW — Trigger-based)
     │   DragonBoarCTRL: "Get Hit" → "GetHit" (renamed để khớp const)
     │   TDEnemyPathMainControl.SpawnBatch: truyền data.dieDuration vào Initialize()
     │
  ✅ Phase 14  (Operator System)        ── DONE (session 21 MVP, session 22 refactor)
     │   TDOperatorRegistry: ValidPathCells, BlockedEnemies, ForceUnblock
     │   TDOperatorView: HP system, TakeDamage, Die + Destroy
     │   TDFlyweightOperatorDataSettings SO + OperatorData struct
     │   TDTowerFactoryControl: detect TDOperatorView vs TDTowerWeaponView
     │   TDEnemyView: check CanBlock on arrive, attackTimer → operator.TakeDamage
     │   OperatorKnight.prefab: Humanoid, AnimatorController, sword animations
     │   MeleeOperator.prefab DELETED; TDMeleeRegistry DELETED
     │   TowerType.Operator (was TowerType.Melee in s21)
     │
  ✅ Phase 15  (Dynamic Slot System)    ── DONE (session 22)
     │   TDTowerSlotInfo struct; BuildSlots() từ cả 2 configs (max 8, random nếu >8)
     │   TDSlotHolderMainView: dynamic instantiate SlotHolder prefab × N
     │   TDSlotHolderItemView: icon, cost, button, disabled overlay
     │   onGetTowerPrefab: Action<GameObject, TowerType> (thay onGetTowerName)
     │   TowerData + OperatorData: thêm icon field
     │   TowerDataDrawer + OperatorDataDrawer: icon + ammoPrefab fields
     │   ✅ Icon đã assign trong Inspector  [s24]
     │
  ✅ Phase 16  (Interface Refactor)     ── DONE (session 23)
     │   IDeployableDTO (naming: DTO suffix, convention-compliant)
     │   IPlacedUnit: TDTowerWeaponView + TDOperatorView implement
     │   OperatorType enum: Knight; TDTowerSlotInfo.operatorType field
     │   TDTowerBehaviorModel: Dictionary<TowerType, IDeployableDTO>, no if/else
     │   TDTowerFactoryControl: CreateUnit + GetComponent<IPlacedUnit>
     │   TDPlaceTowerControl: RegisterOperator moved vào TDOperatorView.Init
     │   onGetTowerPrefab: Action<TDTowerSlotInfo>
     │   TDGameEventBus [NEW]: static event hub cho VFX/Audio cross-cutting
     │
  ✅ Phase 3.9 (PathTile Visual Restore) ── DONE (session 25)
     │   PREFAB_PATH: "Plane" → "PathTile"; Stone_mobile.mat gán vào PathTile.prefab
     │   VisualizeAllPaths(): spawn PathTile tại path cells, tileScale=cellSize/10f
     │   HashSet deduplicate; skip gate cells; parent=view transform
     │
  ✅ Phase HP  (HP Bar System)           ── DONE (session 26, depth fix s27)
     │   TDHPBarView.cs [NEW]: Show/Hide/UpdateHP/ResetBar
     │   HPBar_Enemy.prefab (orange fill, ẩn đến TakeDamage lần đầu)
     │   HPBar_Operator.prefab (blue fill, hiện ngay Init())
     │   World Space Canvas, 200×20 px → 1.6u wide; LateUpdate fix position+rotation
     │   Nested vào 4 enemy prefabs + OperatorKnight.prefab ✅
     │   ✅ Depth bug FIXED s27: Custom/UI_AlwaysOnTop shader (ZTest Always)
     │      HPBar_AlwaysOnTop.mat assigned vào cả 2 HPBar prefabs
     │
  ✅ Phase 18  (Toon Shader + Outline + HP Bar Depth Fix) ── DONE (session 27)
     │   5 Toon materials: Toon_Slime/TurtleTank/SwarmFast/DragonBoarBoss/OperatorKnight
     │   Shader: Toon Shaders Pro/URP/Toon; 3-band, rim light, specular dot
     │   YAML patch trực tiếp vào prefab files; brightness điều chỉnh (GIStrength=1)
     │   OutlineFeature added to URP-Balanced-Renderer.asset
     │   ToonOutlineProfile.asset (DepthNormalOutlines) — Volume cần đặt thủ công vào DTGamePlay
     │   Custom/UI_AlwaysOnTop shader (ZTest Always); HPBar_AlwaysOnTop.mat → 2 HPBar prefabs
     │
  ⬜ Phase 17  (Retreat Mechanic)       ── TODO (requires Phase 16 ✓)
     │   TDOperatorView.Retreat() + TDOperatorRegistry.WithdrawOperator(cell)
     │   UI: click placed operator → Retreat button
     │   Tách biệt Die() (forced) vs Retreat() (voluntary)
     │
  ⚠️  Phase 8   (Win/Lose + HUD)         ── PARTIAL (session 17+18)
     │   ✅ HUD scene elements: top bar, gold counter, tower bar (session 17)
     │   ✅ Control layer: TDGoldControl, TDGameStateControl, TDSpeedControl (session 18)
     │   ✅ View: TDGameplayHUDView — cache + events + FlashScreen (session 18)
     │   ✅ Wiring: Die→gold, Die→state, OnAllWavesSpawned, Pause→SpeedMultiplier (session 18)
     │   ✅ TDGameplayMainView stripped — chỉ giữ init logic (session 18)
     │   ⬜ VictoryPanel UI + GameOverPanel UI (stubs đã có)
     │   ⬜ PausePanel wiring trong TDGameplayHUDView
     │   ⬜ Inspector: gắn TDGameplayHUDView vào Container; goldReward trong SO
     │
  ✅ Phase 8.5 (Pause + Enemy Pool)     ── DONE (session 10)
     │
  ⬜ Phase 9   (Polish + Perf)          ── final pass

  ── Thứ tự implement đề xuất (cập nhật session 27) ──────────────────────
  ✅ Phase 10 → ✅ Phase 7 → ✅ Phase 11 → ✅ Phase 12 → ✅ Phase 13 → ✅ Phase 14 → ✅ Phase 15 → ✅ Phase 16 → ✅ Phase 3.9 → ✅ Phase HP → ✅ Phase 18 → ⬜ Phase 17 (Retreat) → ⚠️ Phase 8 (VictoryPanel/GameOverPanel) → ⬜ Phase 9 (Polish)

  Lý do thứ tự:
    Phase 10 trước Phase 7  : area damage cần grid-cell range → tránh viết lại (DONE)
    Phase 7 DONE            : Single/Multiple/AOE + homing TDAttackVFX (DONE)
    Phase 11 song song P7   : multi-enemy độc lập với combat ammo logic
    Phase 12 DONE           : range visualization dùng TDOffsetRangeDTO từ Phase 10
    Phase 8 sau Phase 7     : Win/Lose cần combat hoàn chỉnh để test đúng
    Phase 9 cuối            : polish sau khi core gameplay stable


════════════════════════════════════════════════════════════════════════════════
 17. KEY FILES QUICK REFERENCE
════════════════════════════════════════════════════════════════════════════════

  ── Model / Config ──────────────────────────────────────────────────────────
  Assets/2.Scripts/Model/Config/Constant/TDConstant.cs
  Assets/2.Scripts/Model/Info/Grid/TDGridMainModel.cs
  Assets/2.Scripts/Model/Info/Grid/TDGridDTO.cs
  Assets/2.Scripts/Model/Info/AStarPath/TDaStarPathModel.cs

  ── Interfaces (Session 23) ──────────────────────────────────────────────────
  Assets/2.Scripts/Model/Tower/Interfaces/IDeployableDTO.cs     ← [NEW s23] Stats chung Tower + Operator
  Assets/2.Scripts/Model/Tower/Interfaces/IPlacedUnit.cs        ← [NEW s23] Unit đang đứng trên map
  Assets/2.Scripts/Model/Tower/Interfaces/ITowerRangeDTO.cs     ← DetectionRadius, IsInRange, GetCellsInRange
  Assets/2.Scripts/Model/Tower/Interfaces/ITowerFactoryControl.cs ← [s23] CreateUnit(prefab, pos, rot, slotInfo)

  ── Event Bus (Session 23) ───────────────────────────────────────────────────
  Assets/2.Scripts/Services/TDGameEventBus.cs                   ← [NEW s23] Static event hub
                                                                   OnEnemyDied, OnTowerAttacked, OnOperatorDied,
                                                                   OnLifeLost, OnVictory, OnGameOver, OnWaveStarted

  ── Tower Range (Session 11) ─────────────────────────────────────────────────
  Assets/2.Scripts/Model/Tower/TowerRange/TDRangeDTO.cs         ← [NEW s11] abstract base; virtual GetCellsInRange (bounding-box scan)
  Assets/2.Scripts/Model/Tower/TowerRange/TDOffsetRangeDTO.cs   ← [NEW s11] Arknights-style; O(n) GetCellsInRange; 4-dir auto-rotate
  Assets/2.Scripts/Editor/TowerDataDrawer.cs                    ← [NEW s11] CustomPropertyDrawer click-to-toggle 8×9 grid (Editor only)
                                                                   [s12] attackType + maxTargets; [s22] icon + ammoPrefab
  Assets/2.Scripts/Editor/OperatorDataDrawer.cs                 ← [NEW s22] CustomPropertyDrawer cho OperatorData; 5×5 grid
  Assets/2.Scripts/View/GamePlay/Projectile/TDAttackVFX.cs      ← [NEW s12] homing VFX; Single/Multiple/AOE OnImpact; AOE splash
  Assets/1.Assets/Resources/Configs/Tower Bullet Config.asset   ← rangeOffsets + attackType + maxTargets + icon per tower

  ── Control / Path ──────────────────────────────────────────────────────────
  Assets/2.Scripts/Control/PathControl/TDaStarPathControl.cs
  Assets/2.Scripts/Control/PathControl/TDPathGeneratorControl.cs
  Assets/2.Scripts/Control/PathControl/TDObstacleControl.cs
  Assets/2.Scripts/Control/PathControl/TDEnemyPathMainControl.cs
  Assets/2.Scripts/Control/PathControl/TDEnemyPathControl.cs

  ── Control / Gameplay ──────────────────────────────────────────────────────
  Assets/2.Scripts/Control/Gameplay/TDGameplayMainControl.cs
  Assets/2.Scripts/Control/Gameplay/TDPlayerLifeControl.cs      ← [Phase 4.5 NEW]
  Assets/2.Scripts/Control/Enemy/TDEnemyControl.cs
  Assets/2.Scripts/Control/Enemy/TDEnemyRegistry.cs             ← [Phase 6.1 NEW]
  Assets/2.Scripts/Control/Enemy/TDOperatorRegistry.cs          ← [Phase 14 s22 NEW]
  Assets/2.Scripts/Control/PathControl/TDEnemyPathMainControl.cs ← [Session 8: CancellationToken]
  Assets/2.Scripts/Control/Tower/TDTowerMainControl.cs           ← [s23: m_CurrentSlot; Action<TDTowerSlotInfo>]
  Assets/2.Scripts/Control/Tower/PlaceTower/TDPlaceTowerControl.cs ← [s23: nhận TDTowerSlotInfo]
  Assets/2.Scripts/Control/Tower/TowerFactory/TDTowerBehaviorMainControl.cs
  Assets/2.Scripts/Control/Tower/TowerFactory/TDTowerFactoryControl.cs  ← [s23: CreateUnit + IPlacedUnit]
  Assets/2.Scripts/Control/Tower/TowerBehavior/TDTowerBehaviorSubControl.cs
  Assets/2.Scripts/Control/TDControl.cs
  Assets/2.Scripts/Control/InputHandler/TDUserInputControl.cs

  ── View ────────────────────────────────────────────────────────────────────
  Assets/2.Scripts/View/GamePlay/TDGameplayMainView.cs           ← [4.5: life+flash]
  Assets/2.Scripts/View/GamePlay/EnemyPath/TDEnemyPathMainView.cs ← [4.5: SpawnGates]
  Assets/2.Scripts/View/GamePlay/EnemyPath/TDEnemyPathView.cs
  Assets/2.Scripts/View/GamePlay/Enemy/TDEnemyView.cs            ← [4.5: reach-end, 6.1: PathProgress+Registry,
                                                                    Phase 14 s22: operator blocking + attack timer]
  Assets/2.Scripts/View/GamePlay/Tower/TDSlotHolderMainView.cs   ← [Phase 15 s22 NEW — dynamic slot bar]
  Assets/2.Scripts/View/GamePlay/Tower/TDSlotHolderItemView.cs   ← [Phase 15 s22 NEW — slot UI item]
  Assets/2.Scripts/View/GamePlay/Tower/TDOperatorView.cs         ← [Phase 14 s22 NEW — operator HP + combat]
  Assets/2.Scripts/View/GamePlay/Tower/TDTowerWeaponView.cs      ← [6.1: tick scan + priority]
  Assets/2.Scripts/View/GamePlay/Tower/TDTowerHolderView.cs
  Assets/2.Scripts/View/GamePlay/Tower/TDTowerFactoryView.cs     ← [6.1: removed SetupSubControl]
  Assets/2.Scripts/View/GamePlay/Projectile/TDBulletsView.cs     ← [6.1: OwnerType property]
  Assets/2.Scripts/View/GamePlay/Gate/TDGateView.cs              ← [Phase 4.5 NEW]
  Assets/2.Scripts/View/GamePlay/Gate/TDGateEndView.cs           ← [Phase 4.5 NEW]

  ── Scene ───────────────────────────────────────────────────────────────────
  Assets/3.Scenes/DTGamePlay.unity
    Camera: Ortho size=14, pos (24.5,15,3.0), pitch 30°  [updated s20]
    GameMapVisualize: scale (4.4,5,2.6), pos (23.3,0,27.7) → grid 22×13  [updated s20]
    PathView/CreatePoint: (1,0,27.7)  [updated s20]

  ── Prefabs ─────────────────────────────────────────────────────────────────
  Assets/1.Assets/Resources/Prefabs/GateStart.prefab    ← [Phase 4.5 NEW, 4.6 REDESIGN]
  Assets/1.Assets/Resources/Prefabs/GateEnd.prefab      ← [Phase 4.5 NEW, 4.6 REDESIGN]
  Assets/1.Assets/Resources/Prefabs/FattyCannonG02.prefab
  Assets/1.Assets/Resources/Prefabs/FattyCatapultG02.prefab
  Assets/1.Assets/Resources/Prefabs/FattyMissileG02.prefab
  Assets/1.Assets/Resources/Prefabs/FattyMissileG03.prefab
  Assets/1.Assets/Resources/Prefabs/FattyMortarG02.prefab
  Assets/1.Assets/Resources/Prefabs/Slime.prefab              ← Normal, scale=1.5 baked; TDEnemyView (no Animator)
  Assets/1.Assets/Resources/Prefabs/SwarmFast.prefab          ← [Phase 11] Fast, scale=0.56; [Phase 13] TDEnemyView+Anim
  Assets/1.Assets/Resources/Prefabs/TurtleTank.prefab         ← [Phase 11] Tank, scale=0.74; [Phase 13] TDEnemyView+Anim
  Assets/1.Assets/Resources/Prefabs/DragonBoarBoss.prefab     ← [Phase 11] Boss, scale=0.32; [Phase 13] TDEnemyView+Anim
  Assets/1.Assets/Resources/Prefabs/OperatorKnight.prefab     ← [Phase 14 s22 NEW] Humanoid, TDOperatorView, AnimatorController
  Assets/1.Assets/Resources/Prefabs/SlotHolder.prefab         ← [Phase 15 s22 NEW] UI slot prefab: ButtonSelector, SlotIcon, Cost, DisabledOverlay
  Assets/1.Assets/Resources/Prefabs/Plane.prefab

  ── ScriptableObjects (Phase 11 NEW, Phase 13 updated) ──────────────────────
  Assets/1.Assets/Resources/Configs/Enemy Data Config.asset
    Normal: baseHP=300,  baseSpeed=3,   dieDuration=0,    prefab=Slime
    Fast:   baseHP=150,  baseSpeed=6,   dieDuration=0.63, prefab=SwarmFast
    Tank:   baseHP=900,  baseSpeed=1.5, dieDuration=2.0,  prefab=TurtleTank
    Boss:   baseHP=3000, baseSpeed=1,   dieDuration=1.33, prefab=DragonBoarBoss
    ⚠️ dieDuration cần set trong Inspector (field mới thêm Phase 13)
  Assets/1.Assets/Resources/Configs/Level Config.asset
    Level 0: Easy, totalEnemies=30, waveCount=5, waveInterval=10, spawnInterval=2
  Assets/1.Assets/Resources/Configs/Melee Operator Config.asset   ← [Phase 14 s22 NEW] TDFlyweightOperatorDataSettings
    OperatorKnight (index 0): hp, damage, attackSpeed, blockCount=2, rangeOffsets={(0,0)}

  ── Scripts (Phase 11 NEW) ────────────────────────────────────────────────────
  Assets/2.Scripts/Model/Config/TDEnemyDataSettings.cs    ← EnemyData + TDEnemyDataSettings SO
  Assets/2.Scripts/Model/Config/TDLevelConfigSettings.cs  ← LevelConfig + DifficultyRatioTable

  ── Materials (Gates) ────────────────────────────────────────────────────────
  Assets/1.Assets/Resources/Materials/Gates/GS_Edge.mat   ← [Phase 4.6 NEW] GateStart edge
  Assets/1.Assets/Resources/Materials/Gates/GS_Face.mat   ← [Phase 4.6 NEW] GateStart face
  Assets/1.Assets/Resources/Materials/Gates/GS_Glow.mat   ← [Phase 4.6 NEW] GateStart glow
  Assets/1.Assets/Resources/Materials/Gates/GS_Arrow.mat  ← [Phase 4.6 NEW] GateStart arrow
  Assets/1.Assets/Resources/Materials/Gates/GE_Edge.mat   ← [Phase 4.6 NEW] GateEnd edge
  Assets/1.Assets/Resources/Materials/Gates/GE_Face.mat   ← [Phase 4.6 NEW] GateEnd face
  Assets/1.Assets/Resources/Materials/Gates/GE_Glow.mat   ← [Phase 4.6 NEW] GateEnd glow

  ── Scripts modified (Session 6) ─────────────────────────────────────────────
  Assets/2.Scripts/View/GamePlay/EnemyPath/TDEnemyPathView.cs
    ← VisualizeAllPaths: skip gate cells (GetRange 1..count-2)

  ── Scene modified (Session 9 — Phase 6.3 HUD Scale Fix) ────────────────────
  Assets/3.Scenes/DTGamePlay.unity
    Canvas / CanvasScaler: Reference Resolution 1125×750 → 1920×1080
    Canvas/SafeArea/TxtLifePoint:
      RectTransform sizeDelta (200,50)→(300,75); anchoredPos (0,-36)→(0,-54)
      TextMeshProUGUI fontSize 36 → 54
    Canvas/SafeArea/TowerMainView:
      RectTransform sizeDelta (686,162)→(1029,243); anchoredPos (20,20)→(30,30)
      HorizontalLayoutGroup padding 6→9, spacing 6→9
    Canvas/SafeArea/TowerMainView/TowerHolder ×5:
      LayoutElement minWidth/preferredWidth 130→195; minHeight/preferredHeight 150→225
      InnerBox  sizeDelta (82,90)→(123,135); anchoredPos (0,-8)→(0,-12)
      Tower     sizeDelta (96,96)→(144,144); anchoredPos (0,4)→(0,6)
      CostBg    sizeDelta (72,36)→(108,54);  anchoredPos (-10,10)→(-15,15)
      Cost TMP  fontSize ×1.5

  ── Scripts modified (Session 8 — Phase 6.2 + Bug Fixes) ────────────────────
  Assets/2.Scripts/Model/Config/Enum/Enums.cs
    ← added RangeType enum: Area | HorizontalCone | VerticalCone
      ⚠️ REMOVED in session 11 (replaced by offset-based approach)
  Assets/2.Scripts/Model/Config/TDFlyweightTowerDataSettings.cs
    ← TowerData + damage/attackSpeed/rangeType/rangeValue fields
    ← GetData(type) pure lookup; GetPrefab/GetCost delegate to GetData
      ⚠️ Session 11: rangeType/rangeValue REMOVED; rangeOffsets (Vector2Int[]) ADDED
  Assets/2.Scripts/Model/Info/Tower Type/TDTowerBehaviorModel.cs
    ← removed 3 hardcode switch; reads damage/speed/range from SO via Setting
  Assets/2.Scripts/Model/Tower/TowerRange/TDVerticalRangeDTO.cs
    ← fixed: perpendicular band Mathf.Abs(Dot(right,...)) > 0.7f (was copy of Horizontal)
  Assets/2.Scripts/Model/Info/Grid/TDGridMainModel.cs
    ← Initialize(): ??= → = (reset singleton on scene reload)
  Assets/2.Scripts/View/GamePlay/Tower/TDTowerMainView.cs
    ← removed [SerializeField] m_DirectionThreshold; added k_DirectionThresholdRatio=0.35f
    ← UpdateGhostTransform(): platform-split via #if UNITY_ANDROID && !UNITY_EDITOR
       Mobile: ghost freezes at 35% cellSize; delta from frozen cell; direction locks until place/cancel
       Desktop: ghost always follows mouse; direction always updates (no threshold); no freeze
  Assets/2.Scripts/View/GamePlay/EnemyPath/TDEnemyPathMainView.cs
    ← SpawnGates(): SetOccupiedCell for both gate cells
    ← added m_WaveCts (CancellationTokenSource); Cancel+Dispose in OnDestroy
    ← StartWaveLoop call passes m_WaveCts.Token
  Assets/2.Scripts/Control/PathControl/TDEnemyPathMainControl.cs
    ← StartWaveLoop + SpawnWave accept CancellationToken
    ← Task.Delay uses token; try/catch OperationCanceledException
  Assets/3.Scenes/DTLoadFirst.unity
    ← RepResourceObject.objects GUID updated → Resources/Configs/Tower Bullet Config.asset
  [DELETED] Assets/2.Scripts/Model/Config/SO/Tower Bullet Config.asset
    ← SO duplicate removed (was stale, data=0 for new fields)
  Assets/1.Assets/Resources/Configs/Tower Bullet Config.asset
    ← filled: damage/attackSpeed/rangeType/rangeValue cho 5 tower types
      ⚠️ Session 11: rangeType/rangeValue replaced by rangeOffsets (Vector2Int[])

  ── Scripts modified (Session 7 — Phase 6.1) ─────────────────────────────────
  Assets/2.Scripts/Model/Config/TDFlyweightTowerDataSettings.cs
    ← removed m_Prefab/SetPrefab/pool callbacks; added GetPrefab(type) pure lookup
    ← TowerData.prefab renamed to bulletPrefab [FormerlySerializedAs]
  Assets/2.Scripts/Model/Info/Flyweight/TDFlyweightBulletFactoryModel.cs
    ← stateless pool: removed m_TowerType; Spawn(TowerType), GetPoolFor(TowerType) lazy
  Assets/2.Scripts/View/GamePlay/Projectile/TDBulletsView.cs
    ← added OwnerType property (set by Spawn, used by ReturnToPool)
  Assets/2.Scripts/Control/Tower/TowerBehavior/TDTowerBehaviorSubControl.cs
    ← removed SetupSubControl(); Attack() dùng Spawn(type) trực tiếp
  Assets/2.Scripts/View/GamePlay/Tower/TDTowerFactoryView.cs
    ← removed SetupSubControl() call
  Assets/2.Scripts/Model/Info/Tower Type/TDTowerBehaviorModel.cs
    ← removed dead settings property
  Assets/2.Scripts/Model/Tower/Interfaces/ITowerRangeDTO.cs
    ← added DetectionRadius property
  Assets/2.Scripts/Model/Tower/TowerRange/TDHorizontalRangeDTO.cs
  Assets/2.Scripts/Model/Tower/TowerRange/TDAreaRangeDTO.cs
  Assets/2.Scripts/Model/Tower/TowerRange/TDVerticalRangeDTO.cs
    ← implement DetectionRadius => m_Range
  Assets/2.Scripts/View/GamePlay/Enemy/TDEnemyView.cs
    ← added PathProgress property; Register/Unregister TDEnemyRegistry
  Assets/2.Scripts/View/GamePlay/Tower/TDTowerWeaponView.cs
    ← replaced trigger/SetTarget with tick scan (0.2s) + PathProgress priority
    ← removed SetTarget() public method
  Assets/2.Scripts/Control/TDControl.cs
    ← added TDEnemyRegistry.api = new TDEnemyRegistry()
  [DELETED] Assets/2.Scripts/View/GamePlay/Tower/TDEnemyDetector.cs

  ── Key Asset Paths ──────────────────────────────────────────────────────────
  HQ prop (GateEnd) : Assets/1.Assets/Resources/Prefabs/KenneyTower/ (TowerCrystals)

  ── Scripts modified (Session 11 — Phase 10 Grid-cell Range Refactor) ────────
  Assets/2.Scripts/Model/Info/Grid/Interfaces/IGridMainModel.cs
    ← added 3 methods: CellToWorld(Vector2Int), WorldToCell(Vector3), IsInBounds(Vector2Int)
  Assets/2.Scripts/Model/Info/Grid/TDGridMainModel.cs
    ← implemented CellToWorld (m_Grid[x,y]), WorldToCell (RoundToInt+Clamp), IsInBounds
  Assets/2.Scripts/Model/Tower/Interfaces/ITowerRangeDTO.cs
    ← added: List<Vector2Int> GetCellsInRange(Vector2Int towerCell, Quaternion towerRotation)
  Assets/2.Scripts/Model/Tower/TowerRange/TDRangeDTO.cs           [NEW]
    ← abstract base class implementing ITowerRangeDTO
    ← virtual GetCellsInRange: bounding-box scan via IsInRange (O(r²))
  Assets/2.Scripts/Model/Tower/TowerRange/TDOffsetRangeDTO.cs     [NEW]
    ← Arknights-style explicit cell offsets; overrides GetCellsInRange O(n)
    ← RotateOffset: Y=0→(dy,dx), Y=90→(dx,dy), Y=180→(-dy,-dx), Y=270→(-dx,-dy)
    ← DetectionRadius = max(offset.magnitude) * cellSize
  Assets/2.Scripts/Model/Config/TDFlyweightTowerDataSettings.cs
    ← TowerData: REMOVED rangeType (RangeType) + rangeValue (int)
    ← TowerData: ADDED rangeOffsets (Vector2Int[])
  Assets/2.Scripts/Model/Info/Tower Type/TDTowerBehaviorModel.cs
    ← GetTowerRange: simplified to new TDOffsetRangeDTO(data?.rangeOffsets)
  Assets/2.Scripts/Model/Config/Enum/Enums.cs
    ← REMOVED RangeType enum (Area | HorizontalCone | VerticalCone)
      (was added session 8; superseded by offset approach)
  Assets/2.Scripts/Editor/TowerDataDrawer.cs                      [NEW]
    ← CustomPropertyDrawer for TowerData; grid x[-1..6] y[-4..4], CELL=20f
    ← click-to-toggle cells; tower at (0,0)=blue/non-toggle; green=active; dark=inactive
    ← 7px coordinate labels on inactive cells for reference
  Assets/1.Assets/Resources/Configs/Tower Bullet Config.asset
    ← rangeOffsets populated for all 5 towers:
       Cannon:    (1,0)(2,0)(3,0)
       Catapult:  (1,-1)(1,0)(1,1)(2,-1)(2,0)(2,1)
       MissileG02:(1,0)(2,0)(3,0)(4,0)
       MissileG03:(1,-1)(1,0)(1,1)(2,-2)(2,-1)(2,0)(2,1)(2,2)(3,-1)(3,0)(3,1)
       Mortar:    (2,-1)(2,0)(2,1)(3,-1)(3,0)(3,1)(4,0)
  [DELETED] Assets/2.Scripts/Model/Tower/TowerRange/TDAreaRangeDTO.cs
  [DELETED] Assets/2.Scripts/Model/Tower/TowerRange/TDHorizontalRangeDTO.cs
  [DELETED] Assets/2.Scripts/Model/Tower/TowerRange/TDVerticalRangeDTO.cs


  ── Scripts / Assets modified (Session 16 — Phase 13 Enemy Animation) ──────
  Assets/2.Scripts/View/GamePlay/Enemy/TDEnemyView.cs
    ← [SerializeField] Animator/string/float fields → XÓA toàn bộ
    ← const string TRIGGER_WALK/GET_HIT/DIE thay SerializeField strings
    ← private Animator m_Animator ← GetComponent<Animator>() trong Awake()
    ← private float m_DieDuration ← nhận qua Initialize() tham số mới
    ← private bool m_IsDying ← guard Update + TakeDamage khi đang Die anim
    ← Initialize(key, hp, speed, dieDuration, enemyType) ← thêm dieDuration param
    ← Die(): tách riêng khỏi TakeDamage; TriggerSafe→Coroutine→PoolReturn
    ← PoolReturn() tách riêng với ReturnToPool() (reach-end)
    ← TriggerSafe(str): null-safe SetTrigger
    ← Rebind()+Update(0f) khi Initialize để reset animator state từ pool

  Assets/2.Scripts/Model/Config/TDEnemyDataSettings.cs
    ← EnemyData: ADDED float dieDuration

  Assets/2.Scripts/Control/PathControl/TDEnemyPathMainControl.cs
    ← SpawnBatch: enemy.Initialize(key, hp, speed, data.dieDuration, type)

  Assets/1.Assets/Resources/AnimatorControllers/SwarmFast-Game.controller [NEW]
    ← Idle(default)→Walk/GetHit/Die via AnyState Triggers; cross-fade 0.1f
    ← Clips từ Swarm09 controller gốc

  Assets/1.Assets/Resources/AnimatorControllers/TurtleTank-Game.controller [NEW]
    ← Idle(default)→Walk/GetHit/Die via AnyState Triggers; cross-fade 0.1f
    ← Clips từ TurtleShell controller gốc

  Assets/1.Assets/UnityStore Resources/FreeDragons/Animators/DragonBoarCTRL.controller
    ← Trigger parameter "Get Hit" → renamed "GetHit" (khớp với const)
    ← Tất cả transition conditions "Get Hit" → "GetHit"

  Assets/1.Assets/Resources/Prefabs/SwarmFast.prefab
    ← TDEnemyView ADDED; m_Animator assigned; controller = SwarmFast-Game

  Assets/1.Assets/Resources/Prefabs/TurtleTank.prefab
    ← TDEnemyView ADDED; m_Animator assigned; controller = TurtleTank-Game

  Assets/1.Assets/Resources/Prefabs/DragonBoarBoss.prefab
    ← TDEnemyView ADDED; m_Animator assigned; controller = DragonBoarCTRL

  ── Scripts / Assets modified (Session 15 — Phase 12 Range Visualization + Fixes) ───
  Assets/2.Scripts/Control/Tower/TDTowerMainControl.cs
    ← onGetTowerName: Action<string> → Action<string, TowerType>
    ← ADDED k_TowerDefs[]: static readonly (name, TowerType)[] lookup table
    ← OnSelectTowerHolder: invoke(name, type) từ k_TowerDefs[]

  Assets/2.Scripts/View/GamePlay/Tower/TDTowerMainView.cs
    ← ADDED [SerializeField] m_RangeHighlightPrefab: GameObject
    ← ADDED m_CurrentTowerType, m_RangeHighlightTiles, m_LastRangeCell, m_LastRangeRotIndex
    ← OnCreateTower signature: (string, TowerType) — lưu m_CurrentTowerType
    ← ADDED RefreshRangeHighlights(): lazy pool, GetCellsInRange, SetActive per tile
    ← ADDED HideRangeHighlights(): deactivate all + reset tracking
    ← CancelPlacement() + OnPlaceTowerSuccess(): gọi HideRangeHighlights()
    ← range highlight y: 0.07f → 0.12f (trên path tile y=0.10f)

  Assets/3.Scenes/DTGamePlay.unity
    ← TDTowerMainView.m_RangeHighlightPrefab = RangeHighlight.prefab (Inspector)
    ← Global Volume profile: Tonemapping ACES → Neutral
    ← Global Volume profile: ColorAdjustments postExposure +0.1 → -0.3

  ── Scripts modified (Session 12 — Phase 7 Attack System) ───────────────────
  Assets/2.Scripts/Model/Config/Enum/Enums.cs
    ← thêm AttackType enum: Single | Multiple | AOE
  Assets/2.Scripts/Model/Config/TDFlyweightTowerDataSettings.cs
    ← TowerData thêm: attackType (AttackType), maxTargets (int)
  Assets/2.Scripts/Model/Info/Tower Type/TDTowerBehaviorModel.cs
    ← thêm GetAttackType(type), GetMaxTargets(type) [Mathf.Clamp 2..5]
  Assets/2.Scripts/Model/Info/Flyweight/TDFlyweightBulletFactoryModel.cs
    ← pool TDAttackVFX thay TDBulletsView; auto-AddComponent nếu prefab thiếu
  Assets/2.Scripts/View/GamePlay/Projectile/TDAttackVFX.cs      [NEW]
    ← homing projectile: track target mỗi frame; fly-to-last-known nếu target chết
    ← OnImpact: Single/Multiple → TakeDamage(target); AOE → ApplyAOEDamage()
    ← ApplyAOEDamage: WorldToCell → GetCellsInRange → damage all enemies in cellSet
    ← ReturnToPool sau OnImpact; m_HasImpacted guard tránh double trigger
  Assets/2.Scripts/Control/Tower/TowerBehavior/TDTowerBehaviorSubControl.cs
    ← Attack(List<TDEnemyView>, spawnProjectile, type, rangeDTO, towerRotation)
    ← spawn 1 TDAttackVFX per target với đúng Init params
  Assets/2.Scripts/Control/Tower/TowerFactory/TDTowerBehaviorMainControl.cs
    ← AttackTargets(lastAttackTime, List<TDEnemyView>, ...) thay AttackTarget single
  Assets/2.Scripts/View/GamePlay/Tower/TDTowerWeaponView.cs
    ← m_Targets List<TDEnemyView> + m_CandidateBuffer; m_MaxTargets cached từ SO
    ← ScanForTargets(): collect in-range → Sort PathProgress desc → take top N
    ← Update(): validate per frame (null/out-of-range) → RotateTowardsPrimary → AttackTargets
    ← AttackTargets call truyền thêm m_TowerRangeDTO + m_OriQuaternion cho AOE
  Assets/2.Scripts/Editor/TowerDataDrawer.cs
    ← DrawField("attackType") luôn hiện
    ← DrawField("maxTargets") conditional (chỉ khi attackType == Multiple, index 1)
    ← GetPropertyHeight cập nhật: 7 fields base + 1 nếu Multiple
  [DELETED] Assets/2.Scripts/View/GamePlay/Projectile/TDBulletsView.cs
    ← replaced hoàn toàn bởi TDAttackVFX


════════════════════════════════════════════════════════════════════════════════
 18. PHASE 11 — MULTI-ENEMY TYPE + LEVEL CONFIG (DESIGN s13, IMPL s14 — ✅ DONE)
════════════════════════════════════════════════════════════════════════════════

  ── Implementation vs Design Delta ───────────────────────────────────────────

  ❌ path1Ratio REMOVED — design ban đầu có field này để split 50% enemy mỗi path.
     Thực tế: StartWaveLoop đã random chọn 1 trong 3 corridor mỗi wave → đủ variety.
     Fix: mỗi wave chọn allPaths[Random.Range(0, allPaths.Count)] — giữ nguyên hành vi cũ.

  ❌ Simple ChunkIntoWaves REPLACED bởi BuildWavePlans() — thiết kế mới:
     Boss wave là "event wave" với số lượng enemy nhiều hơn 2.5×, không chỉ 1 boss.
     Boss spawn LAST trong boss wave để tạo dramatic effect.

  ✅ Boss distribution algorithm: boss k → wave ceil(W×k/B)-1
     - 5 wave 1 boss → wave 5
     - 10 wave 2 boss → wave 5, 10
     - Extreme: 3 boss waves; Nightmare: 5 boss waves (2 boss/wave)

  ✅ Extreme/Nightmare constraint: waveCount≥15, totalEnemies≥75 (clamp trong BuildWavePlans)

  ✅ CONFIG_ENEMY_VISUAL_SCALE override REMOVED — scale baked vào từng prefab:
     Slime (Normal)=1.5, SwarmFast=0.56, TurtleTank=0.74, DragonBoarBoss=0.32
     Sizing basis (cellSize=2f, target widths):
       Normal ~0.98u, Fast ~0.74u, Tank ~1.15u, Boss ~1.48u

  ✅ LevelConfig fields thực tế:
     levelIndex, difficulty, totalEnemies, waveCount, waveInterval, spawnInterval
     (path1Ratio đã bị xóa)

  ── Scripts modified (Session 14 — Phase 11 Implementation) ─────────────────

  Assets/2.Scripts/Model/Config/Enum/Enums.cs
    ← thêm EnemyType enum: Normal | Fast | Tank | Boss
    ← thêm Difficulty enum: Easy | Normal | Hard | Extreme | Nightmare

  Assets/2.Scripts/Model/Config/TDEnemyDataSettings.cs   [NEW]
    ← EnemyData class: type, baseHP, baseSpeed, prefab
    ← TDEnemyDataSettings : ScriptableObject — GetData(EnemyType)

  Assets/2.Scripts/Model/Config/TDLevelConfigSettings.cs  [NEW]
    ← LevelConfig class: levelIndex, difficulty, totalEnemies, waveCount,
                          waveInterval, spawnInterval
    ← TDLevelConfigSettings : ScriptableObject — GetLevel(int)
    ← DifficultyRatioTable static class:
         RatioRow struct: normalPct, fastPct, tankPct, bossPct, hpMult, speedMult
         Get(Difficulty d) → RatioRow
         Distribute(Difficulty d, int total) → int[4] (sum = total, Fisher-remainder)

  Assets/2.Scripts/View/GamePlay/Enemy/TDEnemyView.cs
    ← thêm EnemyType { get; private set; } property
    ← Initialize(string key, float hp, float speed, EnemyType type)
       (bỏ đọc từ TDConstant.CONFIG_ENEMY_HEALTH / CONFIG_ENEMY_MOVE_SPEED)

  Assets/2.Scripts/Control/PathControl/TDEnemyPathMainControl.cs
    ← m_EnemyPool (single) → Dictionary<EnemyType, IObjectPool<TDEnemyView>> m_EnemyPools
    ← InitEnemyPools(TDEnemyDataSettings, Transform): tạo 4 pool, 1 per EnemyType
    ← ReturnEnemy: m_EnemyPools[enemy.EnemyType].Release(enemy)
    ← REMOVED: view.transform.localScale = CONFIG_ENEMY_VISUAL_SCALE (scale từ prefab)
    ← StartWaveLoop: dùng BuildWavePlans() + PauseAwareDelay(config.waveInterval)
    ← SpawnBatch: hp=baseHP×hpMult, speed=baseSpeed×speedMult; key có EnemyType
    ← BuildWavePlans(difficulty, waveCount, totalEnemies):
         GetBossParams → (bossWaves, bossPerWave, bossWaveMult)
         boss wave indices dùng ceil(W×k/B)-1 formula
         regular wave: Normal/Fast/Tank mix ratio-proportional, Fisher-Yates shuffle
         boss wave: mix (bossWaveSize - bossPerWave) + boss(es) appended last
    ← GetBossParams(Difficulty) → switch expression
    ← REMOVED: BuildShuffledQueue, ChunkIntoWaves, DistributeBossesAcrossWaves

  Assets/2.Scripts/View/GamePlay/EnemyPath/TDEnemyPathMainView.cs
    ← REMOVED: m_Slime (single prefab field)
    ← ADDED: [SerializeField] m_EnemyDataSettings : TDEnemyDataSettings
    ← ADDED: [SerializeField] m_LevelConfigSettings : TDLevelConfigSettings
    ← Initialize(): gọi InitEnemyPools(m_EnemyDataSettings, transform)
    ← OnGetAllPaths: load GetLevel(k_CurrentLevelIndex=0) → StartWaveLoop

  Assets/1.Assets/Resources/Configs/Enemy Data Config.asset   [NEW SO]
  Assets/1.Assets/Resources/Configs/Level Config.asset        [NEW SO]
  Assets/1.Assets/Resources/Prefabs/SwarmFast.prefab          [NEW — Swarm09PA scale 0.56]
  Assets/1.Assets/Resources/Prefabs/TurtleTank.prefab         [NEW — TurtleShellPolyart scale 0.74]
  Assets/1.Assets/Resources/Prefabs/DragonBoarBoss.prefab     [NEW — GoldBoarPBR scale 0.32]



  ── Mục tiêu ─────────────────────────────────────────────────────────────────
  1. Thêm 4 tier enemy (Normal / Fast / Tank / Boss) với HP + Speed + prefab khác nhau
  2. Refactor hệ thống wave: config per-màn thay vì per-wave hardcode
  3. Hỗ trợ 2 đường (path) trong 1 màn
  Scope: HP + Speed + prefab only. Chưa làm: DEF/RES stats, Air Drone (DragonSoulEater)

  ── Enemy Tier Specs ──────────────────────────────────────────────────────────

  Tier    | Base HP | Base Speed | Prefab (source)                          | Cảm giác
  --------|---------|------------|------------------------------------------|----------
  Normal  |   300   |    3.0     | SlimePolyart.prefab (đang dùng)          | Mặc định, quen thuộc
  Fast    |   150   |    6.0     | Swarm09PA.prefab (Monster Minion Survivor) | Nhỏ, nhiều chân, nhanh nhẹn
  Tank    |   900   |    1.5     | TurtleShellPolyart.prefab (RPG Monster DUO) | Mai rùa = armor heavy, chậm
  Boss    |  3000   |    1.0     | GoldBoarPBR.prefab (FreeDragons)          | Dragon boar vàng, đe dọa

  Prefab paths:
    Normal: Assets/1.Assets/UnityStore Resources/RPG Monster DUO PBR Polyart/Prefabs/PolyartDefault/SlimePolyart.prefab
    Fast:   Assets/1.Assets/UnityStore Resources/Monster Minion Survivor PBR Polyart/Prefab/Swarm09PA.prefab
    Tank:   Assets/1.Assets/UnityStore Resources/RPG Monster DUO PBR Polyart/Prefabs/PolyartDefault/TurtleShellPolyart.prefab
    Boss:   Assets/1.Assets/UnityStore Resources/FreeDragons/Prefab/DragonBoarPBR/GoldBoarPBR.prefab

  Reserve (không dùng Phase 11):
    DragonSoulEaterPBR → Air Drone (flying, future)
    BeholderPBRDefault → Boss wave 2 / Elite tier (future)
    ChestMonsterPolyartDefault → trap/puzzle mechanic (future)

  ── EnemyType Enum ────────────────────────────────────────────────────────────

  // Enums.cs
  public enum EnemyType { Normal, Fast, Tank, Boss }

  ── EnemyData Config (ScriptableObject) ──────────────────────────────────────

  [Serializable]
  public class EnemyData
  {
      public EnemyType   type;
      public float       baseHP;
      public float       baseSpeed;
      public GameObject  prefab;
  }

  [CreateAssetMenu(...)]
  public class TDEnemyDataSettings : ScriptableObject
  {
      [SerializeField] private List<EnemyData> enemies;
      public EnemyData GetData(EnemyType type) { ... }
  }

  ── LevelConfig System ────────────────────────────────────────────────────────

  Approach: Difficulty + TotalEnemies → auto-distribute (Hướng B)
  Lý do chọn B:
    - Designer set 2 field là xong (không phải 4 count riêng lẻ)
    - Difficulty ratio đảm bảo balance nhất quán
    - totalEnemies cho phép từng màn có cá tính riêng (dù cùng difficulty)
    - Dễ mở rộng: thêm difficulty level mới chỉ cần thêm 1 dòng config

  Difficulty enum:
    public enum Difficulty { Easy, Normal, Hard, Extreme, Nightmare }

  Difficulty Ratio Table (phân phối % + multiplier stats):

    Difficulty | Normal% | Fast% | Tank% | Boss% | HP mult | Speed mult
    -----------|---------|-------|-------|-------|---------|------------
    Easy       |   80%   |  15%  |   4%  |   1%  |  ×0.8   |   ×0.9
    Normal     |   60%   |  25%  |  12%  |   3%  |  ×1.0   |   ×1.0
    Hard       |   45%   |  30%  |  18%  |   7%  |  ×1.2   |   ×1.1
    Extreme    |   30%   |  30%  |  25%  |  15%  |  ×1.5   |   ×1.2
    Nightmare  |   15%   |  25%  |  35%  |  25%  |  ×2.0   |   ×1.5

  Ví dụ: difficulty=Hard, totalEnemies=50
    Normal = 45% × 50 = 22.5 → 23 con
    Fast   = 30% × 50 = 15   → 15 con
    Tank   = 18% × 50 =  9   →  9 con
    Boss   =  7% × 50 =  3.5 →  3 con
    (làm tròn, đảm bảo sum = totalEnemies)

  HP/Speed multiplier apply lúc runtime khi spawn:
    float hp    = baseData.baseHP    * difficultyConfig.hpMult;
    float speed = baseData.baseSpeed * difficultyConfig.speedMult;

  ── LevelConfig Data Structure ────────────────────────────────────────────────

  [Serializable]
  public class LevelConfig
  {
      public int        levelIndex;
      public Difficulty difficulty;
      public int        totalEnemies;       // designer set (e.g. 30, 50, 80)
      [Range(0.1f, 0.9f)]
      public float      path1Ratio;         // % enemy đi path 1; path 2 = 1 - path1Ratio
      public int        waveCount;          // số wave trong màn (e.g. 5, 8, 10)
      public float      waveInterval;       // giây giữa các wave (e.g. 15f)
      public float      spawnInterval;      // giây giữa từng enemy trong 1 wave (e.g. 1.5f)
  }

  [CreateAssetMenu(...)]
  public class TDLevelConfigSettings : ScriptableObject
  {
      [SerializeField] private List<LevelConfig> levels;
      public LevelConfig GetLevel(int index) { ... }
  }

  ── 2-Path System ─────────────────────────────────────────────────────────────

  Mỗi màn có 2 đường → enemy split theo path1Ratio:
    path1Count = Round(totalEnemies × path1Ratio)
    path2Count = totalEnemies - path1Count

  Phân phối type ratio tính trước → sau đó split:
    path1: tỉ lệ type tương đương, Boss ưu tiên path nhiều hơn (path1 nếu path1Ratio ≥ 0.5)

  Spawn: mỗi path có spawn queue riêng, spawn đan xen (hoặc đồng thời)

  ── Wave Subdivision Auto ─────────────────────────────────────────────────────

  totalEnemies chia đều cho waveCount:
    basePerWave = totalEnemies / waveCount
    remainder   = totalEnemies % waveCount (distributed to first N waves)

  Mỗi wave: spawn basePerWave (hoặc +1) enemy, cách nhau spawnInterval giây
  Giữa các wave: waveInterval giây (qua PauseAwareDelay)

  ── Spawn Logic Flow (Phase 11) ───────────────────────────────────────────────

  1. LevelConfig loaded từ SO → GetLevel(levelIndex)
  2. Auto-distribute: DifficultyRatioTable → counts per type (Normal/Fast/Tank/Boss)
  3. Split thành 2 spawn queues (path1 / path2) theo path1Ratio
  4. Chia đều theo waveCount → spawnQueuePerWave[][]
  5. For each wave:
       onWaveStart → GateView.PlaySpawnEffect()
       For each enemy in wave:
         Xác định EnemyType từ queue → lấy EnemyData → tính HP×mult, speed×mult
         GetOrSpawn prefab tương ứng từ pool
         enemy.Initialize(key, hp, speed)
         await PauseAwareDelay(spawnInterval)
       await PauseAwareDelay(waveInterval)

  ── Enemy Pool Refactor ───────────────────────────────────────────────────────

  Hiện: 1 ObjectPool<TDEnemyView> dùng Slime prefab
  Phase 11: 4 pools, 1 per EnemyType (giống bullet pool per TowerType)

  TDEnemyPathMainControl:
    Dictionary<EnemyType, ObjectPool<TDEnemyView>> m_EnemyPools
    InitEnemyPools(): tạo pool cho từng EnemyType từ EnemyDataSettings
    GetEnemy(EnemyType): m_EnemyPools[type].Get()
    ReturnEnemy(enemy): m_EnemyPools[enemy.EnemyType].Release(enemy)

  TDEnemyView refactor:
    + EnemyType enemyType { get; private set; }
    Initialize(key, hp, speed, enemyType): set stats từ tham số thay vì TDConstant

  ── TDEnemyView Stats Refactor ────────────────────────────────────────────────

  Hiện:
    m_EnemyHealth = CONFIG_ENEMY_HEALTH  (hardcode từ TDConstant)
    m_MoveSpeed   = CONFIG_ENEMY_MOVE_SPEED

  Phase 11:
    Initialize(string key, float hp, float speed, EnemyType type):
      m_EnemyHealth = hp
      m_MoveSpeed   = speed
      m_EnemyType   = type
      // reset state, subscribe, register như cũ

  ── Implement Order (Phase 11) ────────────────────────────────────────────────

  Step 1: EnemyType enum (Enums.cs)
  Step 2: EnemyData + TDEnemyDataSettings SO
  Step 3: Difficulty enum + DifficultyRatioConfig (hardcode table hoặc SO)
  Step 4: LevelConfig + TDLevelConfigSettings SO
  Step 5: TDEnemyView.Initialize() thêm hp + speed + type params
  Step 6: TDEnemyPathMainControl: 4 pools per EnemyType
  Step 7: Spawn logic: auto-distribute → queue → spawn với đúng type
  Step 8: 2-path split (path1Ratio → 2 spawn queues)
  Step 9: Assign prefabs trong TDEnemyDataSettings Inspector
  Step 10: Điền LevelConfig cho màn 1 trong TDLevelConfigSettings Inspector

  ── Prefab Notes ─────────────────────────────────────────────────────────────

  Các enemy prefab mới cần:
    - TDEnemyView component (hoặc auto-add như TDAttackVFX pattern)
    - Không cần thay đổi gì khác — logic nằm hoàn toàn ở script
    - Scale có thể khác nhau per prefab → set trong EnemyData hoặc pool createFunc

  Boss: GoldBoarPBR có animation Idle/Walk/Run/Attack — dùng Walk cho di chuyển
  Tank: TurtleShellPolyart — check animation Walk
  Fast: Swarm09PA — small insect swarm, check animation
  Normal: SlimePolyart — đang dùng, giữ nguyên

  ── Còn lại (defer khỏi Phase 11) ────────────────────────────────────────────
  ⬜ DEF / RES stats (physical/arts damage resistance)
  ⬜ Air Drone tier (DragonSoulEaterPBR — flying)
  ⬜ Weight mechanic (heavy enemies không bị knockback)
  ⬜ BeholderPBR — Elite/Boss wave 2 tier
  ⬜ Manual type count override per màn (nếu cần precision cao hơn Approach B)


================================================================================
  19. SESSION 17 — HUD REVIEW + SCENE DOCUMENTATION
================================================================================

  ── Mục tiêu session 17 ───────────────────────────────────────────────────────
    1. Thiết kế lại HUD theo phong cách Arknights (Chip icon + text)
    2. Review toàn bộ scene gameplay và ghi nhận trạng thái thực tế
    3. Map từng HUD element → Layer Lab PictoIconPack01 icon cụ thể
    4. Cập nhật roadmap & tất cả phase status
    HUD implementation trong session: đã thử → reverted theo yêu cầu user
    (user tự revert toàn bộ scene changes bằng git checkout HEAD)

  ── Scene State Confirmed (GameView 1920×1080 screenshot) ────────────────────

    Canvas: CanvasScaler 1920×1080 Scale-With-Screen-Size, Match=0.5
    SafeArea child contains all HUD elements

    [TOP BAR — full width, dark semi-transparent]
      • Red back arrow button "←"   (far-left)  → QUIT GAME action
      • Settings/Gear button "⚙"    (near-left) → function TBD
      • ☠ 36/36  [divider] ♥ 36    (center)    → enemy count + life (text symbols)
      • "2x ▶"                      (far-right) → speed toggle 1×/2×
      • "‖"                         (far-right) → pause button

    [BOTTOM-RIGHT — standalone]
      • ♦ 36                                    → gold counter (diamond text symbol)

    [BOTTOM BAR — tower cards]
      • 5 tower cards, right-aligned (NOT full-width)
      • Each card: 3D tower render | Kenney border frame | "10$" cost badge
      • Cost badge: yellow $ text on dark bg  (NOT gold sprite)

    [3D MAP]
      • Top-down 30° orthographic camera
      • White/gray battlefield plane (42×24 world units)
      • Low-poly trees/rocks at edges
      • Normal enemy (Slime) visible
      • Blue GateEnd (TowerCrystals KenneyTower prop) at far end

  ── Layer Lab PictoIconPack01 → HUD Element Mapping ─────────────────────────

    HUD Element          | Layer Lab Icon File           | Màu đề xuất
    ---------------------|-------------------------------|----------------
    Quit Button (←)      | Navigation_ArrowBack.png      | Đỏ (#FF4444)
    Settings Button      | Control_Settings.png          | Xám (#888888)
    Enemy Count (☠)      | GameElement_Skull.png         | Cam (#FF8800)
    Life Counter (♥)     | GameElement_Heart.png         | Đỏ (#FF4466)
    Speed Toggle (1×/2×) | Control_FastForward.png       | Xanh lá (#44FF88)
    Pause Button (‖)     | Control_Pause.png             | Trắng (#FFFFFF)
    Gold Counter (♦)     | GameElement_Gold.png          | Vàng (#FFD700)
    Wave Indicator       | GameElement_Flag.png          | Xanh dương (#4488FF)
    Tower Cost Badge     | GameElement_Gold.png          | Vàng (#FFD700)

    Icon size chuẩn cho HUD Chip: 48×48px
    Icon size chuẩn cho cost badge: 24×24px
    Tất cả size là bội số của 12px

    ── Chip Layout Formula (multiples of 12) ────────────────────────────────
      Chip width  = pad(12) + icon(48) + spacing(12) + text(72) + pad(12) = 156px
      Chip height = 60px
      HUDTopLeft  = chip + spacing(12) + chip = 156+12+156 = 324×60
      HUDTopRight = 156×(60+12+60) = 156×132 (VLG stacked)

  ── HUD Design Spec (Arknights-inspired, Pending Implementation) ─────────────

    Layout:
      HUDTopLeft  (anchor top-left, offset 12,-12)
        ├── LifeChip    [Heart icon | "20"]     → m_LifeText
        └── EnemyChip   [Skull icon | "0/30"]   → m_EnemyText
      HUDTopRight (anchor top-right, offset -120,-12)
        ├── GoldChip    [Gold icon | "100"]      → m_GoldText
        └── WaveChip    [Flag icon | "W 1/5"]    → m_WaveText
      PauseButton (anchor top-right, offset -12,-12) size 96×60
        └── [Pause icon]
      QuitButton  (anchor top-left, offset 12,-12 but BEHIND HUDTopLeft) — hiện tại là red arrow

    All chips:
      Background: Kenney fantasy-ui-borders panel (9-sliced)
      HLG: padding L12 R12 T6 B6, spacing 12
      Icon: Image 48×48, sprite from PictoIconPack01
      Text: TextMeshProUGUI 36pt Bold

  ── Các Issues từ Scene Review (Session 17) ──────────────────────────────────

    Issue                                  | Priority | Phase
    ---------------------------------------|----------|----------------
    HUD dùng text symbol thay sprite icon  | HIGH     | Phase 8.HUD Polish
    Tower bar right-aligned (không full-W) | MEDIUM   | Phase 8.HUD Polish
    Gold chip ở bottom-right, nên top-right| MEDIUM   | Phase 8.HUD Polish
    "$" trong cost badge → Gold.png sprite | LOW      | Phase 8.HUD Polish
    PlacementPanel GameObject missing      | HIGH     | Phase 6 (Existing ⚠️)
    Settings button chức năng chưa rõ      | LOW      | Phase 9 Polish

  ── New Phase: 8.HUD Polish (Pending) ────────────────────────────────────────

    Mục tiêu: Thay thế tất cả text symbol (☠ ♥ ♦) bằng Layer Lab sprite icons
              và fix layout issues từ scene review

    Step 1: Replace ☠ text → GameElement_Skull.png (48×48 Image)
    Step 2: Replace ♥ text → GameElement_Heart.png (48×48 Image)
    Step 3: Replace ♦ gold → GameElement_Gold.png (48×48 Image)
    Step 4: Move gold chip từ bottom-right → HUDTopRight (stacked với wave chip)
    Step 5: Fix tower bar layout → HLG fill full width (flexibleWidth=1 per card)
    Step 6: Replace "$" in cost badge → GameElement_Gold.png (24×24 Image)
    Step 7: Fix PlacementPanel missing (add GO vào scene)
    Step 8: Ensure all sizes multiples of 12; remove ContentSizeFitter
    Phụ thuộc: Phase 8 control layer DONE (gold events, wave events)

  ── Scripts cần tạo mới (Phase 8 Control Layer) ──────────────────────────────

    TDGoldControl.cs
      int Gold { get; }
      Action<int> onGoldChanged
      void AddGold(int amount)
      bool SpendGold(int amount)   → false if insufficient

    TDGameStateControl.cs
      bool IsGameOver { get; }
      bool IsVictory  { get; }
      int  EnemyAlive { get; }
      Action onVictory
      Action onDefeat
      void OnEnemyDied()           → decrement count; check win
      void OnLifeReachedZero()     → trigger defeat

    TDSpeedControl.cs
      float SpeedMultiplier { get; }  → 1f hoặc 2f
      void ToggleSpeed()              → Time.timeScale *= 2 / reset
      Action<float> onSpeedChanged

    TDGameplayHUDView.cs (View layer — subscribe to all control events)
      [SerializeField] TMP_Text m_LifeText, m_EnemyText, m_GoldText, m_WaveText
      Subscribe: onLifeChanged, onGoldChanged, onWaveChanged, onSpeedChanged
      OnVictory() → show VictoryPanel
      OnDefeat()  → show GameOverPanel

  ── Wiring Changes cần thiết ─────────────────────────────────────────────────

    TDControl.cs:
      + TDGoldControl.api      = new TDGoldControl()
      + TDGameStateControl.api = new TDGameStateControl()
      + TDSpeedControl.api     = new TDSpeedControl()

    TDEnemyView.Die():
      + TDGoldControl.api.AddGold(CONFIG_ENEMY_REWARD_GOLD)
      + TDGameStateControl.api.OnEnemyDied()

    TDPlayerLifeControl (existing):
      + onLifeChanged → TDGameStateControl.api.OnLifeReachedZero() when life==0

    TDEnemyPathMainControl (existing):
      + onAllWavesCompleted (new Action) → TDGameStateControl check

  ── Scripts modified (Session 17) ────────────────────────────────────────────
    (HUD implementation scripts were written and executed, then scene was reverted
     via git checkout HEAD at user's request. No permanent script changes this session.)

    Key decision: user confirmed red ← top-left = QUIT GAME button


================================================================================
  20. SESSION 18 — PHASE 8 CONTROL LAYER + HUD VIEW IMPLEMENTATION
================================================================================

  ── Mục tiêu ─────────────────────────────────────────────────────────────────
    Implement toàn bộ Phase 8 control layer + TDGameplayHUDView
    Ghi nhận Container hierarchy, design TDGameplayHUDView, wire tất cả events

  ── Container Hierarchy (Canvas/SafeArea/Container) ──────────────────────────
    Confirmed via script-execute:

    Container  ← TDGameplayHUDView gắn ở đây
    ├─ Header
    │  ├─ HUDButtonRight
    │  │  ├─ BackButton       [Button, ProceduralImage]  → child: Icon
    │  │  └─ SettingButton    [Button, ProceduralImage, CanvasGroup]  → child: Icon
    │  ├─ Blank
    │  ├─ MapInfo
    │  │  ├─ Background
    │  │  ├─ Line
    │  │  ├─ EnemyCount       [HLG]  → children: IconContent, TxtValue
    │  │  ├─ Line
    │  │  ├─ LifePoint        [HLG]  → children: IconContent, TxtValue
    │  │  └─ Line
    │  ├─ Blank
    │  └─ HUDButtonLeft
    │     ├─ SpeedButton      [Button, VLG] → children: TxtValue, SpeedIcon
    │     │                                   SpeedIcon/Normal, SpeedIcon/SpeedX2
    │     └─ PauseButton      [Button]      → child: Icon
    └─ Bottom
       ├─ Currency            [HLG]  → children: IconContent, TxtValue ← gold
       └─ TowerMainView       ← do TDTowerMainView quản lý (KHÔNG phải HUDView)

    FlashScreen: Canvas/SafeArea/Container/FlashScreen (child của Container)
    → cache bằng transform.Find(PATH_GAMEPLAY_SCREEN_FLASH)
    ⚠️ Session 19 update: user đã di chuyển FlashScreen từ SafeArea → Container
       (TDConstant.PATH_GAMEPLAY_SCREEN_FLASH = "FlashScreen" vẫn đúng)

  ── HUD Logic Spec ───────────────────────────────────────────────────────────
    EnemyCount : "{killed}/{total}" — killed tăng khi Die(), total từ LevelConfig
    LifePoint  : text = lives; color = đỏ nếu lives ≤ CONFIG_LIFE_LOW_THRESHOLD (5)
    Currency   : passive +1 mỗi CONFIG_GOLD_PASSIVE_RATE=5f giây + kill reward per type
                 khởi điểm CONFIG_PLAYER_STARTING_GOLD=10
    SpeedButton: default Normal active + TxtValue="x1"; click → swap Normal/SpeedX2 + "x2"
    BackButton : SceneManager.LoadScene(SCENE_LOAD_FIRST) → quit game
    FlashScreen: Time.unscaledDeltaTime (flash hiển thị cả khi pause)

  ── Config Values (TDConstant.cs — session 18) ───────────────────────────────
    CONFIG_PLAYER_STARTING_GOLD = 10
    CONFIG_GOLD_PASSIVE_RATE    = 5f     // giây per +1 gold
    CONFIG_LIFE_LOW_THRESHOLD   = 5      // <= threshold → text đỏ
    PATH_GAMEPLAY_HUD_BACK_BUTTON       = "Header/HUDButtonRight/BackButton"
    PATH_GAMEPLAY_HUD_SETTING_BUTTON    = "Header/HUDButtonRight/SettingButton"
    PATH_GAMEPLAY_HUD_ENEMY_COUNT       = "Header/MapInfo/EnemyCount/TxtValue"
    PATH_GAMEPLAY_HUD_LIFE_POINT        = "Header/MapInfo/LifePoint/TxtValue"
    PATH_GAMEPLAY_HUD_SPEED_BUTTON      = "Header/HUDButtonLeft/SpeedButton"
    PATH_GAMEPLAY_HUD_SPEED_VALUE       = "Header/HUDButtonLeft/SpeedButton/TxtValue"
    PATH_GAMEPLAY_HUD_SPEED_ICON_NORMAL = "Header/HUDButtonLeft/SpeedButton/SpeedIcon/Normal"
    PATH_GAMEPLAY_HUD_SPEED_ICON_X2     = "Header/HUDButtonLeft/SpeedButton/SpeedIcon/SpeedX2"
    PATH_GAMEPLAY_HUD_PAUSE_BUTTON      = "Header/HUDButtonLeft/PauseButton"
    PATH_GAMEPLAY_HUD_CURRENCY          = "Bottom/Currency/TxtValue"
    PATH_GAMEPLAY_SCREEN_FLASH          = "FlashScreen"

  ── New Files (Session 18) ────────────────────────────────────────────────────

  Assets/2.Scripts/Control/Gameplay/TDGoldControl.cs          [NEW]
    Gold { get }; onGoldChanged: Action<int>
    Initialize(startingGold); Tick(deltaTime) — passive income, pause-aware
    AddGold(amount); SpendGold(amount) → bool

  Assets/2.Scripts/Control/Gameplay/TDGameStateControl.cs     [NEW]
    TotalEnemies, KilledEnemies, AllWavesSpawned, IsGameEnded
    onEnemyCountChanged: Action<int,int>; onVictory: Action
    Initialize(totalEnemies); OnEnemyRemoved() [renamed from OnEnemyKilled — covers escaped too]
    OnAllWavesSpawned(); OnGameOver() → IsGameEnded=true (ngăn victory sau defeat)
    CheckVictory(): AllWavesSpawned && KilledEnemies >= TotalEnemies (guard IsGameEnded)

  Assets/2.Scripts/Control/Gameplay/TDSpeedControl.cs         [NEW]
    SpeedMultiplier: float (1f default); IsFast: bool
    onSpeedChanged: Action<float>
    Initialize() → fire onSpeedChanged(1f)
    ToggleSpeed() → swap 1f/2f; chỉ apply timeScale nếu không pause

  Assets/2.Scripts/View/GamePlay/TDGameplayHUDView.cs          [NEW]
    Gắn vào: Canvas/SafeArea/Container
    Awake: CacheReferences() — tất cả via transform.Find(PATH_xxx)
    Start: SubscribeEvents() → InitControls() → SetupButtons()
           (subscribe TRƯỚC init để nhận event sync đầu tiên)
    Update: TDGoldControl.api.Tick(Time.deltaTime)
    OnDestroy: UnsubscribeEvents()
    Handlers: OnLifeChanged (text + color + flash), OnGoldChanged, OnEnemyCountChanged,
              OnVictory (stub), OnGameOver (stub), OnPauseChanged (stub), OnSpeedChanged
    FlashScreen: coroutine với Time.unscaledDeltaTime

  ── Modified Files (Session 18) ───────────────────────────────────────────────

  Assets/2.Scripts/Model/Config/Constant/TDConstant.cs
    ← 13 PATH_GAMEPLAY_HUD_xxx constants added
    ← CONFIG_PLAYER_STARTING_GOLD=10, CONFIG_GOLD_PASSIVE_RATE=5f, CONFIG_LIFE_LOW_THRESHOLD=5

  Assets/2.Scripts/Model/Config/TDEnemyDataSettings.cs
    ← EnemyData: ADDED int goldReward
    ← ⚠️ Inspector pending: set goldReward trong Enemy Data Config.asset per type

  Assets/2.Scripts/Control/Gameplay/TDPauseControl.cs
    ← Resume(): Time.timeScale = 1f → TDSpeedControl.api?.SpeedMultiplier ?? 1f
    ← Fix: resume sau khi speed=2x sẽ restore đúng 2x thay vì reset về 1x

  Assets/2.Scripts/Control/TDControl.cs
    ← InitOtherControl(): thêm TDGoldControl.api, TDGameStateControl.api, TDSpeedControl.api

  Assets/2.Scripts/View/GamePlay/TDGameplayMainView.cs
    ← STRIPPED: bỏ m_LifeText, m_ScreenFlash, m_PausePanel, FlashScreen(), tất cả event sub
    ← CHỈ GIỮ: CheckSceneLoaded(), TDPlayerLifeControl.api.Initialize(), InitGameplay()

  Assets/2.Scripts/View/GamePlay/Enemy/TDEnemyView.cs
    ← ADDED: private int m_GoldReward
    ← Initialize(): thêm param int goldReward → m_GoldReward = goldReward
    ← Die(): TDGoldControl.api?.AddGold(m_GoldReward) + TDGameStateControl.api?.OnEnemyRemoved()
    ← [Session 19] Update() reach-end block: thêm TDGameStateControl.api?.OnEnemyRemoved()
       (enemy thoát vào gate cũng tính là removed → Victory có thể trigger đúng)

  Assets/2.Scripts/Control/PathControl/TDEnemyPathMainControl.cs
    ← SpawnBatch: enemy.Initialize(key, hp, speed, data.dieDuration, data?.goldReward ?? 0, type)
    ← StartWaveLoop: sau khi loop xong → TDGameStateControl.api?.OnAllWavesSpawned()

  Assets/2.Scripts/View/GamePlay/EnemyPath/TDEnemyPathMainView.cs
    ← OnGetAllPaths: TDGameStateControl.api?.Initialize(config.totalEnemies) trước StartWaveLoop

  ── Bug Fix (Session 18) ─────────────────────────────────────────────────────
    Speed UI "2x" khi start game:
    Root cause: InitControls() chạy trước SubscribeEvents() → onSpeedChanged fire nhưng
                không ai lắng nghe → icon giữ state mặc định trong scene (SpeedX2 active)
    Fix: đổi thứ tự trong TDGameplayHUDView.Start():
         SubscribeEvents() → InitControls() → SetupButtons()

  ── Inspector Actions (DONE — confirmed session 19) ──────────────────────────
    ✅ Gắn TDGameplayHUDView component vào Canvas/SafeArea/Container trong DTGamePlay.unity
    ✅ Set EnemyData.goldReward trong Enemy Data Config.asset per type


================================================================================
  21. SESSION 19 — PHASE 8 COMPLETION: GOLD SPEND, PAUSE PANEL, WIN/LOSE BUGS
================================================================================

  ── Mục tiêu ─────────────────────────────────────────────────────────────────
    Hoàn thiện Phase 8: gold deduction khi đặt tower, disable holder khi thiếu gold,
    PausePanel wiring, fix win/lose logic bugs, fix Stone_normal.mat material,
    xác nhận inspector actions đã xong, di chuyển FlashScreen vào Container.

  ── Modified Files (Session 19) ──────────────────────────────────────────────

  Assets/2.Scripts/Model/Config/Constant/TDConstant.cs
    ← ADDED: PATH_GAMEPLAY_HUD_PAUSE_PANEL    = "PausePanel"
    ← ADDED: PATH_GAMEPLAY_HUD_RESUME_BUTTON  = "PausePanel/ResumeButton"
    ← NOTE: PATH_GAMEPLAY_SCREEN_FLASH = "FlashScreen" vẫn dùng được
            vì FlashScreen đã được di chuyển thành child của Container

  Assets/2.Scripts/View/GamePlay/TDGameplayHUDView.cs
    ← ADDED cache: m_PausePanel  (PATH_GAMEPLAY_HUD_PAUSE_PANEL) → SetActive(false) in CacheReferences
    ← ADDED cache: m_ResumeButton (PATH_GAMEPLAY_HUD_RESUME_BUTTON)
    ← SetupButtons(): wired m_ResumeButton.onClick → OnResumeClicked()
    ← OnPauseChanged(isPaused): m_PausePanel?.SetActive(isPaused)
    ← FlashScreen path: đổi từ transform.parent.Find() → transform.Find()
      (user di chuyển FlashScreen từ SafeArea xuống Container)
    ← OnGameOver(): TDGameStateControl.api?.OnGameOver() trước Debug.Log
      (ngăn victory fire sau khi game đã thua)

  Assets/2.Scripts/Control/Gameplay/TDGameStateControl.cs
    ← RENAMED: OnEnemyKilled() → OnEnemyRemoved()
      (killed + escaped đều là "removed" → tránh nhầm logic)
    ← ADDED: IsGameEnded bool guard trong CheckVictory() + OnEnemyRemoved()
    ← ADDED: OnGameOver() → IsGameEnded = true
      (cross-check: nếu đã thua thì victory không bao giờ fire)

  Assets/2.Scripts/View/GamePlay/Enemy/TDEnemyView.cs
    ← UPDATED: Die(): OnEnemyKilled() → OnEnemyRemoved()
    ← ADDED: Update() reach-end block → TDGameStateControl.api?.OnEnemyRemoved()
      Bug fix: enemy thoát qua gate trước đây không được đếm vào KilledEnemies
      → Victory không bao giờ trigger nếu có enemy thoát.
      Fix: gọi OnEnemyRemoved() cả ở Die() và ở reach-end ReturnToPool()

  Assets/2.Scripts/View/GamePlay/Tower/TDTowerHolderView.cs
    ← ADDED: public int Cost { get; private set; }
    ← SetupTowerCost(int cost): thêm Cost = cost
    ← ADDED: public void SetInteractable(bool interactable)
                towerSelectButton.interactable = interactable

  Assets/2.Scripts/View/GamePlay/Tower/TDTowerMainView.cs
    ← Subscribe OnGoldChanged → RefreshHolderInteractability(int gold)
    ← Unsubscribe in OnDestroy
    ← ADDED: RefreshHolderInteractability(int gold):
               foreach holder → holder.SetInteractable(gold >= holder.Cost)
    ← After InitTowerHolder: RefreshHolderInteractability(TDGoldControl.api.Gold)
      (initial sync: disable holders khi start gold < cost)
    ← OnPlaceTowerSuccess(true): TDGoldControl.api.SpendGold(
                TDFlyweightBulletFactoryModel.api.Setting.GetCost(m_CurrentTowerType))

  Assets/1.Assets/UnityStore Resources/Hand Painted Stone Texture/Material/Stone_normal.mat
    ← Surface Type: Transparent (1) → Opaque (0)
    ← _ZWrite: 0 → 1
    ← RenderType tag: Transparent → Opaque
    ← RenderQueue: 3000 → -1 (default, geometry queue)
    ← disabledShaderPasses: [] (DepthOnly + SHADOWCASTER đã re-enable)
    ← keywords removed: _SURFACE_TYPE_TRANSPARENT, _ALPHAPREMULTIPLY_ON
    Lý do: path tile plane không cần alpha → transparent sorting gây conflict
            với range highlight (y=0.12), gate faces, và UI overlay

  ── Scene Changes (Session 19) ───────────────────────────────────────────────
    Canvas/SafeArea/FlashScreen    →    Canvas/SafeArea/Container/FlashScreen
    (user tự di chuyển trong Inspector — không cần code change vì path = "FlashScreen")

  ── Bug Fixes (Session 19) ───────────────────────────────────────────────────

    Bug A — Victory không trigger nếu enemy thoát gate:
    Root cause: ReturnToPool() (reach-end path) không gọi OnEnemyRemoved()
    Fix: thêm TDGameStateControl.api?.OnEnemyRemoved() trước ReturnToPool() trong
         TDEnemyView.Update() else-block

    Bug B — Victory fire nhiều lần:
    Root cause: CheckVictory() không có guard → mỗi OnEnemyRemoved đều trigger
    Fix: IsGameEnded = true ngay trong CheckVictory() trước khi invoke onVictory

    Bug C — Victory fire sau GameOver (sau khi đã thua):
    Root cause: TDGameStateControl không biết game đã over
    Fix: TDGameplayHUDView.OnGameOver() gọi TDGameStateControl.api?.OnGameOver()
         → set IsGameEnded = true → CheckVictory() luôn return early

    Bug D — PausePanel không hiện khi bấm Pause:
    Root cause: m_PausePanel và m_ResumeButton chưa được cache/wire trong HUDView
    Fix: thêm PATH constants + cache + OnPauseChanged + SetupButtons wiring

    Bug E — TDPauseControl.Resume() reset timeScale về 1× dù đang ở 2×:
    Root cause: Resume() hardcode Time.timeScale = 1f
    Fix: Time.timeScale = TDSpeedControl.api?.SpeedMultiplier ?? 1f
    (Fixed session 18, documented here for completeness)

  ── Inspector Actions (Session 19 — CONFIRMED DONE) ─────────────────────────
    ✅ TDGameplayHUDView gắn vào Canvas/SafeArea/Container
    ✅ EnemyData.goldReward set trong Enemy Data Config.asset

  ── Còn lại (Phase 8 — chưa làm) ────────────────────────────────────────────
    ⬜ VictoryPanel UI  — OnVictory() stub đã có trong TDGameplayHUDView
    ⬜ GameOverPanel UI — OnGameOver() stub đã có trong TDGameplayHUDView


================================================================================
  22. SESSION 20 — TOWERZONE VISUAL + OBSTACLE FOOTPRINT + TOWER Y-OFFSET
================================================================================

  ── Mục tiêu ─────────────────────────────────────────────────────────────────
    1. TowerZone tile: Arknights-style "gờ" cho valid placement zones
    2. Obstacle multi-cell footprint: block đúng số grid theo kích thước visual
    3. Tower y-offset: ghost + placed tower ngồi đúng trên TowerZone top face

  ── New Assets (Session 20) ──────────────────────────────────────────────────

  Assets/1.Assets/Resources/Materials/TowerZone.mat          [NEW]
    URP/Lit, Opaque, light gray (0.85, 0.85, 0.85, 1.0)
    → màu sáng để tương phản với dark ground của path

  Assets/1.Assets/Resources/Prefabs/TowerZone.prefab          [NEW]
    Cube scale (2, 0.12, 2) — khớp cellSize 2u, cao 0.12u
    center y=0 → top face tại y=0.06 (= CONFIG_TOWER_PLACE_Y)
    BoxCollider disabled (không cần physics)

  ── New Constants (Session 20) ───────────────────────────────────────────────

  Assets/2.Scripts/Model/Config/Constant/TDConstant.cs
    ← ADDED: CONFIG_TOWER_ZONE_TILE_HEIGHT = 0.12f
    ← ADDED: CONFIG_TOWER_PLACE_Y          = CONFIG_TOWER_ZONE_TILE_HEIGHT * 0.5f  // = 0.06f

  ── Modified Files (Session 20) ──────────────────────────────────────────────

  Assets/2.Scripts/Control/PathControl/TDObstacleControl.cs
    ← COMPLETE REWRITE for multi-cell footprint support
    ← onObstaclesPlaced signature changed:
       OLD: Action<List<Vector2Int> centers, List<Vector2Int> allBlocked>
       NEW: Action<List<Vector2Int> centers, List<int> prefabIndices, List<Vector2Int> allBlocked>
    ← PlaceObstacles(gridDTO, start, end, pathCells, prefabCount, footprintRadii[]):
       - pre-assigns random prefab index per obstacle at selection time
       - looks up footprintRadii[pIdx] for that prefab
       - marks all (2r+1)×(2r+1) footprint cells isWalkable=false
       - emits centers + prefabIndices + allBlocked
    ← GetFootprintCells(center, radius, gridDTO):
       - returns all cells in (2r+1)×(2r+1) square, clamped to grid bounds
    ← IsTooCloseToExisting, GetCandidateCells, Shuffle: unchanged

  Assets/2.Scripts/View/GamePlay/EnemyPath/TDEnemyPathMainView.cs
    ← REMOVED: [SerializeField] m_ObstacleFootprintRadii (was hardcoded array — removed s20)
    ← ADDED: [SerializeField] m_TowerZonePrefab : GameObject (Inspector assigned)
    ← ADDED: m_TowerZoneTiles : List<GameObject>
    ← ADDED: ComputeFootprintRadii() → int[]:
       Instantiate each prefab briefly (inactive), encapsulate Renderer bounds,
       radius = max(0, CeilToInt(max(size.x, size.z) / (2 × cellSize) - 0.5f))
       Destroy temp instance. Logs result to Console.
    ← ImplementPath(): removed hardcoded radii array;
       now calls ComputeFootprintRadii() then passes to PlaceObstacles
    ← OnObstaclesPlaced signature updated to match new Action:
       (List<Vector2Int> centers, List<int> prefabIndices, List<Vector2Int> allBlocked)
       - spawns visual using pre-assigned prefabIndices[i] (not random at spawn time)
       - calls SetOccupiedCell on ALL allBlocked cells (center + footprint)
       - calls ComputeValidTowerCells(m_GridDTO, m_AllPaths, allBlocked, start, end)
    ← OnValidTowerCellsReady(List<Vector3> validPositions):
       Instantiate m_TowerZonePrefab at (pos.x, 0, pos.z) per valid cell
       Stores in m_TowerZoneTiles list

  Assets/2.Scripts/Control/Tower/PlaceTower/TDPlaceTowerControl.cs
    ← CheckPlaceTower(): placed tower y corrected:
       OLD: CreateTower(currentTower, nearestPosition, rotation)  ← y=0
       NEW: placedPosition = (nearestPosition.x, CONFIG_TOWER_PLACE_Y, nearestPosition.z)
            CreateTower(currentTower, placedPosition, rotation)   ← y=0.06
       SetOccupiedCell still uses nearestPosition (y=0) for correct grid index

  Assets/2.Scripts/View/GamePlay/Tower/TDTowerMainView.cs
    ← UpdateGhostTransform() — BOTH mobile and desktop paths:
       OLD: m_CurrentTower.transform.position = new Vector3(x, 0f, z)
       NEW: m_CurrentTower.transform.position = new Vector3(x, CONFIG_TOWER_PLACE_Y, z)
       m_LastSnappedPos still stores y=0 for direction computation (unchanged)

  ── Scene Changes (Session 20) ───────────────────────────────────────────────

  Assets/3.Scenes/DTGamePlay.unity
    ← TDEnemyPathMainView.m_TowerZonePrefab = TowerZone.prefab (Inspector assigned via script)
    ← TDEnemyPathMainView.m_ObstacleFootprintRadii field REMOVED
       (field no longer exists — auto-computed at runtime)

  ── Scene Changes (Session 20 — continued) ───────────────────────────────────

    GameMapVisualize:
      scale   : (4.2,5,2.4) → user adjusted → (4.39,5,2.60) → final (4.4,5,2.6)
      position: (23.3,0,28) → user adjusted → (23.3,0,27.68) → final (23.3,0,27.7)
      MeshRenderer: disabled (s20 initial) → re-enabled (material: ground2)
      TDGridVisualizerView: disabled (gizmo lines no longer show in Scene View)
      Final bounds: 44u × 26u → grid 22×13, edges align with CityGround tile boundaries

    PathView/CreatePoint: (2,0,29) → (1, 0, 27.7)
    Camera: (23.3, 15, 2.02) → (24.5, 15, 3.0)

  ── Code Changes (Session 20 — continued) ────────────────────────────────────

  Assets/2.Scripts/Model/Config/Constant/TDConstant.cs
    ← CONFIG_ENEMY_END_POINT  : (20,6) → (21,6)   [grid is now 22 wide]
    ← CONFIG_PATH_Y_ZONES[0]  : (7,11) → (7,12)   [use new top row]
    ← CONFIG_PATH_Y_ZONES[2]  : (2,9)  → (2,10)   [mid sweep extended]
    ← CONFIG_TOWER_PLACE_Y    : CONFIG_TOWER_ZONE_TILE_HEIGHT*0.5f → 0.6f
                                  [tower base 0.6u above ground, clears TowerZone tile]
    ← Comment grid: 21×12 → 22×13 | grid comment updated

  ── Inspector Actions (Session 20 — CONFIRMED DONE) ─────────────────────────
    ✅ TDEnemyPathMainView.m_TowerZonePrefab = TowerZone.prefab (DTGamePlay.unity saved)
    ✅ m_ObstacleFootprintRadii field removed from component (field deleted from code)
    ✅ GameMapVisualize: scale (4.4,5,2.6) pos (23.3,0,27.7) — CityGround aligned
    ✅ PathView/CreatePoint: (1, 0, 27.7)
    ✅ Camera: (24.5, 15, 3.0)
    ✅ TDGridVisualizerView: disabled


================================================================================
  23. SESSION 22 — OPERATOR SYSTEM REFACTOR + DYNAMIC SLOT SYSTEM
================================================================================

  ── Mục tiêu ─────────────────────────────────────────────────────────────────
    1. Refactor Phase 14 (Melee Operator): thêm HP system, bidirectional combat,
       OperatorKnight prefab, TDFlyweightOperatorDataSettings SO riêng
    2. Phase 15 (Dynamic Slot System): replace 5 hardcoded holders → dynamic
       BuildSlots() từ TowerBulletConfig + OperatorConfig, max 8 slots

  ── New Files (Session 22) ───────────────────────────────────────────────────

  Assets/2.Scripts/Control/Enemy/TDOperatorRegistry.cs          [NEW]
    singleton; m_ValidPathCells HashSet; m_Operators Dictionary; m_BlockedEnemies
    RegisterPathCell / RegisterOperator / RegisterOperatorView
    CanBlock(cell): slot available; OnEnemyBlocked(cell, enemy)
    GetBlockedEnemies(cell); GetOperatorView(cell)
    UnregisterOperator(cell) → ForceUnblock(all enemies) → m_CurrentPathIndex++

  Assets/2.Scripts/Control/Tower/TowerFactory/TDTowerFactoryControl.cs  [NEW]
    CreateTower(prefab, pos, rot): Instantiate
    Detect TDOperatorView component on result:
      YES → TDOperatorView.Init(cell, data); RegisterOperator + RegisterOperatorView
      NO  → TDTowerWeaponView.Init(towerType) — regular pipeline

  Assets/2.Scripts/View/GamePlay/Tower/TDOperatorView.cs         [NEW]
    [SerializeField] m_OperatorDataIndex: int (= 0 cho OperatorKnight)
    private float m_HP, m_Damage, m_AttackSpeed; private int m_BlockCount
    private Vector2Int m_Cell
    Init(cell, OperatorData data): set stats, cache cell
    Update: attackTimer → GetBlockedEnemies → enemy.TakeDamage + TriggerSafe("Attack")
    TakeDamage(dmg): m_HP -= dmg; TriggerSafe("GetHit"); if ≤ 0 → Die()
    Die(): UnregisterOperator(m_Cell); TriggerSafe("Die"); Destroy(gameObject)

  Assets/2.Scripts/View/GamePlay/Tower/TDSlotHolderMainView.cs   [NEW]
    [SerializeField] m_SlotHolderPrefab: GameObject
    InitTowerHolder(List<TDTowerSlotInfo>): instantiate per slot, SetupIcon/Cost
    OnCreateTower(GameObject prefab, TowerType type): ghost + highlights
    RefreshHolderInteractability(int gold): disable if gold < cost
    ShowOperatorHighlights(): iterate TDOperatorRegistry.GetValidPathCells()
    ShowTowerZoneHighlights(): iterate valid tower cells (green)

  Assets/2.Scripts/View/GamePlay/Tower/TDSlotHolderItemView.cs   [NEW]
    SetupIcon(Sprite icon): m_SlotIcon.sprite = icon
    SetupCost(int cost): m_CostText.text = cost.ToString(); m_Cost = cost
    SetInteractable(bool): m_Button.interactable; m_DisabledOverlay.SetActive(!val)

  Assets/2.Scripts/Model/Config/TDFlyweightOperatorDataSettings.cs  [NEW]
    [Serializable] OperatorData: operatorPrefab, icon, cost, hp, damage,
                                  attackSpeed, blockCount, rangeOffsets
    TDFlyweightOperatorDataSettings : ScriptableObject
    List<OperatorData> m_Operators; GetData(int index): OperatorData

  Assets/2.Scripts/Editor/OperatorDataDrawer.cs                  [NEW]
    CustomPropertyDrawer<OperatorData>
    5×5 grid (x: -2..2, y: -2..2), center (0,0) = orange (operator), non-toggle
    Click → toggle rangeOffsets array (similar to TowerDataDrawer 8×9 grid)

  ── Modified Files (Session 22) ──────────────────────────────────────────────

  Assets/2.Scripts/Control/Enemy/TDEnemyView.cs
    ← Update(): on arrive at waypoint → TDOperatorRegistry.CanBlock(cell)?
      YES → m_IsBlocked=true, snap to cell center, OnEnemyBlocked(cell, this)
      Blocked loop: attackTimer → GetOperatorView(cell).TakeDamage(m_AttackDamage)
                    + TriggerSafe("Attack")
    ← Die()/ReturnToPool(): UnblockFromOperator(cell) trước khi pool release
    ← [SerializeField] m_AttackDamage, m_AttackSpeed added (từ EnemyData)

  Assets/2.Scripts/Control/Tower/TDTowerMainControl.cs
    ← BuildSlots(): merge TowerBulletConfig + OperatorConfig → List<TDTowerSlotInfo>
      max 8 slots (Fisher-Yates random pick nếu tổng > 8)
    ← OnSelectTowerHolder(index): invoke onGetTowerPrefab(prefab, towerType)
    ← onGetTowerPrefab: Action<GameObject, TowerType>
      (thay onGetTowerName: Action<string, TowerType>)

  Assets/2.Scripts/Control/Tower/PlaceTower/TDPlaceTowerControl.cs
    ← CheckPlaceTower(pos, go, TowerType): dispatch
      Operator → validate IsValidPathCell(cell) + !HasOperatorAt(cell)
               → TDTowerFactoryControl.CreateTower(operatorPrefab, ...)
      Regular  → TDTowerFactoryControl.CreateTower(towerPrefab, ...)

  Assets/2.Scripts/Model/Config/TDFlyweightTowerDataSettings.cs
    ← TowerData: ADDED icon : Sprite; ammoPrefab field exposed in Drawer

  Assets/2.Scripts/Editor/TowerDataDrawer.cs
    ← DrawField("icon") added; DrawField("ammoPrefab") added
    ← GetPropertyHeight: tính đúng số dòng với 2 fields mới

  Assets/1.Assets/Resources/Prefabs/OperatorKnight.prefab       [NEW]
    Visual: HumanSpecies_01 (Synty SidekickCharacters), scale (1,1,1)
    TDOperatorView: m_OperatorDataIndex = 0
    Animator: OperatorKnight.controller
      Idle_Battle_SingleSword (loop) → AnyState trigger "Attack" →
      Attack01_SingleSword (exitTime=0.9, speed=1.2) → back to Idle
    RPGTinyHero sword clips retargeted to HumanSpecies_01 rig

  Assets/1.Assets/Resources/Prefabs/SlotHolder.prefab           [NEW]
    HorizontalLayoutGroup root
    ├─ ButtonSelector  [Button, Image]
    ├─ SlotIcon        [Image] — sprite icon
    ├─ CostBg          [Image]
    │  └─ Cost         [TextMeshProUGUI]
    └─ DisabledOverlay [Image, alpha ~0.6]
    TDSlotHolderItemView component attached

  Assets/1.Assets/Resources/Configs/Melee Operator Config.asset [NEW]
    TDFlyweightOperatorDataSettings SO
    Entry 0 (OperatorKnight): prefab=OperatorKnight, cost=X, hp=600,
    damage=50, attackSpeed=1.0, blockCount=2, rangeOffsets={(0,0)}

  [DELETED] Assets/1.Assets/Resources/Prefabs/MeleeOperator.prefab
    → replaced by OperatorKnight.prefab

  ── Scene Changes (Session 22) ───────────────────────────────────────────────
    Canvas/SafeArea/Container:
      PlacementPanel  → Cancel button khi đang drag ghost (mobile)
      PausePanel      → SetActive khi isPaused
      GameOverPanel   → stub (OnGameOver)
      VictoryPanel    → stub (OnVictory)
      FlashScreen     → child của Container (moved s19)
    Bottom:
      TowerMainView   → replaced bởi dynamic SlotHolder instances

  ── Inspector Actions (Session 22 — TODO) ────────────────────────────────────
    ✅ Tower Bullet Config.asset → icon Sprite đã assign cho mỗi TowerData entry  [s24]
    ✅ Melee Operator Config.asset → OperatorData entries đã assign (prefab, icon, stats)  [s24]
    ⬜ TDControl.cs: thêm TDFlyweightOperatorDataSettings.api = new ...
    ⬜ Scene: TDSlotHolderMainView.m_SlotHolderPrefab = SlotHolder.prefab

  ── Design Decisions Recorded (Session 22) ───────────────────────────────────
    ✅ Multiple operator types → dùng OperatorType enum riêng (Phase 16)
       Lý do: TowerType.Operator chỉ là category marker,
              OperatorType (Knight/Medic/...) xác định data + prefab cụ thể
       TDTowerSlotInfo.operatorType field chỉ meaningful khi towerType == Operator

    ✅ Random pick khi BuildSlots() > 8 → không có priority
       Lý do: sau này sẽ có UI select trước màn để user chọn slot set

    ✅ ITowerDataProvider interface → TowerData + OperatorData implement (Phase 16)
       Lý do: xóa 5 if/else Operator branches trong TDTowerBehaviorModel
       KEY: Operator KHÔNG dùng TDTowerBehaviorModel → TDOperatorView đọc SO trực tiếp

    ✅ IPlacedUnit interface → TDTowerWeaponView + TDOperatorView implement (Phase 16)
       Lý do: xóa GetComponent<TDOperatorView> branch trong TDTowerFactoryControl
       Init(string key, TDTowerSlotInfo slot) — unified signature

    ✅ Retreat mechanic → Phase 17 (sau Phase 16 hoàn thành)
       Tách biệt Die() (enemy giết) vs Retreat() (player thu hồi)
       IPlacedUnit.OnRemove() là hook dùng cho cả 2 path

  ── Tech Stack Summary (current state) ───────────────────────────────────────
    Unity       : 2022.3.55f1 LTS
    Render      : Universal Render Pipeline (URP 14)
    Language    : C# (.NET Standard 2.1)
    UI          : uGUI + TextMeshPro
    DI          : Custom lightweight ServiceLocator (Services.DependencyInjection)
    Pooling     : Unity ObjectPool<T>
    Path        : Custom A* (TDaStarPathControl) + random waypoint generator
    Assets      : Newtonsoft.Json, DOTween, Odin Inspector (dev), RapidIcon (dev)
    Input       : Legacy Input System (Android mobile + Editor desktop split)


================================================================================
  24. SESSION 23 — PHASE 16 INTERFACE REFACTOR + EVENT BUS + CODE STYLE
================================================================================

  ── Mục tiêu ─────────────────────────────────────────────────────────────────
    1. Phase 16: IDeployableDTO + IPlacedUnit, xóa if/else Operator branches
    2. Event Bus (TDGameEventBus) cho VFX/Audio cross-cutting concerns
    3. Code style enforcement: Allman braces, 1-space operators

  ── Design decisions ─────────────────────────────────────────────────────────

    Interface naming (settled):
      IDeployableDTO — đúng convention (suffix DTO như ITowerRangeDTO, IGridCellDTO, ...)
      Rejected: ITowerDataProvider, IDeployableData, IGameDeployableData, IDeployableDataDTO

    Event Bus vs ServiceLocator vs DI:
      VFX/Audio là output-only cross-cutting — không cần bidirectional lookup
      → Event Bus là pattern đúng; ServiceLocator và DI Container redundant → sẽ xóa

  ── New Files (Session 23) ───────────────────────────────────────────────────

  Assets/2.Scripts/Model/Tower/Interfaces/IDeployableDTO.cs      [NEW]
    public interface IDeployableDTO
      TowerType, Damage, AttackSpeed, AttackType, MaxTargets, RangeOffsets

  Assets/2.Scripts/Model/Tower/Interfaces/IPlacedUnit.cs         [NEW]
    public interface IPlacedUnit
      UnitType: TowerType; Init(string key, TDTowerSlotInfo slotInfo); OnRemove()

  Assets/2.Scripts/Services/TDGameEventBus.cs                    [NEW]
    static class — events: OnEnemyDied(Vector3,EnemyType), OnEnemySpawned,
    OnTowerAttacked(Vector3,TowerType), OnOperatorDied(Vector2Int),
    OnLifeLost(Vector3), OnVictory, OnGameOver, OnWaveStarted(int)
    Raise helpers: EnemyDied(), TowerAttacked(), OperatorDied(), LifeLost(), ...

  ── Modified Files (Session 23) ──────────────────────────────────────────────

  Enums.cs
    ← + OperatorType enum: public enum OperatorType { Knight }

  TDFlyweightTowerDataSettings.cs
    ← TowerData : IDeployableDTO (explicit interface)
    ← Style: 1-space operators

  TDFlyweightOperatorDataSettings.cs
    ← OperatorData : IDeployableDTO (explicit interface)
    ← + operatorType : OperatorType field
    ← + GetData(OperatorType type) lookup
    ← Xóa GetCost/GetHp/GetDamage/GetAttackSpeed/GetBlockCount per-index helpers (dùng GetData)

  TDTowerBehaviorModel.cs
    ← Dictionary<TowerType, IDeployableDTO> m_DataMap (lazy-init)
    ← BuildDataMap(): chỉ tower thường; Operator KHÔNG vào map
    ← 5 methods: không còn if/else; tất cả qua Get(type)?.Property ?? default
    ← + InvalidateCache() để reset khi scene reload

  TDTowerSlotInfo.cs (struct)
    ← + operatorType : OperatorType

  TDTowerMainControl.cs
    ← m_CurrentSlot : TDTowerSlotInfo stored khi OnSelectTowerHolder
    ← onGetTowerPrefab : Action<TDTowerSlotInfo> (thay Action<GameObject, TowerType>)
    ← OnPlaceTower(GameObject ghost) — không cần TowerType param

  TDTowerWeaponView.cs
    ← implements IPlacedUnit
    ← IPlacedUnit.Init(key, slot) → delegate Init(key)
    ← IPlacedUnit.OnRemove() → unsubscribe onGetLastAttackTime

  TDOperatorView.cs
    ← implements IPlacedUnit
    ← m_OperatorDataIndex (int) → m_OperatorType (OperatorType)
    ← IPlacedUnit.Init(key, slot): m_OperatorType = slot.operatorType;
      GetData(m_OperatorType) → m_CurrentHp; tự RegisterOperator + RegisterOperatorView
    ← IPlacedUnit.OnRemove(): UnregisterOperator (dùng cho Die + future Retreat)
    ← Die() gọi OnRemove() + TDGameEventBus.OperatorDied(cell) + Destroy

  TDTowerFactoryControl.cs
    ← CreateUnit(prefab, pos, rot, slotInfo) — thay CreateTower
    ← GetComponent<IPlacedUnit> — không còn branch GetComponent<TDOperatorView>
    ← Key gen nội bộ
    ← onCreateUnitSuccess: Action<IPlacedUnit>; onCreateTowerSuccess: backward-compat

  ITowerFactoryControl.cs
    ← CreateUnit(prefab, pos, rot, slotInfo) — thay CreateTower

  TDPlaceTowerControl.cs
    ← CheckPlaceTower(pos, ghost, slotInfo: TDTowerSlotInfo)
    ← CheckPlaceOperator: xóa RegisterOperator — TDOperatorView.Init() tự làm

  TDSlotHolderMainView.cs
    ← OnCreateTower(TDTowerSlotInfo slot) — thay (GameObject, TowerType)
    ← OnMouseButton0Clicked → OnPlaceTower(ghost) không pass TowerType
    ← RegistryTowerControlEvents: 1-space operators, xóa alignment padding
    ← k_DirectionThresholdRatio → DIRECTION_THRESHOLD_RATIO (fix compile error)

  TDTowerFactoryView.cs
    ← Empty stub — Init logic chuyển vào TDTowerFactoryControl.CreateUnit

  TDFlyweightBulletFactoryModel.cs
    ← Xóa TowerType.Operator case trong GetPoolFor (operator không dùng bullet pool)

  ── Code style fixes (Session 23) ────────────────────────────────────────────
    Rule 1: Allman braces — { luôn trên dòng riêng
    Rule 2: 1 space quanh operators (+=, -=, =) — NO alignment padding
    Applied to: tất cả files modified session 23 + TDSlotHolderMainView

  ── Compile result ───────────────────────────────────────────────────────────
    ✅ AssetDatabase refresh: Success (0 errors)

  ── TODO còn lại ─────────────────────────────────────────────────────────────
    ⬜ Xóa ServiceLocator.cs + Injector.cs (chưa dùng, conflict với static singleton)
    ⬜ Assign OperatorType trong Melee Operator Config.asset Inspector
    ⬜ Phase 17: Retreat mechanic (IPlacedUnit.OnRemove() đã sẵn sàng)
    ⬜ State Machine cho TDEnemyView (4 bool flags → EnemyState enum)
    ⬜ VFX/Audio systems subscribe TDGameEventBus


================================================================================
  25. SESSION 25 — SCENE AUDIT + PHASE 3.9 PATHTILE VISUAL RESTORE
================================================================================

  ── Mục tiêu ─────────────────────────────────────────────────────────────────
    1. Audit toàn bộ scene và codebase so với roadmap (session 23 cuối)
    2. Kiểm tra GameMapVisualize kích thước và path config
    3. Tìm material phù hợp từ UnityStore Resources
    4. Khôi phục visual path tile bằng PathTile.prefab

  ── Scene Audit Findings (Session 25) ────────────────────────────────────────

    GameMapVisualize (verified via MCP):
      scale   : (4.4000001, 5, 2.5999999) ≈ (4.4, 5, 2.6) ✅
      position: (23.2999992, 0.04, 27.7000008)
      ⚠️  Y = 0.04 thay vì 0 — plane nổi lên 4cm. Không ảnh hưởng gameplay
           (grid offset chỉ dùng X và Z), nhưng về visual thì plane không sát đất.

    PathView/CreatePoint:
      position: (1, 0, 27.7) ✅ — khớp roadmap

    TDConstant path config:
      CONFIG_ENEMY_START_POINT  = (0, 6)           ✅
      CONFIG_ENEMY_END_POINT    = (21, 6)           ✅
      CONFIG_PATH_Y_ZONES       = [(7,12),(0,5),(2,10)] ✅
      CONFIG_GRID_CELL_SIZE     = 2f               ✅

  ── Codebase Discrepancies Found (Session 25) ────────────────────────────────

    1. TDMeleeRegistry.cs — file tên cũ, bên trong là class TDOperatorRegistry.
       Roadmap ghi "TDMeleeRegistry DELETED" nhưng thực tế chỉ đổi tên class.
       File cần rename: TDMeleeRegistry.cs → TDOperatorRegistry.cs
       ⚠️ Compile vẫn OK vì Unity không yêu cầu file name = class name cho plain C#

    2. PREFAB_MELEE_OPERATOR = "MeleeOperator" trong TDConstant.cs
       MeleeOperator.prefab đã bị xóa từ Phase 14. Constant này là dead code.
       Cần update → "OperatorKnight" hoặc xóa nếu không dùng.

    3. TDFlyweightEnemyDataSettings.cs — roadmap doc gọi là TDEnemyDataSettings.cs
       Trong code file tên là TDFlyweightEnemyDataSettings (nhất quán với Flyweight prefix).
       Chỉ là doc discrepancy, không ảnh hưởng compile.

  ── Material Research (Session 25) ──────────────────────────────────────────

    Tìm material trong Assets/1.Assets/UnityStore Resources phù hợp CityGround
    nhưng nổi hơn (cho PathTile visualization):

    Candidates analyzed:
      Stone_mobile.mat (Hand Painted Stone Texture)
        Shader: URP Lit (guid 933532a4)
        Texture: stone albedo (guid 48f4f66d) — cùng texture Stone_normal
        _BaseColor: (1,1,1,1) → full brightness, không tint
        _Smoothness: 0.5; _Metallic: 0; Opaque; _ZWrite: 1
        _EMISSION keyword enabled (EmissionColor=black → có thể thêm glow sau)
        Normal map: guid cfc6179a (khác bumpmap của Stone_normal)
        → CHỌN: sáng hơn Stone_normal (0.38,0.30,0.22 nâu tối), cùng stone texture

      Stone_normal.mat — đã dùng cho path tiles cũ; màu nâu tối (0.38,0.30,0.22)
      ground2.mat      — dùng cho GameMapVisualize ground plane; gray, tile 4×4
      Bg_Floor.mat     — solid near-black (0.078), không texture → không phù hợp

  ── Phase 3.9 Implementation (Session 25) ────────────────────────────────────

    PathTile.prefab (Assets/1.Assets/Resources/Prefabs/PathTile.prefab):
      Mesh: Unity built-in Plane (fileID 10209, native 10u×10u)
      Material trước: guid 03eccdb9 → NOT FOUND trong project (material bị xóa/mất)
      Material sau: Stone_mobile.mat (guid 094c835627124fe4bbbaa762d2240cb1) ✅

    Assets/2.Scripts/Model/Config/Constant/TDConstant.cs
      PREFAB_PATH: "Plane" → "PathTile"

    Assets/2.Scripts/View/GamePlay/EnemyPath/TDEnemyPathView.cs
      VisualizeAllPaths() TRƯỚC: chỉ mark occupied + register operator cells
                                  (comment "Path tile đã bị bỏ" — không spawn gì)
      VisualizeAllPaths() SAU:
        Load PathTile prefab từ Resources (lazy, dùng lại m_PathPrefab)
        float cellSize  = TDGridMainModel.api.cellSize   (derive từ GameMapVisualize)
        float tileScale = cellSize / 10f                 (Plane native 10u → 2u/cell)
        HashSet<Vector2Int> visitedCells — deduplicate 3 paths overlapping
        foreach path → foreach cell (skip i=0 GateStart, i=last GateEnd):
          SetOccupiedCell(worldPos)
          if !visitedCells.Add(gridCell) → continue
          RegisterPathCell(gridCell) → TDOperatorRegistry
          Instantiate PathTile at (worldPos.x, CONFIG_PATH_OFFSET_Y=0.1f, worldPos.z)
          localScale = (tileScale, existingY, tileScale)
          parent = transform (view GO)
          m_InstantiatedTiles.Add(tile)
      + using Object = UnityEngine.Object; (alias để dùng Object.Instantiate)

  ── Files Modified (Session 25) ──────────────────────────────────────────────

    Assets/2.Scripts/Model/Config/Constant/TDConstant.cs
      ← PREFAB_PATH = "Plane" → "PathTile"

    Assets/2.Scripts/View/GamePlay/EnemyPath/TDEnemyPathView.cs
      ← + using Object = UnityEngine.Object
      ← VisualizeAllPaths(): rewritten — thêm PathTile spawn logic

    Assets/1.Assets/Resources/Prefabs/PathTile.prefab
      ← m_Materials[0]: guid 03eccdb9 → guid 094c8356 (Stone_mobile.mat)

  ── TODO còn lại từ audit (Session 25) ───────────────────────────────────────
    ⬜ Rename TDMeleeRegistry.cs → TDOperatorRegistry.cs (file name mismatch)
    ⬜ Fix PREFAB_MELEE_OPERATOR = "MeleeOperator" → "OperatorKnight" hoặc xóa
    ⬜ Fix GameMapVisualize.position.Y = 0.04 → 0 (minor visual float)
    ⬜ Phase 17: Retreat Mechanic
    ⬜ Phase 8: VictoryPanel + GameOverPanel UI
    ⚠️ HP Bar depth sort: WorldSpace Canvas sortingOrder=5 bị đè bởi GateStart wireframe
       → Fix TODO: tăng sortingOrder hoặc assign Sorting Layer riêng cho HP bar canvas


================================================================================
  26. SESSION 26 — HP BAR SYSTEM (Enemy + Operator)
================================================================================

  ── Mục tiêu ─────────────────────────────────────────────────────────────────
    Thêm thanh HP hiển thị trực quan cho enemy và operator:
    - Enemy bar: màu cam, ẨN hoàn toàn cho tới khi bị đánh lần đầu
    - Operator bar: màu xanh, HIỆN NGAY khi đặt xuống map

  ── New Files (Session 26) ────────────────────────────────────────────────────

  Assets/2.Scripts/View/GamePlay/TDHPBarView.cs                   [NEW]
    public void Show()   → gameObject.SetActive(true)
    public void Hide()   → gameObject.SetActive(false)
    public void UpdateHP(float current, float max) → m_Fill.fillAmount = current/max
    public void ResetBar() → fill=1, Hide()
    Awake(): scale compensation — localScale = DESIRED_WORLD_WIDTH / (CANVAS_PX * parentLossyScale.x)
             → bar luôn rộng 1.6u trong world space bất kể parent scale (Slime=1.5, SwarmFast=0.56, ...)
    LateUpdate(): override world position = parent.position + (0, 0.12, 0)
                  override world rotation = Euler(30, 0, 0) → face camera 30° pitch
                  → enemy xoay theo hướng đi nhưng HP bar luôn đứng thẳng nhìn về camera

  Assets/1.Assets/Resources/Prefabs/HPBar_Enemy.prefab            [NEW]
    World Space Canvas, sortingOrder=5
    RectTransform sizeDelta: 200×20 px → 1.6u × 0.16u sau scale
    Background Image: dark (0.08,0.08,0.08,0.85)
    Fill Image:  Orange (1.0, 0.55, 0.1, 1.0), fillMethod=Horizontal, fillOrigin=Left
    TDHPBarView component với m_Fill assigned
    SetActive(false) mặc định

  Assets/1.Assets/Resources/Prefabs/HPBar_Operator.prefab         [NEW]
    Giống HPBar_Enemy nhưng Fill màu Blue (0.2, 0.7, 1.0, 1.0)

  ── Modified Files (Session 26) ──────────────────────────────────────────────

  Assets/2.Scripts/View/GamePlay/Enemy/TDEnemyView.cs
    ← ADDED: [SerializeField] private TDHPBarView m_HPBarView
    ← ADDED: private float m_MaxHealth
    ← Initialize(): m_MaxHealth = hp; m_HPBarView?.ResetBar()
    ← TakeDamage(): m_HPBarView?.Show(); m_HPBarView?.UpdateHP(m_EnemyHealth, m_MaxHealth)
       → Show() chỉ gọi khi bị đánh — bar ẩn hoàn toàn cho tới lần hit đầu tiên
    ← PoolReturn(): m_HPBarView?.ResetBar()
       → reset fill=1 và ẩn lại khi pool release (quan trọng cho pool reuse)

  Assets/2.Scripts/View/GamePlay/Tower/TDOperatorView.cs
    ← ADDED: [SerializeField] private TDHPBarView m_HPBarView
    ← ADDED: private float m_MaxHp
    ← IPlacedUnit.Init(): m_MaxHp = m_CurrentHp; m_HPBarView?.Show(); m_HPBarView?.UpdateHP(...)
       → Hiện ngay khi operator được đặt xuống map
    ← TakeDamage(): m_HPBarView?.UpdateHP(m_CurrentHp, m_MaxHp)

  ── Prefab Setup (via script-execute) ────────────────────────────────────────
    HPBar_Enemy.prefab nested vào:
      Slime.prefab          → TDEnemyView.m_HPBarView wired ✅
      SwarmFast.prefab      → TDEnemyView.m_HPBarView wired ✅
      TurtleTank.prefab     → TDEnemyView.m_HPBarView wired ✅
      DragonBoarBoss.prefab → TDEnemyView.m_HPBarView wired ✅
    HPBar_Operator.prefab nested vào:
      OperatorKnight.prefab → TDOperatorView.m_HPBarView wired ✅

  ── Design Notes ─────────────────────────────────────────────────────────────
    Scale formula: localScale = 1.6 / (200 * parentLossyScale.x)
      Slime (1.5)         → 0.00533
      SwarmFast (0.56)    → 0.01428
      TurtleTank (0.74)   → 0.01081
      DragonBoarBoss(0.32)→ 0.025
      Operator (1.0)      → 0.008

    LateUpdate overrides WORLD transform → Inspector shows LOCAL values
      (Y rotation ≈ -90° local khi enemy facing left là CORRECT behavior)

  ── Known Bug (session 26) ───────────────────────────────────────────────────
    ⚠️ HP bar bị đè bởi GateStart red wireframe box khi Slime đứng gần gate
       Root cause: World Space Canvas depth sort không guaranteed vs 3D opaque mesh
       Triệu chứng: bar hiển thị nhưng bị gate box che một phần
       Fix plan: tăng Canvas.sortingOrder (hiện =5) hoặc tạo Sorting Layer "UI_3D"
                 và assign cho tất cả HP bar canvas → render sau toàn bộ 3D geometry
       Status: TODO (user yêu cầu ghi chú, chưa fix)


================================================================================
  END OF DOCUMENT
================================================================================
